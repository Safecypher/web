---
phase: ad-hoc
task: 1
total_tasks: 1
status: in_progress
last_updated: 2026-03-01T15:58:22.649Z
---

<current_state>
Paused mid-todo. Just completed v1.0 milestone archival, then captured 5 team bio todos, then started check-todos and selected todo #6 "Make the calculator teaser more prominent". Brainstormed 3 options, user chose Option A (dedicated section). Todo moved to .planning/todos/done/. Implementation not yet started.
</current_state>

<completed_work>

- v1.0 milestone fully archived (MILESTONES.md, ROADMAP.md collapsed, REQUIREMENTS.md deleted, PROJECT.md evolved, RETROSPECTIVE.md created, phase dirs archived to milestones/v1.0-phases/, git tag v1.0 created)
- 5 team bio todos captured for Andy Perry, Missi Smith, Ben Jordan, Mark Phillips, Jeff Slawsky
- Todo #6 (calculator teaser prominence) moved to done/, brainstorm complete, direction decided
</completed_work>

<remaining_work>

- Implement Option A: extract calculator teaser out of DemoFormSection into its own dedicated `CalculatorTeaserSection` component
  - Create `src/components/marketing/home/CalculatorTeaserSection.tsx` — standalone section with strong outcome-led headline, large portfolio-size input, redirect to /portal/login?callbackUrl=/portal/calculator
  - Remove the teaser block from `DemoFormSection.tsx` (lines 186-205: the `mt-8 pt-8 border-t` div at the bottom)
  - Add `<CalculatorTeaserSection />` to `src/app/(marketing)/page.tsx` between `<HumanCostSection />` and `<DemoFormSection />`
</remaining_work>

<decisions_made>

- Option A chosen over B/C: extract teaser as its own section (not just style changes in-place)
- Placement: between HumanCostSection and DemoFormSection — gives calculator its own conversion moment before the high-friction demo form
- The calculator is the lower-friction CTA (no name/email — just a number → instant result), so it deserves first-class section status
- Copy direction: outcome-led ("See what CNP fraud is costing your portfolio") not question-led ("Already know your transaction volume?")
- Keep the redirect logic identical: window.location.href = `/portal/login?callbackUrl=/portal/calculator?portfolioSize=...`
</decisions_made>

<blockers>
None.
</blockers>

<context>
Current DemoFormSection.tsx has the teaser at lines 186-205 — a small `input-sm`/`btn-sm` widget with 50% opacity text tacked onto the bottom of the demo form. It's easily missed. Homepage section order: HeroSection → UrgencySection → AudiencesSection → OneIntegrationSection → ProofSection → QuoteSection → HumanCostSection → DemoFormSection. New CalculatorTeaserSection goes between HumanCostSection and DemoFormSection.
</context>

<next_action>
Start with: Create `src/components/marketing/home/CalculatorTeaserSection.tsx` — a 'use client' component with:
- Outcome-led headline: "See exactly what CNP fraud is costing your portfolio"
- Subheadline: "Enter your card account volume for an instant savings estimate."
- Large `input input-bordered input-lg` for portfolioSize (number, placeholder "e.g. 5,000,000 card accounts")
- `btn btn-primary btn-lg` CTA: "Calculate my savings →"
- handleTeaserSubmit: `window.location.href = /portal/login?callbackUrl=${encodeURIComponent('/portal/calculator?portfolioSize=' + portfolioSize)}`
- Visual: distinct background (bg-base-200 or gradient), centred layout, max-w-2xl

Then remove lines 186-205 from DemoFormSection.tsx.
Then add import + `<CalculatorTeaserSection />` to page.tsx between HumanCostSection and DemoFormSection.
</next_action>
