---
phase: 02-homepage
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/marketing/home/UrgencySection.tsx
  - src/components/marketing/home/AudiencesSection.tsx
  - src/components/marketing/home/OneIntegrationSection.tsx
autonomous: true
requirements: [HOME-02, HOME-03, HOME-04]

must_haves:
  truths:
    - "The Urgency block presents CNP fraud statistics, agentic commerce as emerging threat, and static credentials as root cause"
    - "The Urgency block primary CTA is 'Request Demo' (scrolls to #demo); secondary CTA deep-links to /portal/calculator"
    - "The Three Audiences strip (id='audiences') renders three cards: Transactions, People, Agents — each with an icon, label, 1–2 sentence pitch, and a link"
    - "On mobile the Three Audiences strip stacks vertically (flex-col); on desktop it is a three-column row"
    - "The One Integration block has 'Integrate once. Unlock everything.' headline, architecture diagram placeholder, and a TSYS reference"
    - "Urgency block uses light section colour (bg-neutral); Three Audiences and One Integration use dark (bg-base-100)"
  artifacts:
    - path: "src/components/marketing/home/UrgencySection.tsx"
      provides: "Urgency block — CNP fraud argument + CTAs"
      min_lines: 40
    - path: "src/components/marketing/home/AudiencesSection.tsx"
      provides: "Three Audiences strip — Transactions / People / Agents cards"
      min_lines: 50
    - path: "src/components/marketing/home/OneIntegrationSection.tsx"
      provides: "One Integration block — diagram placeholder + copy"
      min_lines: 30
  key_links:
    - from: "src/components/marketing/home/AudiencesSection.tsx"
      to: "section id='audiences'"
      via: "id prop on section element"
      pattern: "id=\"audiences\""
    - from: "src/components/marketing/home/UrgencySection.tsx"
      to: "/portal/calculator"
      via: "Next.js Link href"
      pattern: "portal/calculator"
    - from: "src/components/marketing/home/UrgencySection.tsx"
      to: "#demo"
      via: "Next.js Link href for primary CTA"
      pattern: "href=\"#demo\""
---

<objective>
Build the Urgency block, Three Audiences strip, and One Integration block — the three sections that follow the hero and carry the core sales argument for Heads of Fraud and Digital at card issuers.

Purpose: After the hero earns attention, the Urgency block makes the case (why act now), the Audiences strip qualifies the three buyer types, and the One Integration block removes the "big IT project" objection. Together these sections move a visitor from interested to convinced.

Output: Three fully-content section components replacing their stubs in page.tsx (stubs created by Plan 02-01). No modifications to page.tsx needed — imports already exist.
</objective>

<execution_context>
@/Users/markwright/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markwright/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-homepage/02-CONTEXT.md
@.planning/phases/02-homepage/02-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/02-homepage/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build UrgencySection — CNP fraud argument + CTAs</name>
  <files>
    src/components/marketing/home/UrgencySection.tsx
  </files>
  <action>
    Replace the Plan 02-01 stub with a full Server Component. No `'use client'`.

    Section colours (locked decision): light/off-white. Use `bg-neutral` (`oklch(25%)`) for visible contrast against the dark hero above. Add `border-t border-base-300` at the top for a hard section edge.

    Container: `mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-24 lg:py-32`.

    Layout: two-column on desktop (`grid lg:grid-cols-2 gap-16 items-start`), single column on mobile.

    Left column — the argument:
    - Eyebrow: "The Threat Is Growing" badge or small uppercase label (`text-xs uppercase tracking-widest text-accent font-semibold mb-4`)
    - Heading: "Card-not-present fraud is accelerating. Agentic commerce is about to make it catastrophic." — `text-3xl lg:text-4xl font-bold text-neutral-content leading-tight`
    - Body paragraphs (use `text-neutral-content/70 mt-4 text-base leading-relaxed`):
      - "CNP fraud costs card issuers billions annually. With no physical card present, static CVV credentials are the only line of defence — and fraudsters know it."
      - "AI agents can now initiate thousands of card-not-present transactions autonomously. Each transaction uses the same static credentials. One breach exposes every future purchase."
      - "The root cause is not fraud detection. It is static credentials. SafeCypher eliminates the root cause."

    Right column — stats + CTAs:
    - Stat block (use `card bg-base-100 p-6 rounded-xl mb-6` for contrast on the light section):
      - "£8.4bn" in large font (`text-5xl font-bold text-primary`) + "annual CNP fraud losses globally (2024)" in smaller text below
      - Divider line
      - "340%" in large font (`text-5xl font-bold text-error`) + "increase in AI-driven fraud attempts year-on-year"
    - CTA row below stat block:
      - Primary: `<Link href="#demo" className="btn btn-primary">Request Demo</Link>`
      - Secondary: `<Link href="/portal/calculator" className="btn btn-ghost">See the value for your portfolio →</Link>`

    Use `Link` from `'next/link'`. All text colours use DaisyUI semantic tokens (not hardcoded hex). Section MUST NOT have `id` — only AudiencesSection gets `id="audiences"`.
  </action>
  <verify>
    Run `npx tsc --noEmit` — zero errors.
    Run `npm run lint` — zero errors.
    Run `npm run dev`, visit http://localhost:3000 and scroll to the urgency section — confirm: light background visible against dark hero, stat numbers visible, both CTAs present, /portal/calculator link is correct href.
  </verify>
  <done>
    UrgencySection renders CNP fraud argument with statistics, primary "Request Demo" CTA (href="#demo"), and secondary calculator deep-link (href="/portal/calculator"). Light background contrasts visibly against dark hero section above.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build AudiencesSection and OneIntegrationSection</name>
  <files>
    src/components/marketing/home/AudiencesSection.tsx
    src/components/marketing/home/OneIntegrationSection.tsx
  </files>
  <action>
    **AudiencesSection.tsx** — Replace stub. Server Component. Dark section (`bg-base-100 border-t border-base-300 py-24 lg:py-32`). Add `id="audiences"` to the `<section>` element — this is the smooth-scroll target for the "See How It Works" hero CTA.

    Container: `mx-auto max-w-7xl px-4 sm:px-6 lg:px-8`.

    Section header (centred):
    - Eyebrow: "Who SafeCypher Protects" — `text-xs uppercase tracking-widest text-accent font-semibold mb-3`
    - H2: "Eliminating fraud across every transaction type" — `text-3xl lg:text-4xl font-bold text-base-content mb-4`
    - Sub-heading: "One integration. Three audiences. Zero compromise." — `text-base-content/60`

    Three audience cards — `grid md:grid-cols-3 gap-6 mt-12`. Mobile: single column (`flex-col`). Each card uses `card bg-base-200 p-8 rounded-xl`:

    Card 1 — Transactions:
    - Icon: inline SVG of a transaction/payment (credit card icon with a checkmark, or similar — use a simple 24×24 path). `text-accent` colour.
    - Label: "Transactions" — `text-xl font-bold text-base-content mt-4`
    - Pitch: "Every card-not-present transaction is validated against a dynamic security code that expires in seconds. Stolen credentials are worthless before the thief can use them."
    - Link: `<Link href="/platform" className="text-accent text-sm font-medium mt-4 block">Learn about the platform →</Link>`

    Card 2 — People:
    - Icon: person/user SVG icon
    - Label: "People"
    - Pitch: "Real cardholders get genuine protection, not friction. No app required. No extra step for the customer. The security is invisible — until a fraudster tries to use a stolen number."
    - Link: `<Link href="/dynamic-security-codes" className="text-accent text-sm font-medium mt-4 block">See how it works →</Link>`

    Card 3 — Agents:
    - Icon: robot/AI agent SVG icon (simplified — a square with an antenna or abstract geometric shape)
    - Label: "Agents"
    - Pitch: "AI agents initiating legitimate purchases are authenticated just like human cardholders. Fraudulent bots fail instantly — they have no access to the dynamic credential."
    - Link: `<Link href="/platform" className="text-accent text-sm font-medium mt-4 block">Explore agentic commerce security →</Link>`

    Use inline SVG for all icons per Phase 1 decision (no icon library). Icons should be `w-8 h-8` and `text-accent` or `stroke-current`.

    ---

    **OneIntegrationSection.tsx** — Replace stub. Server Component. Dark section (`bg-base-100 border-t border-base-300 py-24 lg:py-32`). No `id` needed.

    Container: `mx-auto max-w-7xl px-4 sm:px-6 lg:px-8`.

    Two-column layout on desktop (`grid lg:grid-cols-2 gap-16 items-center`), single column on mobile.

    Left column — copy:
    - H2: "Integrate once. Unlock everything." — `text-3xl lg:text-4xl font-bold text-base-content`
    - Body: "SafeCypher connects via a single REST API. One integration unlocks dynamic security codes for card-not-present, Safe Verify for call centre authentication, and every product in the platform." — `text-base-content/70 mt-4 leading-relaxed`
    - TSYS note: "Already integrated with TSYS for card issuer networks." — `text-sm text-base-content/50 mt-6 flex items-center gap-2` with a small inline circle/checkmark SVG.
    - CTA: `<Link href="/platform" className="btn btn-outline btn-accent mt-8">See the architecture →</Link>`

    Right column — architecture diagram placeholder:
    - A styled placeholder box: `bg-base-200 border border-base-300 rounded-xl p-8 flex items-center justify-center min-h-64`
    - Inside: a simple diagram sketch using nested divs or SVG boxes connected by lines — OR a placeholder text block styled to look like a diagram frame:
      - Top node: "Your Card Issuer System" in a `bg-base-300 rounded px-3 py-2 text-xs font-mono text-base-content/60` box
      - Arrow down (single `border-l-2 border-accent h-8 mx-auto`)
      - Middle node: "SafeCypher API" in `bg-accent/20 border border-accent/30 rounded px-3 py-2 text-xs font-mono text-accent font-bold`
      - Arrow down
      - Bottom row of three mini nodes: "DSC", "Safe Verify", "Analytics" — three `bg-base-300 rounded px-2 py-1 text-xs font-mono` boxes in a flex row
    - Caption below: "Architecture diagram — full detail on the Platform page" in `text-xs text-base-content/40 mt-4 text-center`
  </action>
  <verify>
    Run `npx tsc --noEmit` — zero errors.
    Run `npm run lint` — zero errors.
    Run `npm run dev`, visit http://localhost:3000:
    - Scroll to Three Audiences section — confirm three cards render in a row on desktop, stacked on narrow viewport (resize browser)
    - Confirm `id="audiences"` is on the section (inspect in browser devtools)
    - Click "See How It Works" in hero — smooth-scrolls to Audiences section
    - Confirm One Integration section shows diagram placeholder and TSYS reference
  </verify>
  <done>
    Three Audiences strip has id="audiences", renders Transactions/People/Agents cards with inline SVG icons, collapses to vertical stack on mobile. One Integration section has "Integrate once. Unlock everything." headline, architecture diagram placeholder, and TSYS reference. All hrefs resolve correctly. TypeScript passes.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `npm run build` passes
- AudiencesSection has `id="audiences"` — hero "See How It Works" CTA smooth-scrolls to it
- UrgencySection shows stat numbers and two CTAs including /portal/calculator deep-link
- Alternating section colours are visible: light Urgency → dark Audiences → dark OneIntegration
- Mobile layout confirmed: Audiences cards stack vertically at narrow viewport
</verification>

<success_criteria>
- HOME-02 (Urgency block with CNP stats, agentic commerce argument, calculator CTA) implemented
- HOME-03 (Three Audiences strip — Transactions/People/Agents with icons, pitches, links, mobile stack) implemented
- HOME-04 (One Integration block — architecture placeholder, "Integrate once" copy, TSYS note) implemented
- CI gates pass (lint + type-check + build)
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage/02-02-SUMMARY.md`
</output>
