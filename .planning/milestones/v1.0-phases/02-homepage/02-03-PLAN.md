---
phase: 02-homepage
plan: "03"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/marketing/home/ProofSection.tsx
  - src/components/marketing/home/HumanCostSection.tsx
autonomous: true
requirements: [HOME-05, HOME-06]

must_haves:
  truths:
    - "The Proof section displays '800,000+' transactions, '18 months', and 'zero CNP fraud' as headline stats"
    - "The Proof section shows the An Post logo (or a styled text placeholder in An Post brand green if SVG is absent) and the Irish Fintech Award badge"
    - "The Proof section links to /proof/an-post"
    - "The Proof section uses a light background (bg-neutral) for contrast against the dark section above it"
    - "The Human Cost section uses emotionally weighted copy about real cardholders — not abstract business metrics"
    - "The Human Cost section links to the Company page (/company)"
    - "The Human Cost section uses a dark background (bg-base-100)"
  artifacts:
    - path: "src/components/marketing/home/ProofSection.tsx"
      provides: "Proof section — An Post stats, logo, badge, link to /proof/an-post"
      min_lines: 40
    - path: "src/components/marketing/home/HumanCostSection.tsx"
      provides: "Human Cost section — emotional fraud victim copy, company link"
      min_lines: 35
  key_links:
    - from: "src/components/marketing/home/ProofSection.tsx"
      to: "/proof/an-post"
      via: "Next.js Link href"
      pattern: "proof/an-post"
    - from: "src/components/marketing/home/HumanCostSection.tsx"
      to: "/company"
      via: "Next.js Link href"
      pattern: "href=\"/company\""
---

<objective>
Build the Proof section (social proof via An Post case study stats) and Human Cost section (emotional resonance via real-world fraud victim framing).

Purpose: Proof converts sceptics — the An Post stat (800,000+ transactions, zero CNP fraud) is the hardest-hitting evidence on the page. Human Cost reframes the stakes from business metrics to real harm, creating emotional momentum into the demo form. Together these two sections complete the narrative arc before the call-to-action.

Output: Two content-complete section components replacing their stubs in page.tsx. No modifications to page.tsx needed.
</objective>

<execution_context>
@/Users/markwright/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markwright/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-homepage/02-CONTEXT.md
@.planning/phases/02-homepage/02-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/02-homepage/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ProofSection — An Post case study stats, logo, award badge</name>
  <files>
    src/components/marketing/home/ProofSection.tsx
  </files>
  <action>
    Replace the Plan 02-01 stub with a full Server Component. No `'use client'`.

    Section colours (locked decision): light/off-white — use `bg-neutral border-t border-base-300 py-24 lg:py-32`.
    Container: `mx-auto max-w-7xl px-4 sm:px-6 lg:px-8`.

    Layout — centred with a header, then two-column (stats left, proof card right) on desktop:

    Section header (text-neutral-content, centred — `text-center mb-16`):
    - Eyebrow: "Proven in Production" — `text-xs uppercase tracking-widest text-accent font-semibold mb-3`
    - H2: "Real results. Verified at scale." — `text-3xl lg:text-4xl font-bold text-neutral-content`
    - Sub-heading: "Not a proof of concept. Not a pilot. A production deployment protecting real transactions." — `text-neutral-content/60 mt-4 max-w-2xl mx-auto`

    Main grid (`grid lg:grid-cols-2 gap-12 items-start`):

    Left — three stat blocks stacked vertically (`space-y-8`):
    - Stat 1: "800,000+" in `text-5xl font-bold text-primary`, "card-not-present transactions processed" in `text-neutral-content/70 text-base mt-1`
    - Stat 2: "18 months" in `text-5xl font-bold text-primary`, "of live production operation" in `text-neutral-content/70 text-base mt-1`
    - Stat 3: "Zero" in `text-5xl font-bold text-accent`, "CNP fraud incidents" in `text-neutral-content/70 text-base mt-1`

    Below stats, a `Link` to the case study: `<Link href="/proof/an-post" className="btn btn-outline btn-primary mt-8">Read the An Post case study →</Link>`

    Right — proof card (`card bg-base-100 rounded-xl p-8 shadow-lg`):

    Top: An Post logo area. Check if `public/an-post-logo.svg` or `public/an-post-logo.png` exists — it almost certainly does not yet (flagged as open question in research). Use a styled text placeholder: `<span className="font-bold text-2xl" style={{ color: '#006229' }}>An Post</span>` — An Post brand green is `#006229`. Add a subtitle below: "Ireland's national postal service" in `text-neutral-content/50 text-sm`.

    Divider: `<hr className="border-base-300 my-6" />`

    Quote block: `<blockquote className="text-neutral-content/80 text-base italic leading-relaxed">` with copy: "SafeCypher's dynamic security codes gave us complete confidence in our card-not-present transactions. No fraud. No friction. No compromises." `<cite className="text-neutral-content/50 text-sm not-italic mt-3 block">— An Post Financial Services</cite>`

    Bottom: Irish Fintech Award badge placeholder. No physical asset exists — use a badge component:
    ```
    <div className="flex items-center gap-3 mt-6 p-3 bg-base-200 rounded-lg border border-base-300">
      <div className="w-10 h-10 rounded-full bg-warning/20 flex items-center justify-center flex-shrink-0">
        {/* Star SVG icon */}
        <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor" className="text-warning">
          <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
        </svg>
      </div>
      <div>
        <p className="text-xs font-semibold text-neutral-content">Irish Fintech Award</p>
        <p className="text-xs text-neutral-content/50">Fraud Prevention Category</p>
      </div>
    </div>
    ```

    Import `Link` from `'next/link'`. Use DaisyUI semantic tokens throughout — no hardcoded colours except the An Post brand green `#006229` (which is a real brand colour, not a design system colour).
  </action>
  <verify>
    Run `npx tsc --noEmit` — zero errors.
    Run `npm run lint` — zero errors.
    Run `npm run dev`, scroll to Proof section:
    - Confirm light background visible against dark section above
    - "800,000+", "18 months", "Zero" stats visible
    - "An Post" in green text renders in the proof card
    - Award badge placeholder renders
    - "Read the An Post case study →" link present (href="/proof/an-post")
  </verify>
  <done>
    ProofSection renders 800,000+ / 18 months / zero CNP fraud stats, An Post styled text placeholder in brand green, Irish Fintech Award badge placeholder, and a link to /proof/an-post. Light background contrasts against dark Three Audiences section above. TypeScript passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build HumanCostSection — emotional fraud victim framing</name>
  <files>
    src/components/marketing/home/HumanCostSection.tsx
  </files>
  <action>
    Replace the Plan 02-01 stub with a full Server Component. No `'use client'`.

    Section colours (locked decision): dark — `bg-base-100 border-t border-base-300 py-24 lg:py-32`.
    Container: `mx-auto max-w-4xl px-4 sm:px-6 lg:px-8` — narrower max-width for an editorial feel.

    The Human Cost section is intentionally different in layout — more editorial, more emotional, not a grid of features. It should feel weighty and personal.

    Layout: centred single column, max-w-4xl, generous whitespace.

    Content:

    Eyebrow: "The Human Cost" — `text-xs uppercase tracking-widest text-error font-semibold mb-6` (use error/red tone to signal the gravity — this is the only section using error colour intentionally).

    H2: "Behind every fraudulent transaction is a real person." — `text-3xl lg:text-5xl font-bold text-base-content leading-tight mb-8`

    Pull stat block (visually prominent — `border-l-4 border-error pl-8 my-10`):
    - Large number: "1 in 4" — `text-4xl font-bold text-base-content`
    - Context: "cardholders have been a victim of payment fraud. Most never recover the full amount. Many never feel safe using their card online again." — `text-base-content/70 mt-2 text-lg leading-relaxed`

    Body paragraphs (`text-base-content/70 text-base leading-relaxed space-y-4 my-8`):
    - "Card-not-present fraud is not a line item on a quarterly loss report. It is a family discovering their savings are gone. It is an elderly customer who no longer trusts online shopping. It is a small business owner waiting for a chargeback that may never come."
    - "Every static CVV that sits on a card is a vulnerability waiting to be exploited. SafeCypher closes that vulnerability permanently — not for the bank's balance sheet, but for every person whose card is in their wallet."

    Second pull stat:
    - "£360" — `text-4xl font-bold text-base-content`
    - "average loss per CNP fraud victim — often unrecoverable by the time it is reported" — `text-base-content/70 mt-2 text-lg leading-relaxed`
    (Wrap in the same bordered block style: `border-l-4 border-error pl-8 my-10`)

    Closing paragraph (`text-base-content/60 text-base leading-relaxed mt-8 italic`):
    "We built SafeCypher because we believe fraud prevention is a human obligation, not just a business efficiency."

    CTA at the bottom (`mt-10 flex flex-wrap gap-4`):
    - `<Link href="/company" className="btn btn-ghost">Our story →</Link>`
    - `<Link href="#demo" className="btn btn-primary">Request Demo</Link>`

    Import `Link` from `'next/link'`. Do not use `text-red-*` — use `text-error` (DaisyUI semantic token, maps to `oklch(65% 0.22 25)` per theme.css).
  </action>
  <verify>
    Run `npx tsc --noEmit` — zero errors.
    Run `npm run lint` — zero errors.
    Run `npm run dev`, scroll to Human Cost section:
    - Dark background against light Proof section above — visible hard edge
    - "1 in 4" and "£360" stats visible with left red border accent
    - Emotionally weighted paragraphs present (not bullet points, not feature list)
    - "Our story →" links to /company; "Request Demo" links to #demo
  </verify>
  <done>
    HumanCostSection renders editorial-style emotional fraud impact copy with "1 in 4" and "£360" stats, left-bordered pull quotes in error colour, closing mission statement, and CTAs to /company and #demo. Dark background contrasts against light Proof section above. TypeScript passes.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `npm run build` passes
- Proof section: 800,000+ / 18 months / zero stats visible, An Post green text, award badge, /proof/an-post link
- Human Cost section: editorial layout with emotional copy, not a feature grid, error-coloured pull stats, /company link
- Alternating colours correct: dark Audiences → light Proof → dark HumanCost
- No 'use client' in either file
</verification>

<success_criteria>
- HOME-05 (Proof section: 800,000+ stat, 18 months, zero CNP fraud, An Post reference, Fintech Award badge, /proof/an-post link) implemented
- HOME-06 (Human Cost section: emotional cardholder fraud copy, /company link) implemented
- CI gates pass (lint + type-check + build)
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage/02-03-SUMMARY.md`
</output>
