---
phase: 07-add-value-calculators-to-the-portal
plan: "05"
type: execute
wave: 4
depends_on:
  - "07-04"
files_modified: []
autonomous: false

requirements:
  - PORT-02
  - PORT-03
  - PORT-04
  - PORT-05
  - PORT-06
  - PORT-07
  - PORT-08
  - HOME-07

must_haves:
  truths:
    - "The complete portal funnel works end-to-end: homepage teaser -> login -> calculator with pre-filled portfolio size"
    - "Unauthenticated requests to all /portal/* routes (except /portal/login) are redirected to /portal/login"
    - "The calculator produces Year 1 Net Savings in the millions range at USD defaults (should be ~$3.87M at 5M debit, 907K credit, 25% adoption)"
    - "Breakeven days is approximately 23-24 days at USD defaults"
    - "The PDF download generates a real file with SafeCypher branding"
    - "The Attio event route accepts calculator_run payloads without 500 errors"
    - "The sensitivity table shows 25% -> ~$3.6M, 50% -> ~$7.2M, 75% -> ~$10.8M, 90% -> ~$12.9M"
  artifacts:
    - path: "src/middleware.ts"
      provides: "Auth protection verified working end-to-end"
    - path: "src/app/(portal)/portal/calculator/page.tsx"
      provides: "Calculator page fully functional"
    - path: "src/components/marketing/home/DemoFormSection.tsx"
      provides: "Teaser widget functional and redirecting correctly"
---

<objective>
Human verification of the complete Phase 7 feature set. Confirm the full portal funnel, calculator accuracy (including verified spreadsheet values), PDF export, and Attio events before phase is marked complete.

Purpose: Phase 7 is a complex multi-piece feature spanning auth, formula math, UI interactions, and external service integration. Human verification catches visual issues, broken flows, and math discrepancies that automated build checks cannot.
Output: Verified Phase 7 feature set; any issues identified for immediate fix.
</objective>

<execution_context>
@/Users/markwright/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markwright/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-add-value-calculators-to-the-portal/07-CONTEXT.md
@.planning/phases/07-add-value-calculators-to-the-portal/07-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end portal and calculator verification</name>
  <files>N/A — verification only, no files modified</files>
  <action>Run `npm run dev` and execute the full verification sequence below to confirm all Phase 7 requirements are met end-to-end.</action>
  <what-built>
    Complete portal: Supabase magic link auth protecting /portal/*, minimal dashboard, interactive value calculator with sliders + real-time results + bar chart + sensitivity table + PDF export + URL state sharing, agentic demo page, homepage teaser widget, and contact form calculator results display.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and verify the following flows:

    **Auth protection (PORT-02):**
    1. Open an incognito window. Navigate to http://localhost:3000/portal/calculator
    2. Confirm redirect to http://localhost:3000/portal/login?callbackUrl=%2Fportal%2Fcalculator
    3. Enter a real email address. Confirm "Check your email" state appears with a resend button.
    4. Click the magic link in the email. Confirm redirect lands on /portal/calculator with a session active.
    5. Refresh /portal/calculator — confirm you remain logged in (session preserved).

    **Portal dashboard (PORT-03):**
    6. Navigate to http://localhost:3000/portal
    7. Confirm: portal sidebar visible with Calculator and Demo links. Welcome content with "Open Calculator" and "View Agentic Demo" buttons.
    8. Confirm the marketing Nav and Footer are NOT present inside the portal (sidebar navigation only).

    **Calculator — basic interaction (PORT-05):**
    9. Navigate to /portal/calculator. Confirm sliders render for: Debit Accounts, Credit Accounts, Fraud Rate (debit), Year 1 Adoption Rate.
    10. Move the Debit Accounts slider. Confirm Year 1 Net Savings headline updates immediately (no visible lag).
    11. Click the displayed value on any slider — confirm it becomes an editable text input for direct number entry.
    12. Click "Advanced mode" toggle — confirm additional inputs appear (fraud rate credit, CNP transactions, etc.).

    **Calculator — math accuracy (PORT-05):**
    13. Reset all inputs to defaults (reload the page without URL params, or manually set to: 5,000,000 debit accounts, 907,000 credit accounts, 25% year 1 adoption, "Direct + Halo Effect" mode). Verify:
        - Year 1 Net Savings headline: approximately $3,866,043 (expect $3.8–3.9M range)
        - Breakeven: approximately 23–24 days
    14. The sensitivity table below the calculator should show approximately:
        - 25% adoption: ~$3.6M annual savings, ~24 days breakeven
        - 50% adoption: ~$7.2M annual savings, ~12 days breakeven
        - 90% adoption: ~$12.9M annual savings, ~7 days breakeven (labelled "Mandatory adoption")

    **Calculator — URL state sharing (PORT-05):**
    15. Adjust a slider to a non-default value. Copy the URL. Open a new browser tab and paste the URL.
    16. Confirm the sliders restore to the exact same values in the new tab.

    **Chart and visualisation (PORT-05):**
    17. Confirm the bar chart shows two bars labelled "Year 1 Savings" and "Ongoing Annual".
    18. Confirm the Ongoing bar is visibly larger than the Year 1 bar (at default inputs, $14.9M vs $3.9M).

    **PDF export (PORT-05):**
    19. Click "Download report". Confirm a PDF file downloads. Open the PDF and verify it contains:
        - "SafeCypher Value Calculator Report" as the title
        - Year 1 Net Savings figure
        - Breakeven days figure

    **Attio events (PORT-04, PORT-05):**
    20. In the terminal running `npm run dev`, check console output after loading /portal:
        - Look for `[Attio stub] { event: 'portal_login', ... }` in console
    21. Navigate to /portal/calculator. Move a slider. After approximately 500ms, look for:
        - `[Attio stub] { event: 'calculator_run', ... }` in console

    **Agentic demo (PORT-07):**
    22. Navigate to http://localhost:3000/portal/demo
    23. Confirm: "Agentic Commerce Demo" context panel renders on the right side.
    24. Check terminal console for `[Attio stub] { event: 'mockup_viewed', ... }`.

    **Homepage teaser (HOME-07, PORT-08):**
    25. Navigate to http://localhost:3000 and scroll to the demo form section.
    26. Enter "1000000" in the portfolio size input field. Click "See your savings".
    27. Confirm redirect to /portal/login?callbackUrl=... (URL should contain portfolioSize=1000000).
    28. Complete the magic link login flow. Confirm /portal/calculator loads with:
        - Debit accounts approximately 700,000
        - Credit accounts approximately 300,000

    **Contact CTA results display (PORT-06):**
    29. Navigate to http://localhost:3000/contact?from=calculator&yr1=5000000&breakeven=45
    30. Confirm: heading shows "Talk to us about your results" (not "Get in touch").
    31. Confirm: a "Your calculator results" summary box appears showing Year 1 savings = $5,000,000 and Breakeven = 45 days.

    **Attio route guard (Plan 01 fix verification):**
    32. Open browser DevTools. Navigate to /portal/calculator and adjust a slider.
    33. In the Network tab, confirm no requests to /api/attio/event from the browser (events go server-side only via Server Actions).
  </how-to-verify>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <done>All verification steps pass. Build and lint are clean. Phase 7 is feature-complete.</done>
  <resume-signal>Type "approved" if all checks pass, or describe which specific checks failed with details</resume-signal>
</task>

</tasks>

<verification>
All manual verification steps above pass.
</verification>

<success_criteria>
- Full auth-protected portal funnel working end-to-end (incognito -> login -> calculator)
- Calculator Year 1 Net Savings at USD defaults: approximately $3,866,043
- Calculator Breakeven at USD defaults: approximately 23-24 days
- Sensitivity table values approximately match verified spreadsheet values
- Interchange uplift shown as a separate display item (not merged into headline savings)
- PDF download works in browser and contains SafeCypher branding + key metrics
- Attio events appear in dev console: portal_login, calculator_run (debounced), mockup_viewed
- Homepage teaser redirects through auth to calculator with portfolioSize pre-filling accounts (70/30 split)
- Contact form shows calculator results summary with correct values
- No console errors in browser DevTools during normal usage flows
- Attio route events fire server-side only — no /api/attio/event calls visible in browser Network tab
</success_criteria>

<output>
After completion, create `.planning/phases/07-add-value-calculators-to-the-portal/07-05-SUMMARY.md`
</output>
