---
phase: 01-foundation
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/app/globals.css
  - src/styles/theme.css
  - src/components/ui/Button.tsx
  - src/components/ui/Card.tsx
  - src/components/ui/Badge.tsx
  - src/components/ui/Input.tsx
  - src/components/ui/index.ts
autonomous: true
requirements:
  - FOUND-04

must_haves:
  truths:
    - "DaisyUI components render with the SafeCypher brand theme (blue primary, dark background)"
    - "The primary colour token resolves to oklch(62% 0.214 259.815) (#3b82f6 equivalent)"
    - "The dark theme is active by default (data-theme='safecypher-dark' on html element)"
    - "Both safecypher-dark and safecypher-light themes are defined"
    - "Base UI components (Button, Card, Badge, Input) exist and accept standard props"
    - "`npm run build` passes with no CSS or TypeScript errors"
  artifacts:
    - path: "src/styles/theme.css"
      provides: "CSS custom properties brand tokens + DaisyUI theme definitions"
      contains: "@plugin \"daisyui/theme\""
    - path: "src/app/globals.css"
      provides: "Tailwind v4 entry point with DaisyUI plugin"
      contains: "@import \"tailwindcss\""
    - path: "src/components/ui/Button.tsx"
      provides: "Base Button component using DaisyUI btn class"
    - path: "src/components/ui/Card.tsx"
      provides: "Base Card component using DaisyUI card class"
    - path: "src/components/ui/index.ts"
      provides: "Barrel export for all UI components"
  key_links:
    - from: "src/app/globals.css"
      to: "src/styles/theme.css"
      via: "@import directive"
      pattern: "@import.*theme\\.css"
    - from: "src/app/globals.css"
      to: "DaisyUI plugin"
      via: "@plugin directive"
      pattern: "@plugin \"daisyui\""
    - from: "src/styles/theme.css"
      to: "DaisyUI theme system"
      via: "@plugin daisyui/theme"
      pattern: "@plugin \"daisyui/theme\""
---

<objective>
Install and configure DaisyUI v5 + Tailwind CSS v4 with the SafeCypher custom brand theme. Create the full design token set in `src/styles/theme.css` as CSS custom properties. Build the base UI component library (Button, Card, Badge, Input).

Purpose: Every page built in subsequent phases uses these tokens and components. Getting the design system right here prevents visual drift across all 20+ subsequent plans.
Output: A working DaisyUI theme with SafeCypher brand colours, dark-first rendering, and a set of typed React base components.
</objective>

<execution_context>
@/Users/markwright/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markwright/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Tailwind v4 + DaisyUI v5 with SafeCypher theme</name>
  <files>
    src/app/globals.css
    src/styles/theme.css
  </files>
  <action>
CRITICAL architecture notes before writing any files:
- Tailwind v4 is CSS-only — there is NO tailwind.config.ts for DaisyUI configuration. Do NOT create tailwind.config.ts (that's the Tailwind v3 pattern).
- DaisyUI v5 themes are defined with `@plugin "daisyui/theme"` blocks inside CSS files only.
- Import order in globals.css MUST be: `@import "tailwindcss"` → `@plugin "daisyui"` → `@import theme.css` (strict order).
- DaisyUI v5 requires OKLCH colour format. Convert #3b82f6 = oklch(62% 0.214 259.815).
- With --src-dir, the relative path from src/app/globals.css to src/styles/theme.css is `../styles/theme.css`.

1. **Rewrite `src/app/globals.css`** (replace the scaffold's Tailwind v3 content entirely):
   ```css
   @import "tailwindcss";

   @plugin "daisyui" {
     themes: safecypher-dark --default, safecypher-light;
     logs: false;
   }

   @import "../styles/theme.css";
   ```

2. **Create `src/styles/theme.css`** with the full SafeCypher brand token set:
   ```css
   /* =============================================================================
      SafeCypher Brand Tokens
      CSS custom properties for the SafeCypher design system.
      To rebrand: update values here. DaisyUI inherits these variables.
      Primary colour: #3b82f6 (blue) — canonical token until rebrand.
   ============================================================================= */

   :root {
     --brand-blue: #3b82f6;
     --font-sans: 'Outfit', system-ui, sans-serif;
     --font-serif: 'Playfair Display', Georgia, serif;
   }

   /* Dark theme (default) */
   @plugin "daisyui/theme" {
     name: "safecypher-dark";
     default: true;
     prefersdark: true;
     color-scheme: dark;

     /* Primary — brand blue #3b82f6 */
     --color-primary: oklch(62% 0.214 259.815);
     --color-primary-content: oklch(98% 0.01 260);

     /* Secondary — subtle blue-grey */
     --color-secondary: oklch(55% 0.15 260);
     --color-secondary-content: oklch(98% 0.01 260);

     /* Accent — teal for highlights */
     --color-accent: oklch(70% 0.18 195);
     --color-accent-content: oklch(10% 0.02 195);

     /* Neutral */
     --color-neutral: oklch(25% 0.01 260);
     --color-neutral-content: oklch(90% 0.01 260);

     /* Base — dark backgrounds */
     --color-base-100: oklch(10% 0.005 260);
     --color-base-200: oklch(14% 0.005 260);
     --color-base-300: oklch(18% 0.005 260);
     --color-base-content: oklch(95% 0.01 260);

     /* Semantic */
     --color-info: oklch(70% 0.18 230);
     --color-info-content: oklch(10% 0.02 230);
     --color-success: oklch(70% 0.18 150);
     --color-success-content: oklch(10% 0.02 150);
     --color-warning: oklch(80% 0.2 80);
     --color-warning-content: oklch(10% 0.02 80);
     --color-error: oklch(65% 0.22 25);
     --color-error-content: oklch(98% 0.01 25);

     /* Shape */
     --radius-selector: 0.5rem;
     --radius-field: 0.375rem;
     --radius-box: 0.75rem;
   }

   /* Light theme */
   @plugin "daisyui/theme" {
     name: "safecypher-light";
     color-scheme: light;

     /* Primary — brand blue #3b82f6 */
     --color-primary: oklch(62% 0.214 259.815);
     --color-primary-content: oklch(98% 0.01 260);

     /* Secondary */
     --color-secondary: oklch(55% 0.15 260);
     --color-secondary-content: oklch(98% 0.01 260);

     /* Accent — teal */
     --color-accent: oklch(55% 0.18 195);
     --color-accent-content: oklch(98% 0.01 195);

     /* Neutral */
     --color-neutral: oklch(85% 0.01 260);
     --color-neutral-content: oklch(20% 0.01 260);

     /* Base — light backgrounds */
     --color-base-100: oklch(100% 0 0);
     --color-base-200: oklch(96% 0.005 260);
     --color-base-300: oklch(92% 0.005 260);
     --color-base-content: oklch(15% 0.01 260);

     /* Semantic */
     --color-info: oklch(55% 0.18 230);
     --color-info-content: oklch(98% 0.01 230);
     --color-success: oklch(55% 0.18 150);
     --color-success-content: oklch(98% 0.01 150);
     --color-warning: oklch(65% 0.2 80);
     --color-warning-content: oklch(10% 0.02 80);
     --color-error: oklch(55% 0.22 25);
     --color-error-content: oklch(98% 0.01 25);

     /* Shape */
     --radius-selector: 0.5rem;
     --radius-field: 0.375rem;
     --radius-box: 0.75rem;
   }
   ```

3. Verify the `src/app/layout.tsx` already has `data-theme="safecypher-dark"` on the `<html>` element (set in Plan 01-01). If not, add it now.

4. Do NOT create `tailwind.config.ts` — Tailwind v4 does not use it for DaisyUI configuration. If create-next-app generated one, delete it (it was generated for Tailwind v3 compatibility and will conflict with v4).
  </action>
  <verify>
    Run: `npm run build`
    Expected: Build completes with no CSS errors.

    Run: `npm run dev` then visit http://localhost:3000
    Expected: Page background is dark (base-100 = oklch(10% 0.005 260)).

    Check: `grep -n "safecypher-dark" src/styles/theme.css` returns results.
    Check: `grep -n "@import \"tailwindcss\"" src/app/globals.css` returns line 1.
    Check: `grep -n "@plugin \"daisyui\"" src/app/globals.css` returns a result.
  </verify>
  <done>
    - `src/styles/theme.css` defines both safecypher-dark and safecypher-light themes using OKLCH
    - `src/app/globals.css` imports tailwindcss, then DaisyUI plugin, then theme.css (correct order)
    - `npm run build` passes with no errors
    - Dark background visible at localhost:3000
    - No tailwind.config.ts created (Tailwind v4 CSS-only)
  </done>
</task>

<task type="auto">
  <name>Task 2: Build base UI component library</name>
  <files>
    src/components/ui/Button.tsx
    src/components/ui/Card.tsx
    src/components/ui/Badge.tsx
    src/components/ui/Input.tsx
    src/components/ui/index.ts
  </files>
  <action>
Create the base UI component library. These are thin, typed wrappers around DaisyUI classes — not custom implementations. They establish the component contract that all subsequent phases consume.

1. **`src/components/ui/Button.tsx`**:
   ```tsx
   import { type ButtonHTMLAttributes, forwardRef } from 'react'

   type ButtonVariant = 'primary' | 'secondary' | 'accent' | 'ghost' | 'outline'
   type ButtonSize = 'xs' | 'sm' | 'md' | 'lg'

   interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
     variant?: ButtonVariant
     size?: ButtonSize
     loading?: boolean
     fullWidth?: boolean
   }

   const variantClass: Record<ButtonVariant, string> = {
     primary: 'btn-primary',
     secondary: 'btn-secondary',
     accent: 'btn-accent',
     ghost: 'btn-ghost',
     outline: 'btn-outline',
   }

   const sizeClass: Record<ButtonSize, string> = {
     xs: 'btn-xs',
     sm: 'btn-sm',
     md: '',
     lg: 'btn-lg',
   }

   export const Button = forwardRef<HTMLButtonElement, ButtonProps>(
     ({ variant = 'primary', size = 'md', loading = false, fullWidth = false, className = '', children, ...props }, ref) => {
       return (
         <button
           ref={ref}
           className={[
             'btn',
             variantClass[variant],
             sizeClass[size],
             fullWidth ? 'w-full' : '',
             loading ? 'loading' : '',
             className,
           ]
             .filter(Boolean)
             .join(' ')}
           disabled={loading || props.disabled}
           {...props}
         >
           {children}
         </button>
       )
     }
   )

   Button.displayName = 'Button'
   ```

2. **`src/components/ui/Card.tsx`**:
   ```tsx
   import { type HTMLAttributes } from 'react'

   interface CardProps extends HTMLAttributes<HTMLDivElement> {
     padding?: boolean
   }

   export function Card({ padding = true, className = '', children, ...props }: CardProps) {
     return (
       <div
         className={['card bg-base-200', padding ? 'card-body' : '', className]
           .filter(Boolean)
           .join(' ')}
         {...props}
       >
         {children}
       </div>
     )
   }

   interface CardBodyProps extends HTMLAttributes<HTMLDivElement> {}

   export function CardBody({ className = '', children, ...props }: CardBodyProps) {
     return (
       <div className={['card-body', className].filter(Boolean).join(' ')} {...props}>
         {children}
       </div>
     )
   }

   interface CardTitleProps extends HTMLAttributes<HTMLHeadingElement> {}

   export function CardTitle({ className = '', children, ...props }: CardTitleProps) {
     return (
       <h2 className={['card-title', className].filter(Boolean).join(' ')} {...props}>
         {children}
       </h2>
     )
   }
   ```

3. **`src/components/ui/Badge.tsx`**:
   ```tsx
   import { type HTMLAttributes } from 'react'

   type BadgeVariant = 'primary' | 'secondary' | 'accent' | 'ghost' | 'outline' | 'neutral'
   type BadgeSize = 'xs' | 'sm' | 'md' | 'lg'

   interface BadgeProps extends HTMLAttributes<HTMLSpanElement> {
     variant?: BadgeVariant
     size?: BadgeSize
   }

   const variantClass: Record<BadgeVariant, string> = {
     primary: 'badge-primary',
     secondary: 'badge-secondary',
     accent: 'badge-accent',
     ghost: 'badge-ghost',
     outline: 'badge-outline',
     neutral: 'badge-neutral',
   }

   const sizeClass: Record<BadgeSize, string> = {
     xs: 'badge-xs',
     sm: 'badge-sm',
     md: '',
     lg: 'badge-lg',
   }

   export function Badge({ variant = 'primary', size = 'md', className = '', children, ...props }: BadgeProps) {
     return (
       <span
         className={['badge', variantClass[variant], sizeClass[size], className]
           .filter(Boolean)
           .join(' ')}
         {...props}
       >
         {children}
       </span>
     )
   }
   ```

4. **`src/components/ui/Input.tsx`**:
   ```tsx
   import { type InputHTMLAttributes, forwardRef } from 'react'

   interface InputProps extends InputHTMLAttributes<HTMLInputElement> {
     label?: string
     error?: string
     helpText?: string
     fullWidth?: boolean
   }

   export const Input = forwardRef<HTMLInputElement, InputProps>(
     ({ label, error, helpText, fullWidth = false, className = '', id, ...props }, ref) => {
       const inputId = id ?? label?.toLowerCase().replace(/\s+/g, '-')

       return (
         <div className={['form-control', fullWidth ? 'w-full' : ''].filter(Boolean).join(' ')}>
           {label && (
             <label className="label" htmlFor={inputId}>
               <span className="label-text">{label}</span>
             </label>
           )}
           <input
             ref={ref}
             id={inputId}
             className={[
               'input input-bordered',
               error ? 'input-error' : '',
               fullWidth ? 'w-full' : '',
               className,
             ]
               .filter(Boolean)
               .join(' ')}
             {...props}
           />
           {(error ?? helpText) && (
             <label className="label">
               <span className={`label-text-alt ${error ? 'text-error' : 'text-base-content/60'}`}>
                 {error ?? helpText}
               </span>
             </label>
           )}
         </div>
       )
     }
   )

   Input.displayName = 'Input'
   ```

5. **`src/components/ui/index.ts`** (barrel export):
   ```ts
   export { Button } from './Button'
   export { Card, CardBody, CardTitle } from './Card'
   export { Badge } from './Badge'
   export { Input } from './Input'
   ```

6. Update `src/app/(marketing)/page.tsx` to render a demo of each component so they can be visually verified in the browser:
   ```tsx
   import { Button, Card, CardBody, CardTitle, Badge, Input } from '@/components/ui'

   export default function HomePage() {
     return (
       <main className="min-h-screen p-8 bg-base-100">
         <h1 className="text-4xl font-bold text-base-content mb-8">
           SafeCypher Design System
         </h1>
         <p className="text-base-content/60 mb-8">
           [PLACEHOLDER — Homepage content in Phase 2]
         </p>

         <section className="space-y-8">
           <div className="space-x-4">
             <Button variant="primary">Request Demo</Button>
             <Button variant="secondary">Learn More</Button>
             <Button variant="accent">Get Started</Button>
             <Button variant="ghost">Ghost</Button>
             <Button variant="outline">Outline</Button>
           </div>

           <div className="space-x-4">
             <Badge variant="primary">Primary</Badge>
             <Badge variant="accent">New</Badge>
             <Badge variant="neutral">Beta</Badge>
           </div>

           <Card className="max-w-md">
             <CardBody>
               <CardTitle>An Post Partnership</CardTitle>
               <p className="text-base-content/80">
                 800,000+ transactions. Zero CNP fraud.
               </p>
               <Button variant="primary" size="sm">Learn more</Button>
             </CardBody>
           </Card>

           <Input
             label="Work Email"
             type="email"
             placeholder="you@company.com"
             helpText="We'll send you a demo link"
             fullWidth
             className="max-w-sm"
           />
         </section>
       </main>
     )
   }
   ```
  </action>
  <verify>
    Run: `npx tsc --noEmit`
    Expected: Zero type errors.

    Run: `npm run build`
    Expected: Build passes.

    Run: `npm run dev` then visit http://localhost:3000
    Expected: Dark background, blue "Request Demo" button visible, card component renders, Badge renders.

    Run: `npm run lint`
    Expected: Zero lint errors.
  </verify>
  <done>
    - Button, Card, Badge, Input components exist in src/components/ui/
    - All components are typed with no `any` types
    - Barrel export at src/components/ui/index.ts exports all components
    - Homepage placeholder renders the design system demo with dark background and brand blue button
    - `npx tsc --noEmit` and `npm run build` both pass
  </done>
</task>

</tasks>

<verification>
1. `npm run build` — passes with no errors
2. `npx tsc --noEmit` — zero type errors
3. `npm run lint` — zero lint errors
4. `cat src/app/globals.css` — shows @import "tailwindcss" on line 1, @plugin "daisyui" next, @import "../styles/theme.css" after
5. `cat src/styles/theme.css` — shows both safecypher-dark and safecypher-light @plugin "daisyui/theme" blocks
6. Visit http://localhost:3000 — dark background, blue primary button renders correctly
7. No tailwind.config.ts exists (Tailwind v4 is CSS-only)
8. `ls src/components/ui/` — Button.tsx, Card.tsx, Badge.tsx, Input.tsx, index.ts all present
</verification>

<success_criteria>
- DaisyUI v5 + Tailwind v4 configured with CSS-only approach (no tailwind.config.ts)
- SafeCypher dark and light themes defined in src/styles/theme.css using OKLCH colour tokens
- Primary colour is oklch(62% 0.214 259.815) — the #3b82f6 blue per user decision
- Dark theme is active by default (data-theme="safecypher-dark")
- Button, Card, Badge, Input components typed and exported from @/components/ui
- Full build pipeline passes: lint + type-check + build
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` following the summary template.
</output>
