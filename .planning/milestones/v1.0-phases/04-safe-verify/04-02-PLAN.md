---
phase: 04-safe-verify
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/components/marketing/safe-verify/SvUseCaseTabs.tsx
  - src/components/marketing/safe-verify/SvFlowDiagram.tsx
autonomous: true
requirements:
  - SV-02
  - SV-04
  - SV-08

must_haves:
  truths:
    - "A visitor can see four tabs labelled Outbound, Inbound, Bi-directional, and Branch"
    - "Clicking each tab displays distinct flow-step content without a full page reload"
    - "The Outbound tab content is verbatim from the source HTML six-step flow"
    - "The Inbound, Bi-directional, and Branch tabs each have authored step content describing their verification flow"
    - "A CSS outbound call flow diagram placeholder renders below the tabs section"
    - "The tabs section has id='use-cases' so the hero CTA scrolls to it"
  artifacts:
    - path: "src/components/marketing/safe-verify/SvUseCaseTabs.tsx"
      provides: "4-tab use-case section with flow-step cards per tab"
      contains: "'use client'"
      min_lines: 150
    - path: "src/components/marketing/safe-verify/SvFlowDiagram.tsx"
      provides: "CSS outbound call flow placeholder diagram"
      min_lines: 40
  key_links:
    - from: "SvUseCaseTabs.tsx"
      to: "useState hook"
      via: "activeTab state"
      pattern: "useState"
    - from: "tab button"
      to: "tab content panel"
      via: "conditional render on activeTab"
      pattern: "activeTab ==="
---

<objective>
Implement the four-tab use-case section (`SvUseCaseTabs`) and the CSS outbound flow diagram placeholder (`SvFlowDiagram`). This plan delivers the core interactive element of the Safe Verify page — replacing the stub files created in plan 04-01.

Purpose: The tabbed section is the primary content differentiator — it shows visitors the verification flow for their specific use case (outbound banking call, inbound customer query, bidirectional hybrid, branch visit). SV-02 and SV-04 are fulfilled here.

Output: Functional 4-tab section with authored flow-step content; CSS diagram placeholder with labelled boxes.
</objective>

<execution_context>
@/Users/markwright/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markwright/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-safe-verify/04-CONTEXT.md
@.planning/phases/04-safe-verify/04-RESEARCH.md
@.planning/phases/04-safe-verify/04-01-SUMMARY.md
@_archive/safe-verify-landing.html
@src/components/marketing/dsc/HowItWorksSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: SvUseCaseTabs — 4-tab section with flow steps</name>
  <files>src/components/marketing/safe-verify/SvUseCaseTabs.tsx</files>
  <action>
    Replace the stub with the full `SvUseCaseTabs` implementation.

    **Directive:** `'use client'` — MUST be the first line. This is the ONLY 'use client' component in the safe-verify directory.

    **Imports:** `useState` from 'react'.

    **State:** `const [activeTab, setActiveTab] = useState<'outbound' | 'inbound' | 'bidirectional' | 'branch'>('outbound')`

    **Section wrapper:**
    ```jsx
    <section id="use-cases" className="bg-base-100 border-t border-base-300 py-24 lg:py-32">
      <div className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
    ```

    **Section header:**
    ```jsx
    <p className="text-xs uppercase tracking-widest text-accent font-semibold mb-4">// How It Works</p>
    <h2 className="text-3xl lg:text-4xl font-bold text-base-content">
      Outbound call <span className="font-serif italic font-normal">verification flow</span>
    </h2>
    <div className="w-16 h-0.5 bg-base-content/20 mt-6 mb-10" />
    ```

    **Tab bar (underline style — locked decision):**
    ```jsx
    <div className="flex gap-1 border-b border-base-300 mb-10">
      {(['outbound', 'inbound', 'bidirectional', 'branch'] as const).map((tab) => {
        const labels = { outbound: 'Outbound', inbound: 'Inbound', bidirectional: 'Bi-directional', branch: 'Branch' }
        const isActive = activeTab === tab
        return (
          <button
            key={tab}
            onClick={() => setActiveTab(tab)}
            className={[
              'px-5 py-3 text-sm font-semibold transition-colors',
              isActive
                ? 'text-primary border-b-2 border-primary -mb-px'
                : 'text-base-content/40 border-b-2 border-transparent -mb-px hover:text-base-content/70'
            ].join(' ')}
          >
            {labels[tab]}
          </button>
        )
      })}
    </div>
    ```

    **Tab content area:** Render one of four `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5">` grids based on `activeTab`. Each tab has flow-step cards. Each card:
    ```jsx
    <div className="p-6 bg-base-200 rounded-2xl border border-base-300 hover:border-primary/40 transition-colors">
      <div className="text-xs font-mono text-primary mb-3">{stepLabel}</div>  {/* e.g. "Step 01" or "Fallback" */}
      <div className="w-12 h-12 rounded-xl flex items-center justify-center mb-4 {bgColour}">
        {/* inline SVG icon */}
      </div>
      <h3 className="text-base font-semibold text-base-content mb-2">{title}</h3>
      <p className="text-sm text-base-content/60 leading-relaxed">{description}</p>
    </div>
    ```

    Icon background colour classes for variety: `bg-primary/10`, `bg-success/10`, `bg-warning/10`, `bg-error/10`.

    **OUTBOUND TAB steps (verbatim from source HTML `_archive/safe-verify-landing.html`, lines 536–588):**

    Step 01 — "Bank initiates outbound call" — icon: phone SVG, bg-primary/10
    - desc: "The agent calls the customer. Safe Verify simultaneously sends a push notification / deep link to the customer&rsquo;s banking app."

    Step 02 — "Customer opens app &amp; verifies identity" — icon: bell SVG, bg-success/10
    - desc: "Customer opens the banking app via the notification and authenticates using three layers: phone unlock, app login, and biometric (Face ID / fingerprint)."

    Step 03 — "Bank agent verified in-app" — icon: shield-check SVG, bg-warning/10
    - desc: "The app displays the agent&rsquo;s name and department — a display that can only be triggered by a call originating from the bank. Customer sees proof it&rsquo;s legitimate."

    Step 04 — "Active question validation" — icon: question-circle SVG, bg-primary/10
    - desc: "The agent sends verification questions to the app. The customer selects the correct answer on-screen — cementing bidirectional trust without speaking sensitive details aloud."

    Step 05 — "Authenticated conversation begins" — icon: check-circle SVG, bg-success/10
    - desc: "Both parties are verified. The agent can serve the customer immediately — no repetitive questions, no wasted time, no opportunity for social engineering."

    Fallback — "Graceful fallback" — icon: refresh/repeat SVG, bg-error/10
    - desc: "If the customer is unavailable or verification fails, a message is left to contact the bank directly. Security is never compromised."

    **INBOUND TAB steps (Claude-authored, adapted from source HTML):**

    Step 01 — "Customer calls the bank" — icon: phone SVG, bg-primary/10
    - desc: "The customer dials the bank&rsquo;s contact centre. Safe Verify detects the incoming call and instantly pushes a verification prompt to the customer&rsquo;s banking app."

    Step 02 — "Customer authenticates in-app" — icon: fingerprint/lock SVG, bg-success/10
    - desc: "Before the agent answers, the customer completes three-layer authentication in the app: phone unlock, app login, and biometric confirmation."

    Step 03 — "Agent receives verified status" — icon: shield-check SVG, bg-warning/10
    - desc: "The agent&rsquo;s screen displays a &ldquo;Customer Verified&rdquo; status before the conversation begins. No security questions required."

    Step 04 — "Conversation starts immediately" — icon: chat/message SVG, bg-success/10
    - desc: "With identity confirmed, the agent addresses the customer&rsquo;s query directly. Reduced handle time. No friction for genuine callers."

    Fallback — "Graceful fallback" — icon: repeat SVG, bg-error/10
    - desc: "If the customer cannot complete in-app verification, the agent falls back to standard KBA — ensuring no call is ever abandoned due to the verification layer."

    **BI-DIRECTIONAL TAB steps (Claude-authored):**

    Step 01 — "Either party initiates" — icon: arrows-exchange SVG (two opposing arrows), bg-primary/10
    - desc: "Whether the bank calls the customer or vice versa, Safe Verify activates simultaneously — one protocol handles both directions."

    Step 02 — "Customer verifies themselves" — icon: fingerprint SVG, bg-success/10
    - desc: "The customer authenticates via the banking app using phone unlock, app login, and biometric — proving their identity to the bank."

    Step 03 — "Bank verifies itself to customer" — icon: shield-check SVG, bg-warning/10
    - desc: "The in-app display shows the agent&rsquo;s name and department, triggered only by a legitimate bank call. The customer sees proof — not just a caller ID that can be spoofed."

    Step 04 — "Mutual trust confirmed" — icon: check-circle SVG, bg-success/10
    - desc: "Both parties are verified simultaneously. The conversation proceeds with full bidirectional trust — eliminating vishing and impersonation in a single frictionless moment."

    Step 05 — "Serve immediately" — icon: lightning SVG, bg-primary/10
    - desc: "No repetitive questions. No security theatre. The agent can resolve the customer&rsquo;s issue from the first word of the conversation."

    **BRANCH TAB steps (Claude-authored):**

    Step 01 — "Customer arrives at branch" — icon: building/bank SVG, bg-primary/10
    - desc: "The customer approaches a teller or relationship manager. The branch advisor initiates a Safe Verify session from their terminal."

    Step 02 — "Push notification to banking app" — icon: bell SVG, bg-success/10
    - desc: "A verification prompt is pushed to the customer&rsquo;s banking app. No printed form, no verbal question — the app does the heavy lifting."

    Step 03 — "Customer verifies in-app" — icon: fingerprint SVG, bg-warning/10
    - desc: "The customer authenticates with three layers in the app. The branch advisor&rsquo;s terminal shows &ldquo;Customer Verified&rdquo; within seconds."

    Step 04 — "Transaction proceeds" — icon: check-circle SVG, bg-success/10
    - desc: "With identity confirmed to the same standard as a digital session, the branch interaction continues without manual ID checks or knowledge-based questions."

    Fallback — "ID document fallback" — icon: card SVG (rectangle with line), bg-error/10
    - desc: "If the customer&rsquo;s phone is unavailable, the advisor falls back to document ID — ensuring branch operations are never blocked by the verification layer."

    **Inline SVGs to use** (all `className="w-6 h-6"`, `viewBox="0 0 24 24"`, `fill="none"`, `stroke="currentColor"`, `strokeWidth={2}`, `aria-hidden="true"`):

    - Phone: `<path strokeLinecap="round" strokeLinejoin="round" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 8V5z" />`
    - Bell: `<path strokeLinecap="round" strokeLinejoin="round" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6 6 0 10-12 0v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9" />`
    - Shield-check: `<path strokeLinecap="round" strokeLinejoin="round" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />`
    - Question-circle: `<path strokeLinecap="round" strokeLinejoin="round" d="M8.228 9c.549-1.165 2.03-2 3.772-2 2.21 0 4 1.343 4 3 0 1.4-1.278 2.575-3.006 2.907-.542.104-.994.54-.994 1.093m0 3h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />`
    - Check-circle: `<path strokeLinecap="round" strokeLinejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />`
    - Repeat/refresh: `<path strokeLinecap="round" strokeLinejoin="round" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />`
    - Lock: `<path strokeLinecap="round" strokeLinejoin="round" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />`
    - Chat/message: `<path strokeLinecap="round" strokeLinejoin="round" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />`
    - Lightning: `<path strokeLinecap="round" strokeLinejoin="round" d="M13 10V3L4 14h7v7l9-11h-7z" />`
    - Arrows-exchange (two opposing arrows): `<path strokeLinecap="round" strokeLinejoin="round" d="M8 7h12m0 0l-4-4m4 4l-4 4m0 6H4m0 0l4 4m-4-4l4-4" />`
    - Building/bank: `<path strokeLinecap="round" strokeLinejoin="round" d="M8 14v3m4-3v3m4-3v3M3 21h18M3 10h18M3 7l9-4 9 4M4 10h16v11H4V10z" />`
    - Card: `<path strokeLinecap="round" strokeLinejoin="round" d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z" />`
    - Fingerprint: `<path strokeLinecap="round" strokeLinejoin="round" d="M12 11c0 3.517-1.009 6.799-2.753 9.571m-3.44-2.04l.054-.09A13.916 13.916 0 008 11a4 4 0 118 0c0 1.017-.07 2.019-.203 3m-2.118 6.844A21.88 21.88 0 0015.171 17m3.839 1.132c.645-2.266.99-4.659.99-7.132A8 8 0 008 4.07M3 15.364c.64-1.319 1-2.8 1-4.364 0-1.457.39-2.823 1.07-4" />`
  </action>
  <verify>
    1. `npm run build` exits 0
    2. Visit /safe-verify in dev — four tab buttons visible (Outbound, Inbound, Bi-directional, Branch)
    3. Click each tab — content changes, active tab has underline indicator (border-primary)
    4. Outbound tab: six cards including one labelled "Fallback" visible
    5. Inbound tab: five distinct steps (different copy from Outbound) visible
    6. No TypeScript errors for SvUseCaseTabs.tsx
  </verify>
  <done>
    - Tab switching works without page reload
    - All four tabs have distinct authored flow-step content
    - Outbound tab content is verbatim from source HTML
    - Active tab shows border-primary underline; inactive tabs show border-transparent
    - id="use-cases" is present on the section element
  </done>
</task>

<task type="auto">
  <name>Task 2: SvFlowDiagram — CSS outbound call flow placeholder</name>
  <files>src/components/marketing/safe-verify/SvFlowDiagram.tsx</files>
  <action>
    Replace the stub with the full `SvFlowDiagram` implementation. Server Component — no 'use client'.

    This is a CSS-only diagram placeholder using the same approach as the ArchitectureDiagram in plan 03-01: labelled bordered boxes with connector lines, visually marked as a placeholder pending a production SVG asset.

    **Section wrapper:** `<section className="bg-base-200 border-t border-base-300 py-20 lg:py-24">`

    **Header:**
    ```jsx
    <div className="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8">
      <p className="text-xs uppercase tracking-widest text-accent font-semibold mb-3">// Outbound Call Flow</p>
      <h2 className="text-2xl font-bold text-base-content mb-2">How the verification moment works</h2>
      <p className="text-base-content/50 text-sm mb-10">Production diagram — coming soon. Structure below represents the live flow.</p>
    ```

    **Diagram** (horizontal flow on lg, vertical stack on mobile): five labelled boxes connected by arrows using CSS borders and the `hidden sm:block` pattern from the architecture diagram.

    ```jsx
    <div className="flex flex-col sm:flex-row items-center gap-4 overflow-x-auto pb-4">

      {/* Box 1 — Bank Agent */}
      <div className="flex-shrink-0 w-40 rounded-xl border-2 border-primary/40 bg-base-100 p-4 text-center">
        <div className="text-xs font-mono text-primary mb-1">01</div>
        <div className="text-sm font-semibold text-base-content">Bank Agent</div>
        <div className="text-xs text-base-content/40 mt-1">Initiates call</div>
      </div>

      {/* Connector */}
      <div className="hidden sm:flex items-center text-primary/40">
        <div className="w-8 h-px bg-primary/30" />
        <svg xmlns="http://www.w3.org/2000/svg" className="w-4 h-4" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
          <path fillRule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clipRule="evenodd" />
        </svg>
      </div>
      <div className="sm:hidden text-primary/40 text-xl">↓</div>

      {/* Box 2 — Safe Verify API */}
      <div className="flex-shrink-0 w-44 rounded-xl border-2 border-accent/60 bg-base-100 p-4 text-center">
        <div className="text-xs font-mono text-accent mb-1">02</div>
        <div className="text-sm font-semibold text-base-content">Safe Verify API</div>
        <div className="text-xs text-base-content/40 mt-1">Sends push notification</div>
      </div>

      {/* Connector */}
      <div className="hidden sm:flex items-center text-primary/40">
        <div className="w-8 h-px bg-primary/30" />
        <svg xmlns="http://www.w3.org/2000/svg" className="w-4 h-4" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
          <path fillRule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clipRule="evenodd" />
        </svg>
      </div>
      <div className="sm:hidden text-primary/40 text-xl">↓</div>

      {/* Box 3 — Customer App */}
      <div className="flex-shrink-0 w-40 rounded-xl border-2 border-primary/40 bg-base-100 p-4 text-center">
        <div className="text-xs font-mono text-primary mb-1">03</div>
        <div className="text-sm font-semibold text-base-content">Customer App</div>
        <div className="text-xs text-base-content/40 mt-1">3-layer biometric auth</div>
      </div>

      {/* Connector — bidirectional */}
      <div className="hidden sm:flex items-center text-primary/40">
        <div className="w-8 h-px bg-primary/30" />
        <svg xmlns="http://www.w3.org/2000/svg" className="w-4 h-4" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
          <path fillRule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clipRule="evenodd" />
        </svg>
      </div>
      <div className="sm:hidden text-primary/40 text-xl">↓</div>

      {/* Box 4 — Verified Both Ways */}
      <div className="flex-shrink-0 w-44 rounded-xl border-2 border-success/60 bg-base-100 p-4 text-center">
        <div className="text-xs font-mono text-success mb-1">04</div>
        <div className="text-sm font-semibold text-base-content">Mutual Verification</div>
        <div className="text-xs text-base-content/40 mt-1">Agent + customer confirmed</div>
      </div>

      {/* Connector */}
      <div className="hidden sm:flex items-center text-primary/40">
        <div className="w-8 h-px bg-primary/30" />
        <svg xmlns="http://www.w3.org/2000/svg" className="w-4 h-4" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
          <path fillRule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clipRule="evenodd" />
        </svg>
      </div>
      <div className="sm:hidden text-primary/40 text-xl">↓</div>

      {/* Box 5 — Authenticated Conversation */}
      <div className="flex-shrink-0 w-44 rounded-xl border-2 border-success/60 bg-success/5 p-4 text-center">
        <div className="text-xs font-mono text-success mb-1">05</div>
        <div className="text-sm font-semibold text-base-content">Serve Customer</div>
        <div className="text-xs text-base-content/40 mt-1">No security questions</div>
      </div>

    </div>

    {/* Placeholder label */}
    <p className="text-xs text-base-content/30 mt-6 text-center tracking-widest uppercase">
      Placeholder — production SVG to replace in a future update
    </p>
    ```
  </action>
  <verify>
    1. `npm run build` exits 0
    2. /safe-verify in dev: flow diagram section visible with 5 labelled boxes
    3. On desktop (lg): boxes display horizontally with arrow connectors
    4. On mobile (sm): boxes stack vertically with ↓ separators
    5. Placeholder label text visible below diagram
  </verify>
  <done>
    - Flow diagram renders as CSS boxes (not placeholder `<section />`from stub)
    - Five stages visible: Bank Agent → Safe Verify API → Customer App → Mutual Verification → Serve Customer
    - Responsive: horizontal on desktop, vertical stack on mobile
    - "Placeholder — production SVG" label present
    - bg-base-200 background creating visual contrast from the bg-base-100 tabs section above
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
- `npm run build` and `npm run lint` pass cleanly
- Tab switching on /safe-verify is functional in browser
- Outbound tab content matches source HTML verbatim (six steps + fallback)
- Three new tabs (Inbound, Bi-directional, Branch) each render distinct content
- Flow diagram renders below tabs with five CSS boxes
- id="use-cases" on the tabs section (hero CTA scroll-link target confirmed)
</verification>

<success_criteria>
- Interactive tab switching with useState (no full-page reload)
- Outbound tab: six flow-step cards with verbatim source HTML copy
- Inbound, Bi-directional, Branch: authored steps appropriate to each use case
- Active tab underline indicator using border-primary
- CSS outbound flow diagram with five labelled stages and placeholder label
- Build passes without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-safe-verify/04-02-SUMMARY.md` following the summary template.
</output>
