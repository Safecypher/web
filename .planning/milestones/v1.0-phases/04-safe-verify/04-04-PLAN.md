---
phase: 04-safe-verify
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - src/components/marketing/safe-verify/SvIntegrationSection.tsx
  - src/components/marketing/safe-verify/SvCtaSection.tsx
autonomous: true
requirements:
  - SV-05
  - SV-07
  - SV-09

must_haves:
  truths:
    - "A visitor can read integration details for Amazon Connect, REST API, IVR Drop-in, White-label SDK, Tokenized Phone, GDPR by Design, and CCPA Ready as feature cards"
    - "The integration section heading 'Drop-in. Not rip-and-replace.' is present verbatim"
    - "A contextual inline link to /dynamic-security-codes appears within the integration or benefits section copy"
    - "The page ends with a CTA section containing a calculator link and a 'Request Demo' button"
    - "The full page builds cleanly with all seven sections rendering"
  artifacts:
    - path: "src/components/marketing/safe-verify/SvIntegrationSection.tsx"
      provides: "Seven integration feature cards with DSC cross-link"
      min_lines: 100
    - path: "src/components/marketing/safe-verify/SvCtaSection.tsx"
      provides: "CTA wrapper over PageCtaSection"
      min_lines: 10
  key_links:
    - from: "SvIntegrationSection.tsx"
      to: "/dynamic-security-codes"
      via: "Link href"
      pattern: "dynamic-security-codes"
    - from: "SvCtaSection.tsx"
      to: "PageCtaSection"
      via: "import and render"
      pattern: "PageCtaSection"
---

<objective>
Implement `SvIntegrationSection` (seven feature cards + DSC cross-link) and `SvCtaSection` (thin wrapper over shared PageCtaSection). This plan completes the /safe-verify page — all seven sections are fully implemented, fulfilling SV-05, SV-07, and SV-09.

Purpose: Integration details answer the procurement question ("can this actually fit into our stack?"). The DSC cross-link seeds discovery of the flagship product. The CTA closes the conversion loop.

Output: Fully-built /safe-verify page with all sections implemented; build passes clean.
</objective>

<execution_context>
@/Users/markwright/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markwright/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-safe-verify/04-CONTEXT.md
@.planning/phases/04-safe-verify/04-RESEARCH.md
@.planning/phases/04-safe-verify/04-01-SUMMARY.md
@.planning/phases/04-safe-verify/04-02-SUMMARY.md
@.planning/phases/04-safe-verify/04-03-SUMMARY.md
@_archive/safe-verify-landing.html
@src/components/marketing/shared/PageCtaSection.tsx
@src/components/marketing/dsc/DscCtaSection.tsx
@src/components/marketing/dsc/ForIssuersSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: SvIntegrationSection — seven feature cards + DSC cross-link</name>
  <files>src/components/marketing/safe-verify/SvIntegrationSection.tsx</files>
  <action>
    Replace the stub with the full `SvIntegrationSection` implementation. Server Component — no 'use client'.

    **Section background:** `bg-base-200` (locked decision: integration section uses bg-base-200 per section rhythm).

    Import `Link` from `next/link`.

    **Section wrapper:** `<section className="bg-base-200 border-t border-base-300 py-24 lg:py-32">`

    **Header:**
    ```jsx
    <div className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
      <div className="text-center mb-14">
        <p className="text-xs uppercase tracking-widest text-accent font-semibold mb-3">// Integration</p>
        <h2 className="text-3xl lg:text-4xl font-bold text-base-content">
          Drop-in.{' '}
          <span className="font-serif italic font-normal">Not rip-and-replace.</span>
        </h2>
        <p className="text-base-content/60 mt-4 max-w-2xl mx-auto">
          Safe Verify integrates with your existing IVR and contact centre infrastructure.
          Compatible with Amazon Connect and major telephony platforms. No full auth stream required.
        </p>
      </div>
    ```

    **Seven feature cards** (from source HTML integration-tag list, lines 707–715, expanded into cards per locked decision). Grid: `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-5">`

    Note: 7 cards in a 4-col grid = row 1 (4 cards) + row 2 (3 cards). This is fine.

    Card structure (follows ForIssuersSection pattern from dsc/ForIssuersSection.tsx):
    ```jsx
    <div className="card bg-base-100 rounded-xl p-6 border border-base-300">
      {/* inline SVG icon */}
      <h3 className="text-base font-semibold text-base-content mt-3">{title}</h3>
      <p className="text-sm text-base-content/60 mt-2 leading-relaxed">{desc}</p>
    </div>
    ```

    All SVGs: `className="w-6 h-6 text-accent"`, `viewBox="0 0 24 24"`, `fill="none"`, `stroke="currentColor"`, `strokeWidth={2}`, `aria-hidden="true"`.

    **Card 1 — White-label SDK:**
    - SVG (smartphone): `<rect x="5" y="2" width="14" height="20" rx="2" ry="2" /><line x1="12" y1="18" x2="12.01" y2="18" />`
    - Title: "White-label SDK"
    - Desc: "Embed Safe Verify verification directly into your existing banking app under your brand. No new application for customers to install."

    **Card 2 — Amazon Connect:**
    - SVG (cloud): `<path strokeLinecap="round" strokeLinejoin="round" d="M3 15a4 4 0 004 4h9a5 5 0 10-.1-9.999 5.002 5.002 0 10-9.78 2.096A4.001 4.001 0 003 15z" />`
    - Title: "Amazon Connect"
    - Desc: "Native integration with Amazon Connect. Safe Verify slots into your existing contact centre stack without a platform migration."

    **Card 3 — IVR Drop-in:**
    - SVG (telephone): `<path strokeLinecap="round" strokeLinejoin="round" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 8V5z" />`
    - Title: "IVR Drop-in"
    - Desc: "Works with your existing IVR telephony infrastructure. Safe Verify activates as an overlay on your current call routing — no replacement required."

    **Card 4 — Tokenized Phone:**
    - SVG (key): `<path strokeLinecap="round" strokeLinejoin="round" d="M15 7a2 2 0 012 2m4 0a6 6 0 01-7.743 5.743L11 17H9v2H7v2H4a1 1 0 01-1-1v-2.586a1 1 0 01.293-.707l5.964-5.964A6 6 0 1121 9z" />`
    - Title: "Tokenized Phone"
    - Desc: "Phone numbers are tokenized — no raw PII traverses the verification layer. Consistent with the semi-stateless architecture powering the whole SafeCypher platform."

    **Card 5 — GDPR by Design:**
    - SVG (globe): `<path strokeLinecap="round" strokeLinejoin="round" d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />`
    - Title: "GDPR by Design"
    - Desc: "Zero data at rest. Verification calculations are on-the-fly. No personal data is stored or processed beyond the moment of authentication."

    **Card 6 — CCPA Ready:**
    - SVG (shield-check): `<path strokeLinecap="round" strokeLinejoin="round" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />`
    - Title: "CCPA Ready"
    - Desc: "Compliant with California Consumer Privacy Act requirements. No sale or retention of personal data — verification events leave no residue."

    **Card 7 — REST API:**
    - SVG (code/brackets): `<path strokeLinecap="round" strokeLinejoin="round" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" />`
    - Title: "REST API"
    - Desc: "Standard JSON REST API documented via OpenAPI spec. Your engineering team integrates once — Safe Verify handles the telephony coordination."

    **DSC cross-link** — placed as a paragraph below the cards grid (SV-07 fulfilled here). This is an inline contextual link per the locked decision (not a standalone section):
    ```jsx
    <p className="text-base-content/40 text-sm mt-10">
      The semi-stateless, zero-storage architecture is shared across the SafeCypher platform &mdash;{' '}
      <Link href="/dynamic-security-codes" className="text-accent hover:underline">
        see how Dynamic Security Codes use the same approach to eliminate CNP fraud
      </Link>
      .
    </p>
    ```

    This DSC cross-link pattern matches `DscSolutionSection.tsx` (which cross-links to /platform in the same position).
  </action>
  <verify>
    1. `npm run build` exits 0
    2. /safe-verify in dev: integration section visible with bg-base-200 background
    3. Heading "Drop-in. Not rip-and-replace." present (verbatim)
    4. Seven integration cards render (White-label SDK, Amazon Connect, IVR Drop-in, Tokenized Phone, GDPR by Design, CCPA Ready, REST API)
    5. DSC cross-link visible with href="/dynamic-security-codes" and text-accent colour
    6. Clicking DSC cross-link navigates to /dynamic-security-codes in dev mode
  </verify>
  <done>
    - Seven feature cards with titles and descriptions for all integration options
    - "Drop-in. Not rip-and-replace." heading verbatim
    - DSC inline cross-link present with correct href
    - bg-base-200 section background
    - All inline SVGs (no Bootstrap Icons CDN)
  </done>
</task>

<task type="auto">
  <name>Task 2: SvCtaSection + parity check against source HTML</name>
  <files>src/components/marketing/safe-verify/SvCtaSection.tsx</files>
  <action>
    STEP A — Implement `SvCtaSection`. Server Component — no 'use client'.

    Follows the DscCtaSection pattern exactly (thin 5-line wrapper):
    ```tsx
    import { PageCtaSection } from '@/components/marketing/shared/PageCtaSection'

    export function SvCtaSection() {
      return <PageCtaSection />
    }
    ```

    The shared `PageCtaSection` already contains:
    - Heading: "Ready to eliminate CNP fraud?"
    - Calculator link: "See the value for your portfolio" → /portal/calculator
    - Demo CTA: "Request a demo" → /#demo

    This satisfies SV-09 (calculator link + demo CTA).

    STEP B — Parity check against source HTML. Read `_archive/safe-verify-landing.html` and verify the following are all present in the implemented /safe-verify page. Document any gaps.

    Source HTML sections to confirm ported:
    1. Hero: Headline "Vishing calls erode customer trust." + "Restore it instantly." accent line — MUST be present in SvHeroSection
    2. Hero CTA: Primary button labelled "Request Demo" (updated from source's "Book a Demo") — MUST be present
    3. Stats: "Bidirectional trust. / Inbound & outbound. Verified both ways." + three metrics (3–5 min / Zero data / 3 layers) — MUST be present in SvHeroSection stats strip
    4. Six-step Outbound flow: Six cards including "Graceful fallback" — MUST be present in SvUseCaseTabs Outbound tab
    5. Nuclear key: Three cards 01 Customer identification / 02 Agent identification / 03 Cementing the trust — MUST be in SvNuclearKeySection
    6. Benefits: Four items (3–5 min saved / Vishing protection / Zero data at rest / Bidirectional verification) — MUST be in SvBenefitsSection
    7. Metrics panel: Five rows including strikethrough "6–12 months" — MUST be in SvBenefitsSection
    8. Integration: Heading "Drop-in. Not rip-and-replace." + seven tags/cards — MUST be in SvIntegrationSection

    For any gap found: note it in the SUMMARY as a known discrepancy, but do NOT stop execution — the phase is considered complete if all eight items above are present.

    STEP C — Run full build and lint verification:
    ```bash
    npm run build
    npm run lint
    npx tsc --noEmit
    ```

    Fix any remaining TypeScript errors across all safe-verify component files before writing the SUMMARY.
  </action>
  <verify>
    1. `npm run build` exits 0
    2. `npm run lint` exits 0 — no warnings for safe-verify files
    3. `npx tsc --noEmit` exits 0
    4. /safe-verify in dev: full page renders with all seven sections top to bottom
    5. SvCtaSection: "See the value for your portfolio" and "Request a demo" buttons visible
    6. All eight parity items confirmed present (or gap noted)
  </verify>
  <done>
    - SvCtaSection wraps PageCtaSection (calculator link + Request Demo CTA)
    - Full build, lint, and type-check pass with zero errors
    - All eight source HTML sections confirmed ported to React components
    - /safe-verify page renders complete in dev mode: hero → stats → tabs → diagram → nuclear key → benefits → integration → CTA
  </done>
</task>

</tasks>

<verification>
Full phase 04 completion check:
- `npm run build` exits 0 across entire codebase (not just safe-verify files)
- `npm run lint` exits 0
- `npx tsc --noEmit` exits 0
- /safe-verify loads and renders all seven sections in correct order
- Section backgrounds follow locked rhythm: bg-base-100 → bg-base-200 → bg-base-100 → bg-base-200 → bg-neutral → bg-base-100 → bg-base-200 → bg-base-100
- DSC cross-link at /dynamic-security-codes navigates correctly
- Calculator CTA links to /portal/calculator
- Demo CTA links to /#demo
- No Bootstrap Icons CDN references anywhere in safe-verify components
- All SV-01 through SV-09 requirements addressed across plans 04-01 through 04-04
</verification>

<success_criteria>
- Seven integration feature cards with expanded descriptions (not pill tags)
- "Drop-in. Not rip-and-replace." heading verbatim from source
- DSC inline cross-link in integration section copy
- SvCtaSection is a 5-line wrapper over shared PageCtaSection
- Full codebase build passes cleanly
- Source HTML parity check: eight sections confirmed ported
</success_criteria>

<output>
After completion, create `.planning/phases/04-safe-verify/04-04-SUMMARY.md` following the summary template.

Then update `.planning/STATE.md`:
- Current Position: Phase 4 complete
- Last activity: today's date — Phase 4 /safe-verify complete
- Performance Metrics: add Phase 4 row
- Accumulated Context → Decisions: add Phase 04 decisions
- Session Continuity: note stopped point
</output>
