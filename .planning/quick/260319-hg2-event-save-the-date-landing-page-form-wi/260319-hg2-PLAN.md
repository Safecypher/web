---
phase: 260319-hg2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/save-the-date/page.tsx
  - src/components/marketing/save-the-date/SaveTheDateSection.tsx
  - src/app/api/submit/event-interest/route.ts
  - public/__forms.html
autonomous: true
requirements: [HG2-01]

must_haves:
  truths:
    - "Visitor lands on /save-the-date and sees event details with a name+email form"
    - "Submitting the form creates/updates an Attio contact and fires an event_interest event"
    - "Form shows a success state after submission"
    - "Page is styled with the existing safecypher-dark design system"
  artifacts:
    - path: "src/app/(marketing)/save-the-date/page.tsx"
      provides: "Marketing page with metadata, server component"
    - path: "src/components/marketing/save-the-date/SaveTheDateSection.tsx"
      provides: "Hero + form section, client component"
    - path: "src/app/api/submit/event-interest/route.ts"
      provides: "Form submission handler: Netlify form + Attio event_interest"
    - path: "public/__forms.html"
      provides: "Netlify form detection registration for event-interest"
  key_links:
    - from: "SaveTheDateSection.tsx"
      to: "/api/submit/event-interest"
      via: "fetch POST in handleSubmit"
      pattern: "fetch.*api/submit/event-interest"
    - from: "src/app/api/submit/event-interest/route.ts"
      to: "/api/attio/event"
      via: "server-to-server fetch with x-internal-token"
      pattern: "x-internal-token"
---

<objective>
Build a /save-the-date marketing page with a name + email capture form for event interest.
Submissions fan out to Netlify Forms (email notification) and Attio CRM (event_interest event,
which upserts the contact and creates a note). Nav and Footer are provided automatically by the
(marketing) route group layout.

Purpose: Capture attendee interest for an upcoming event, feeding leads directly into the CRM.
Output: One new route (/save-the-date), one form-submission API route, Netlify form registration.
</objective>

<execution_context>
@/Users/markwright/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markwright/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Key patterns the executor needs. No codebase exploration required. -->

<!-- UI primitives (src/components/ui/index.ts) -->
```typescript
import { Input, Button } from '@/components/ui'
// Input props: label, name, type, placeholder, required, fullWidth
// Button props: type, variant ("primary"|"ghost"|...), size ("lg"|...), fullWidth, loading
```

<!-- DaisyUI v5 fieldset pattern used by Input/Textarea (from Phase 02-04 decision) -->
<!-- Input uses fieldset/fieldset-legend pattern — no form-control/label-text -->

<!-- Attio event route (src/app/api/attio/event/route.ts) -->
<!-- Three-branch logic: name+email → upsert person + note; email-only → note only; neither → log -->
<!-- POST body: { event: string, email?: string, name?: string, ...extra } -->
<!-- Guard: x-internal-token header must match INTERNAL_API_SECRET -->

<!-- Existing submit route pattern (src/app/api/submit/contact-request/route.ts) -->
<!-- Step 1: POST to ${siteUrl}/__forms.html (Netlify form capture) -->
<!-- Step 2: POST to ${siteUrl}/api/attio/event with x-internal-token header -->
<!-- Return: Response.json({ ok: true }) -->

<!-- Client form pattern (ContactFormSection.tsx) -->
<!-- 'use client', useState<'idle'|'submitting'|'success'|'error'> -->
<!-- handleSubmit: e.preventDefault(), FormData, fetch('/api/submit/...') -->
<!-- Success state: replace section with confirmation UI -->
<!-- Error state: alert alert-error div with message -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API route — event-interest submission handler</name>
  <files>
    src/app/api/submit/event-interest/route.ts
    public/__forms.html
  </files>
  <action>
    Create `src/app/api/submit/event-interest/route.ts` as a POST handler following the exact
    pattern of `src/app/api/submit/contact-request/route.ts`.

    Request body type:
    ```typescript
    { name: string; email: string }
    ```

    Step 1 — Forward to Netlify Forms:
    POST to `${siteUrl}/__forms.html` with URLSearchParams:
    - form-name: 'event-interest'
    - name: body.name
    - email: body.email

    Step 2 — Fire Attio event (server-to-server):
    POST to `${siteUrl}/api/attio/event` with header `x-internal-token: process.env.INTERNAL_API_SECRET ?? ''`
    Body: `{ event: 'event_interest', email: body.email, name: body.name }`

    Log Attio failure if `!attioRes.ok && process.env.NODE_ENV !== 'development'`.
    Return `Response.json({ ok: true })`.

    In `public/__forms.html`, append a new hidden form BEFORE the closing `</body>` tag:
    ```html
    <form name="event-interest" data-netlify="true" hidden>
      <input type="hidden" name="form-name" value="event-interest" />
      <input type="text" name="name" />
      <input type="email" name="email" />
      <button type="submit">Submit</button>
    </form>
    ```
    Do NOT remove or modify the existing demo-request or contact-request forms.
  </action>
  <verify>
    `npm run build` passes with no TypeScript errors.
    Manually confirm `public/__forms.html` now contains all three forms (demo-request, contact-request, event-interest).
  </verify>
  <done>
    Route file exists at src/app/api/submit/event-interest/route.ts.
    __forms.html has event-interest form registration.
    Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Page and form component — /save-the-date</name>
  <files>
    src/app/(marketing)/save-the-date/page.tsx
    src/components/marketing/save-the-date/SaveTheDateSection.tsx
  </files>
  <action>
    Create `src/components/marketing/save-the-date/SaveTheDateSection.tsx` as a Client Component
    ('use client'). Use `useState` for form state: `'idle' | 'submitting' | 'success' | 'error'`.

    Layout: single centered section, `bg-base-100 py-24 lg:py-32`, `max-w-2xl mx-auto px-4 sm:px-6`.

    Hero block (above form):
    - Small eyebrow label: `text-sm font-semibold text-primary uppercase tracking-widest mb-4`
      text: "Save the Date"
    - Heading: `text-4xl lg:text-5xl font-bold text-base-content mb-6`
      text: "HG2 Payments Innovation Summit" (use a placeholder the client can swap)
    - Subheading/description paragraph: `text-base-content/60 text-lg mb-10`
      text: "Register your interest and be the first to receive event details, agenda and confirmed speakers."
    - Divider: `border-t border-base-300 mb-10`

    Form (name="event-interest"):
    - Two fields stacked (full width, not side by side — simpler for event landing):
      1. Input: label="Full Name *", name="name", type="text", placeholder="Jane Smith", required, fullWidth
      2. Input: label="Email Address *", name="email", type="email", placeholder="you@company.com", required, fullWidth
    - Error alert (shown when formState === 'error'):
      `<div className="alert alert-error text-sm">` with inline SVG circle-i icon + message
      "Something went wrong. Please try again."
    - Submit Button: variant="primary", size="lg", fullWidth, loading={formState === 'submitting'}
      text: "Register Interest"

    handleSubmit: preventDefault, setFormState('submitting'), build payload `{ name, email }`,
    POST to '/api/submit/event-interest', on res.ok → setFormState('success'), else setFormState('error').
    Wrap in try/catch → setFormState('error') on network failure.

    Success state: replace entire section content with centered confirmation:
    - Checkmark circle (same pattern as ContactFormSection: w-16 h-16 rounded-full bg-success/20 + SVG polyline)
    - Heading: "You're on the list."
    - Sub: "We'll be in touch with event details as they're confirmed."

    Create `src/app/(marketing)/save-the-date/page.tsx` as a Server Component:
    ```typescript
    import type { Metadata } from 'next'
    import { SaveTheDateSection } from '@/components/marketing/save-the-date/SaveTheDateSection'

    export const metadata: Metadata = {
      title: 'Save the Date — HG2 Payments Innovation Summit | SafeCypher',
      description: 'Register your interest in the HG2 Payments Innovation Summit. Be first to receive event details, agenda and confirmed speakers.',
    }

    export default function SaveTheDatePage() {
      return <SaveTheDateSection />
    }
    ```

    Note: No `use client` on page.tsx — it stays a Server Component exporting metadata
    (per Phase 07-04 decision: metadata + 'use client' cannot coexist).
  </action>
  <verify>
    `npm run dev` — visit http://localhost:3000/save-the-date.
    Page loads with Nav + Footer (from (marketing) layout).
    Form renders with name + email fields and "Register Interest" button.
    Submit with test data — network tab shows POST to /api/submit/event-interest returning 200.
    Success state replaces the form.
    `npm run lint` passes with no errors.
  </verify>
  <done>
    /save-the-date renders correctly with the event landing page design.
    Form submits to the API route and shows success state.
    No lint or build errors.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run build` — clean build, no TypeScript or lint errors
2. Visit http://localhost:3000/save-the-date — page loads with Nav, hero block, form, Footer
3. Submit form with name="Test User" email="test@example.com":
   - Network tab: POST /api/submit/event-interest → 200 response
   - Server logs: "[Attio stub] { event: 'event_interest', ... }" (dev stub mode)
   - UI: success confirmation replaces form
4. Confirm public/__forms.html has event-interest form alongside existing forms
</verification>

<success_criteria>
- /save-the-date route exists and is reachable
- Form captures name + email and submits to /api/submit/event-interest
- API route forwards to Netlify Forms and fires Attio event_interest
- Success and error states both render correctly
- Build and lint pass cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260319-hg2-event-save-the-date-landing-page-form-wi/260319-hg2-SUMMARY.md`
</output>
