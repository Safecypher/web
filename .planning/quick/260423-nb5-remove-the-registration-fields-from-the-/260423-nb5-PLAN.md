---
phase: quick
plan: 260423-nb5
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/marketing/save-the-date/SaveTheDateSection.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "When the UK (April London) event is selected, no registration form is shown"
    - "When the UK event is selected, a note is displayed saying the session recording is coming soon"
    - "The UK2 and CA events still show the registration form as before"
  artifacts:
    - path: src/components/marketing/save-the-date/SaveTheDateSection.tsx
      provides: "Conditional form rendering based on selected event"
  key_links:
    - from: selectedEvent state
      to: form render
      via: conditional check for 'UK' event id
      pattern: "selectedEvent === 'UK'"
---

<objective>
Remove the registration form from the April London (UK) event view on the save-the-date page and replace it with a note that the session recording is coming soon. The UK2 (June London) and CA (Toronto) events are unaffected.

Purpose: The April event has passed (2026-04-23). Registrations are no longer relevant; visitors should see a "recording coming soon" message instead.
Output: Updated SaveTheDateSection.tsx with conditional rendering based on selected event.
</objective>

<execution_context>
@/Users/markwright/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markwright/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/components/marketing/save-the-date/SaveTheDateSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace form with recording note for the UK April event</name>
  <files>src/components/marketing/save-the-date/SaveTheDateSection.tsx</files>
  <action>
    In SaveTheDateSection.tsx, after the event details block and the `<div className="border-t ...">` divider, conditionally render based on `selectedEvent`:

    - If `selectedEvent === 'UK'`: do NOT render the `<form>` block. Instead render a note section:
      ```tsx
      <div className="bg-base-200 rounded-2xl px-6 py-6 text-center">
        <p className="text-base-content font-semibold mb-1">This event has now taken place.</p>
        <p className="text-base-content/60 text-sm">A recording of the session will be available here soon.</p>
      </div>
      ```
    - Otherwise (UK2, CA): render the existing `<form>` block unchanged.

    Also update the intro paragraph text (currently "Register your interest...") to only show when `selectedEvent !== 'UK'`, or replace it with a neutral alternative such as:
      - When UK: hide the "Register your interest" paragraph entirely (or conditionally)
      - When UK2/CA: keep the existing paragraph text

    The `formState === 'success'` early-return (the "You're on the list" screen) and the `handleSubmit` function can remain — they are unreachable for UK but harmless to keep.

    Do not change any other event data, styling tokens, or component structure.
  </action>
  <verify>npm run build 2>&1 | tail -5</verify>
  <done>
    - Visiting /save-the-date (UK selected by default) shows the "This event has now taken place" note and no form
    - Switching to London June or Toronto shows the registration form as before
    - Build passes with no type errors or lint warnings
  </done>
</task>

</tasks>

<verification>
npm run lint && npm run build
</verification>

<success_criteria>
The UK April event tab shows a "recording coming soon" message with no form fields. UK2 and CA tabs remain fully functional with the registration form. No build or lint errors.
</success_criteria>

<output>
After completion, create `.planning/quick/260423-nb5-remove-the-registration-fields-from-the-/260423-nb5-SUMMARY.md`
</output>
