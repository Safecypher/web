---
phase: 01-foundation
plan: "04"
type: execute
wave: 3
depends_on:
  - "01-03"
files_modified:
  - src/components/marketing/Nav.tsx
  - src/components/marketing/Footer.tsx
  - src/app/(marketing)/layout.tsx
autonomous: false
requirements:
  - FOUND-05

must_haves:
  truths:
    - "Every page in the (marketing) route group shows the sticky nav at the top"
    - "Every page in the (marketing) route group shows the footer at the bottom"
    - "The nav sticks to the top of the viewport when scrolling"
    - "Clicking 'Platform' in the nav opens a mega-menu dropdown with product links"
    - "The nav contains a Portal icon (lock) that links to /portal"
    - "The nav contains a 'Request Demo' CTA button right-aligned"
    - "The footer shows 'An Post: 800,000+ transactions. Zero CNP fraud.' proof stat"
    - "The footer shows an award badge placeholder"
  artifacts:
    - path: "src/components/marketing/Nav.tsx"
      provides: "Sticky mega-menu navigation component"
      contains: "Platform"
    - path: "src/components/marketing/Footer.tsx"
      provides: "Footer with proof stat and award badge"
      contains: "800,000+"
    - path: "src/app/(marketing)/layout.tsx"
      provides: "Marketing layout wiring Nav + Footer around page content"
      contains: "Nav"
  key_links:
    - from: "src/app/(marketing)/layout.tsx"
      to: "src/components/marketing/Nav.tsx"
      via: "import and render"
      pattern: "import.*Nav"
    - from: "src/app/(marketing)/layout.tsx"
      to: "src/components/marketing/Footer.tsx"
      via: "import and render"
      pattern: "import.*Footer"
    - from: "src/components/marketing/Nav.tsx"
      to: "/portal"
      via: "Portal icon link"
      pattern: "href.*portal"
---

<objective>
Build the sticky mega-menu navigation and footer, then wire them into the marketing layout. These components appear on every public page for all subsequent phases.

Purpose: Without the shared layout, every subsequent phase would need to build nav/footer individually. This creates the shell that all marketing pages inherit.
Output: Nav.tsx with mega-menu, Footer.tsx with An Post proof stat, and (marketing)/layout.tsx wiring them together.
</objective>

<execution_context>
@/Users/markwright/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markwright/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build sticky mega-menu Nav component</name>
  <files>src/components/marketing/Nav.tsx</files>
  <action>
Create `src/components/marketing/Nav.tsx` — a sticky navigation with Platform mega-menu dropdown, Portal icon, and Request Demo CTA.

The component must be a Client Component (`'use client'`) because it manages open/close state for the mega-menu dropdown.

Nav requirements from user decisions (LOCKED — do not deviate):
- Sticky: fixed to top, always visible on scroll
- Platform mega-menu dropdown: opens on click/hover, contains product links + Platform Overview link
- Portal icon: lock icon when logged out, links to `/portal`
- "Request Demo" CTA: right-aligned, uses Button variant="primary"
- No auth yet — portal icon just links to /portal with no auth check

Platform mega-menu products to include (from PRD/existing site):
- Platform Overview → /platform
- Dynamic Security Codes → /dynamic-security-codes
- Safe Verify → /safe-verify
- (Additional product links as placeholders)

Other nav links:
- Proof → /proof (placeholder)
- Company → /company (placeholder)
- Resources → /resources (placeholder)

```tsx
'use client'

import { useState } from 'react'
import Link from 'next/link'
import { Button } from '@/components/ui'

const platformLinks = [
  { label: 'Platform Overview', href: '/platform', description: 'One integration. Seven products.' },
  { label: 'Dynamic Security Codes', href: '/dynamic-security-codes', description: 'Eliminate CNP fraud at source' },
  { label: 'Safe Verify', href: '/safe-verify', description: 'Phone channel authentication' },
]

export function Nav() {
  const [platformOpen, setPlatformOpen] = useState(false)

  return (
    <header className="sticky top-0 z-50 w-full border-b border-base-300 bg-base-100/95 backdrop-blur supports-[backdrop-filter]:bg-base-100/80">
      <nav className="mx-auto flex max-w-7xl items-center justify-between px-4 py-3 sm:px-6 lg:px-8">
        {/* Logo */}
        <Link href="/" className="flex items-center gap-2">
          <span className="text-xl font-bold text-base-content">
            Safe<span className="text-primary">Cypher</span>
          </span>
        </Link>

        {/* Centre nav links */}
        <div className="hidden items-center gap-1 md:flex">
          {/* Platform dropdown */}
          <div className="relative">
            <button
              onClick={() => setPlatformOpen((prev) => !prev)}
              className="btn btn-ghost btn-sm gap-1"
              aria-expanded={platformOpen}
              aria-haspopup="true"
            >
              Platform
              <svg
                className={`h-4 w-4 transition-transform ${platformOpen ? 'rotate-180' : ''}`}
                fill="none"
                viewBox="0 0 24 24"
                stroke="currentColor"
                strokeWidth={2}
                aria-hidden="true"
              >
                <path strokeLinecap="round" strokeLinejoin="round" d="M19 9l-7 7-7-7" />
              </svg>
            </button>

            {/* Mega-menu dropdown */}
            {platformOpen && (
              <div className="absolute left-1/2 top-full z-50 mt-2 w-80 -translate-x-1/2 rounded-box bg-base-200 p-4 shadow-xl border border-base-300">
                <div className="space-y-1">
                  {platformLinks.map((link) => (
                    <Link
                      key={link.href}
                      href={link.href}
                      onClick={() => setPlatformOpen(false)}
                      className="block rounded-lg px-3 py-2 hover:bg-base-300 transition-colors"
                    >
                      <p className="font-medium text-base-content">{link.label}</p>
                      <p className="text-sm text-base-content/60">{link.description}</p>
                    </Link>
                  ))}
                </div>
              </div>
            )}
          </div>

          <Link href="/proof" className="btn btn-ghost btn-sm">Proof</Link>
          <Link href="/company" className="btn btn-ghost btn-sm">Company</Link>
          <Link href="/resources" className="btn btn-ghost btn-sm">Resources</Link>
        </div>

        {/* Right side: Portal icon + CTA */}
        <div className="flex items-center gap-3">
          {/* Portal icon (lock) */}
          <Link
            href="/portal"
            className="btn btn-ghost btn-sm btn-square"
            aria-label="Customer Portal"
            title="Customer Portal"
          >
            <svg
              className="h-5 w-5"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              strokeWidth={2}
              aria-hidden="true"
            >
              <path
                strokeLinecap="round"
                strokeLinejoin="round"
                d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"
              />
            </svg>
          </Link>

          {/* Request Demo CTA */}
          <Link href="/contact">
            <Button variant="primary" size="sm">
              Request Demo
            </Button>
          </Link>
        </div>
      </nav>

      {/* Close mega-menu when clicking outside */}
      {platformOpen && (
        <div
          className="fixed inset-0 z-40"
          onClick={() => setPlatformOpen(false)}
          aria-hidden="true"
        />
      )}
    </header>
  )
}
```

Note: Bootstrap Icons (from existing site) are NOT imported — we use inline SVG for the lock and chevron icons to avoid a CDN dependency. The existing site used Bootstrap Icons via CDN; in Next.js we use inline SVGs for zero-dependency icon rendering. This is a discretion choice.
  </action>
  <verify>
    Run: `npx tsc --noEmit`
    Expected: Zero type errors in Nav.tsx.

    Run: `npm run build`
    Expected: Passes.

    Manual check: `grep -n "sticky top-0" src/components/marketing/Nav.tsx` returns a result.
    Manual check: `grep -n "href.*portal" src/components/marketing/Nav.tsx` returns a result.
    Manual check: `grep -n "Request Demo" src/components/marketing/Nav.tsx` returns a result.
  </verify>
  <done>
    - Nav.tsx exists with 'use client' directive
    - Sticky positioning via `sticky top-0 z-50`
    - Platform mega-menu dropdown with platformLinks array (Platform Overview, Dynamic Security Codes, Safe Verify)
    - Portal lock icon links to /portal
    - Request Demo button (primary variant) right-aligned
    - Zero TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Footer component and wire marketing layout</name>
  <files>
    src/components/marketing/Footer.tsx
    src/app/(marketing)/layout.tsx
  </files>
  <action>
1. **Create `src/components/marketing/Footer.tsx`**:

Footer requirements from user decisions (LOCKED):
- An Post proof stat: "800,000+ transactions. Zero CNP fraud."
- Award badge placeholder (Irish Fintech Award — actual image not yet available, use placeholder)
- Standard links (navigation, legal)

```tsx
import Link from 'next/link'

const footerLinks = {
  Products: [
    { label: 'Platform Overview', href: '/platform' },
    { label: 'Dynamic Security Codes', href: '/dynamic-security-codes' },
    { label: 'Safe Verify', href: '/safe-verify' },
  ],
  Company: [
    { label: 'About Us', href: '/company' },
    { label: 'Contact', href: '/contact' },
    { label: 'Resources', href: '/resources' },
  ],
  Legal: [
    { label: 'Privacy Policy', href: '/privacy' },
    { label: 'Terms & Conditions', href: '/terms' },
  ],
}

export function Footer() {
  return (
    <footer className="border-t border-base-300 bg-base-200">
      {/* Proof bar */}
      <div className="border-b border-base-300 bg-base-300/50">
        <div className="mx-auto flex max-w-7xl flex-col items-center justify-between gap-4 px-4 py-6 sm:flex-row sm:px-6 lg:px-8">
          {/* An Post proof stat */}
          <div className="text-center sm:text-left">
            <p className="text-lg font-semibold text-base-content">
              An Post:{' '}
              <span className="text-primary">800,000+ transactions.</span>
            </p>
            <p className="text-base-content/60">Zero CNP fraud.</p>
          </div>

          {/* Award badge placeholder */}
          <div className="flex items-center gap-2 rounded-lg border border-base-content/20 px-4 py-2">
            <div className="h-10 w-10 rounded bg-base-content/10 flex items-center justify-center">
              <svg className="h-6 w-6 text-primary" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
              </svg>
            </div>
            <div>
              <p className="text-sm font-medium text-base-content">Irish Fintech Award</p>
              <p className="text-xs text-base-content/60">[PLACEHOLDER — badge image pending]</p>
            </div>
          </div>
        </div>
      </div>

      {/* Main footer content */}
      <div className="mx-auto max-w-7xl px-4 py-10 sm:px-6 lg:px-8">
        <div className="grid grid-cols-2 gap-8 md:grid-cols-4">
          {/* Brand column */}
          <div className="col-span-2 md:col-span-1">
            <Link href="/" className="text-xl font-bold text-base-content">
              Safe<span className="text-primary">Cypher</span>
            </Link>
            <p className="mt-3 text-sm text-base-content/60">
              Eliminate card-not-present fraud. Not reduce. Eliminate.
            </p>
          </div>

          {/* Link columns */}
          {Object.entries(footerLinks).map(([category, links]) => (
            <div key={category}>
              <h3 className="text-sm font-semibold uppercase tracking-wider text-base-content/40">
                {category}
              </h3>
              <ul className="mt-4 space-y-2">
                {links.map((link) => (
                  <li key={link.href}>
                    <Link
                      href={link.href}
                      className="text-sm text-base-content/60 hover:text-base-content transition-colors"
                    >
                      {link.label}
                    </Link>
                  </li>
                ))}
              </ul>
            </div>
          ))}
        </div>

        {/* Bottom bar */}
        <div className="mt-10 border-t border-base-300 pt-6">
          <p className="text-center text-xs text-base-content/40">
            © {new Date().getFullYear()} SafeCypher Ltd. All rights reserved.
          </p>
        </div>
      </div>
    </footer>
  )
}
```

2. **Update `src/app/(marketing)/layout.tsx`** to wire Nav and Footer around page content:
   ```tsx
   import { Nav } from '@/components/marketing/Nav'
   import { Footer } from '@/components/marketing/Footer'

   export default function MarketingLayout({
     children,
   }: {
     children: React.ReactNode
   }) {
     return (
       <>
         <Nav />
         <main className="min-h-screen">{children}</main>
         <Footer />
       </>
     )
   }
   ```

   CRITICAL: Nav and Footer go in (marketing)/layout.tsx ONLY — NOT in the root src/app/layout.tsx. The root layout must stay html+body only. Portal pages do NOT get the marketing nav/footer.
  </action>
  <verify>
    Run: `npx tsc --noEmit`
    Expected: Zero type errors.

    Run: `npm run build`
    Expected: Build passes.

    Run: `npm run dev` then visit http://localhost:3000
    Expected:
    - Sticky nav appears at top with SafeCypher logo, Platform link, Portal lock icon, Request Demo button
    - Footer appears at bottom with "800,000+ transactions. Zero CNP fraud." proof stat and award badge placeholder
    - Scrolling the page keeps nav fixed at top

    Check structure: `grep -n "Nav" src/app/(marketing)/layout.tsx` returns import and usage.
    Check structure: `grep -n "Footer" src/app/(marketing)/layout.tsx` returns import and usage.
    Check that root layout is NOT modified: `grep -n "Nav\|Footer" src/app/layout.tsx` returns no results.
  </verify>
  <done>
    - Footer.tsx renders An Post proof stat ("800,000+ transactions. Zero CNP fraud.") and award badge placeholder
    - (marketing)/layout.tsx imports and renders Nav above main, Footer below main
    - Root layout.tsx still contains ONLY html+body (no Nav/Footer)
    - Nav is sticky and visible at top of localhost:3000
    - Footer is visible at bottom of localhost:3000
    - Zero TypeScript errors, build passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete foundation</name>
  <files>No files — human verification only</files>
  <action>Human visual and functional verification of the complete Phase 1 foundation (see how-to-verify below).</action>
  <verify>See how-to-verify section. Run: npm run dev, visit http://localhost:3000, and confirm all items listed.</verify>
  <done>Human approves via resume-signal: all nav, footer, theme, and CI items confirmed working.</done>
  <what-built>
    Complete Phase 1: Foundation — Next.js project with CI/CD pipeline, SafeCypher brand theme, and shared layout.
    - Next.js 14+ App Router with TypeScript strict mode and correct folder structure
    - GitHub Actions CI (lint + type-check + build) on every PR
    - DaisyUI v5 + Tailwind v4 with safecypher-dark theme (blue primary, dark background)
    - Sticky mega-menu Nav with Platform dropdown, Portal lock icon, Request Demo CTA
    - Footer with An Post proof stat and award badge placeholder
    - Base UI components: Button, Card, Badge, Input
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:3000
    2. Verify: Dark background, SafeCypher branding visible
    3. Verify: Sticky nav at top — scroll down, nav stays fixed
    4. Verify: Click "Platform" — mega-menu opens showing Platform Overview, Dynamic Security Codes, Safe Verify links
    5. Verify: Lock icon visible in nav (right side), links to /portal
    6. Verify: "Request Demo" blue button visible right-aligned in nav
    7. Verify: Scroll to bottom — footer shows "800,000+ transactions. Zero CNP fraud." and award badge placeholder
    8. Verify: Blue primary colour on buttons matches #3b82f6
    9. Run `npx tsc --noEmit` — confirm zero errors
    10. Run `npm run lint` — confirm zero errors
  </how-to-verify>
  <resume-signal>Type "approved" if the foundation looks correct, or describe any visual/functional issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` — passes
2. `npx tsc --noEmit` — zero errors
3. `npm run lint` — zero errors
4. localhost:3000 shows dark background with blue SafeCypher nav
5. Platform mega-menu opens and closes on click
6. Nav is sticky (stays at top when scrolling)
7. Portal lock icon present in nav, links to /portal
8. Footer renders proof stat: "800,000+ transactions. Zero CNP fraud."
9. Footer renders award badge placeholder
10. Root layout.tsx has no Nav/Footer references (marketing layout only)
</verification>

<success_criteria>
- Full Phase 1 foundation visible and functional in browser
- Sticky nav with Platform mega-menu, Portal icon, and Request Demo CTA
- Footer with An Post proof stat and award badge placeholder
- DaisyUI brand theme rendering correctly (dark background, blue primary)
- All three CI checks pass locally: lint, type-check, build
- Human visual verification approved before phase closes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md` following the summary template.
</output>
