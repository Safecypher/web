---
phase: 03-platform-dynamic-security-codes
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/dynamic-security-codes/page.tsx
  - src/components/marketing/dsc/DscHeroSection.tsx
  - src/components/marketing/dsc/DscSolutionSection.tsx
  - src/components/marketing/dsc/HowItWorksSection.tsx
  - src/components/marketing/dsc/DscProofSection.tsx
  - src/components/marketing/dsc/ForIssuersSection.tsx
  - src/components/marketing/dsc/DscCtaSection.tsx
  - public/screenshots/dsc/.gitkeep
autonomous: true
requirements: [DSC-01, DSC-02, DSC-03]

must_haves:
  truths:
    - "A visitor navigating to /dynamic-security-codes sees an above-fold problem frame about static CVVs being the attack surface"
    - "The solution section explains dynamic time-sensitive codes delivered through the cardholder's banking app, expiring after single use"
    - "The how-it-works section renders six step cards in a 2-column (mobile) / 3-column (desktop) grid, each with a step number, heading, and description"
    - "Each step card has a placeholder image area — a styled div with step number label, never a broken image — until screenshots are placed in public/screenshots/dsc/"
    - "The page shares the marketing layout (Nav + Footer) from (marketing)/layout.tsx"
  artifacts:
    - path: "src/app/(marketing)/dynamic-security-codes/page.tsx"
      provides: "/dynamic-security-codes route with metadata and six section imports"
      exports: ["default function DscPage", "metadata"]
    - path: "src/components/marketing/dsc/DscHeroSection.tsx"
      provides: "Problem frame — static CVV credential design failure argument"
      min_lines: 30
    - path: "src/components/marketing/dsc/DscSolutionSection.tsx"
      provides: "Dynamic code solution — time-limited, app-native, single-use"
      min_lines: 30
    - path: "src/components/marketing/dsc/HowItWorksSection.tsx"
      provides: "Six step cards grid with placeholder image areas (div-based, not Image) and copy"
      min_lines: 80
  key_links:
    - from: "src/app/(marketing)/dynamic-security-codes/page.tsx"
      to: "src/components/marketing/dsc/HowItWorksSection.tsx"
      via: "named import"
      pattern: "import.*HowItWorksSection"
    - from: "src/components/marketing/dsc/DscSolutionSection.tsx"
      to: "/platform"
      via: "Next.js Link"
      pattern: "href.*platform"
    - from: "src/components/marketing/dsc/HowItWorksSection.tsx"
      to: "placeholder div areas"
      via: "div with aspect-[9/16] bg-base-300"
      pattern: "aspect-\\[9/16\\]"
---

<objective>
Build the /dynamic-security-codes route (page.tsx with metadata) and its first three section components: the static CVV problem frame (DSC-01), the dynamic code solution section (DSC-02), and the visual six-step how-it-works flow with screenshot placeholder areas (DSC-03). Also scaffold stub files for the three remaining sections that plan 03-04 will fill.

Purpose: This is the flagship product page for SafeCypher's most mature product. Visitors coming from the Platform page, the homepage hero, or search need an immediate argument that static CVVs are the attack vector — and a concrete explanation of how dynamic codes eliminate that vector, illustrated with real cardholder UX steps.
Output: /dynamic-security-codes route with DscHeroSection, DscSolutionSection, HowItWorksSection as Server Components. Three stubs (DscProofSection, ForIssuersSection, DscCtaSection) scaffold the page for plan 03-04.
</objective>

<execution_context>
@/Users/markwright/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markwright/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-platform-dynamic-security-codes/03-CONTEXT.md
@.planning/phases/03-platform-dynamic-security-codes/03-RESEARCH.md
@src/app/(marketing)/page.tsx
@src/components/marketing/home/UrgencySection.tsx
@src/components/marketing/home/ProofSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold /dynamic-security-codes route and stub imports</name>
  <files>
    src/app/(marketing)/dynamic-security-codes/page.tsx
    src/components/marketing/dsc/DscProofSection.tsx
    src/components/marketing/dsc/ForIssuersSection.tsx
    src/components/marketing/dsc/DscCtaSection.tsx
    public/screenshots/dsc/.gitkeep
  </files>
  <action>
Create `src/app/(marketing)/dynamic-security-codes/page.tsx` as a Server Component (no 'use client'). Export a `metadata` object:
```tsx
export const metadata: Metadata = {
  title: 'Dynamic Security Codes — SafeCypher',
  description: 'Time-limited CVV codes delivered through your cardholder\'s banking app. Stolen credentials expire before they can be used.',
}
```
Import type `Metadata` from `'next'`. Import and render six section components in order: DscHeroSection, DscSolutionSection, HowItWorksSection, DscProofSection, ForIssuersSection, DscCtaSection — all from `@/components/marketing/dsc/`.

Create stub files for the three sections plan 03-04 will fill. Each stub exports a function returning `null` — no 'use client', no JSX content:
- `src/components/marketing/dsc/DscProofSection.tsx`
- `src/components/marketing/dsc/ForIssuersSection.tsx`
- `src/components/marketing/dsc/DscCtaSection.tsx`

Create `public/screenshots/dsc/` directory with a `.gitkeep` file so the path exists before screenshot assets are placed. Do NOT create placeholder `<Image>` pointing to non-existent files — the image handling is in Task 3.
  </action>
  <verify>Run `npx tsc --noEmit` — zero errors. Run `npm run build` — succeeds with /dynamic-security-codes in the build output routes list.</verify>
  <done>/dynamic-security-codes route exists in the build output. page.tsx exports metadata with correct title and description. Three stub component files exist and compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Build DscHeroSection and DscSolutionSection</name>
  <files>
    src/components/marketing/dsc/DscHeroSection.tsx
    src/components/marketing/dsc/DscSolutionSection.tsx
  </files>
  <action>
**DscHeroSection** (`src/components/marketing/dsc/DscHeroSection.tsx`) — pure Server Component:
- Section: `<section className="bg-base-100 py-24 lg:py-32">`
- Container: `max-w-5xl` (editorial width — this is a product argument, not a gallery)
- Eyebrow: "Dynamic Security Codes" (text-accent uppercase tracking-widest text-xs font-semibold)
- Product badge above eyebrow: `<span className="badge badge-primary badge-outline mb-4 text-xs">Flagship Product</span>` on its own line before the eyebrow
- Headline: `text-4xl lg:text-5xl font-bold text-base-content` — "The CVV on your card never changes. That's the problem."
- Sub-headline: `text-xl text-base-content/60 mt-4 leading-relaxed` — "Every card-not-present fraud attack exploits the same vulnerability: static credentials printed on a physical card that remain valid forever, regardless of how many times they're stolen."
- Three-paragraph argument:
  1. "The 3-digit CVV on the back of your card was designed in the 1990s as a basic verification mechanism. It has not fundamentally changed since. Once a fraudster captures it — through phishing, data breach, or skimming — they have everything they need to make card-not-present purchases indefinitely." (`text-base-content/70 leading-relaxed`)
  2. "This is not a verification problem. It is a credential design problem. No amount of anomaly detection, transaction monitoring, or 3DS friction changes the fact that a stolen static CVV is a valid credential that works until the card expires." (`text-base-content/70 leading-relaxed`)
  3. "Dynamic Security Codes eliminate the attack surface. A new, time-limited code is generated for each transaction. The moment a fraudster captures it, it is already invalid." (`text-base-content font-medium leading-relaxed` — slightly more prominent to close the argument)
- No CTA — the argument leads into DscSolutionSection.

**DscSolutionSection** (`src/components/marketing/dsc/DscSolutionSection.tsx`) — pure Server Component:
- Section: `<section className="bg-neutral border-t border-base-300 py-24 lg:py-32">`
- Container: `max-w-5xl`
- Eyebrow: "The Solution" (text-accent uppercase tracking-widest text-xs font-semibold)
- Headline: "A CVV that expires. Every time." (`text-3xl lg:text-4xl font-bold text-neutral-content`)
- Body: "SafeCypher generates a fresh 3-digit Dynamic Security Code for every card-not-present transaction. The code appears in the cardholder's banking app — the one they already use every day. It is valid for a single use, and expires in minutes. Stolen credentials become worthless the moment they leave the fraudster's hands." (`text-neutral-content/70 leading-relaxed mt-4 text-lg`)

Four highlight cards (`grid grid-cols-1 sm:grid-cols-2 gap-6 mt-10`), each `card bg-base-100 rounded-xl p-6`:
- Card 1: inline SVG (clock icon, w-6 h-6 text-accent stroke-current strokeWidth 2) + "Single-use" heading + "One code. One transaction. Used once, it's gone."
- Card 2: inline SVG (smartphone icon) + "App-native" heading + "Delivered in the cardholder's existing banking app. No new device. No SMS to intercept."
- Card 3: inline SVG (shield icon) + "No card reissuance" heading + "Works with the existing card in the cardholder's wallet. No plastic replacement cycle."
- Card 4: inline SVG (check/tick icon) + "Invisible to genuine customers" heading + "Three extra taps to activate. Then business as usual — but fraud-free."

Below the cards, add a cross-link paragraph: `<p className="text-neutral-content/50 text-sm mt-10">Part of the SafeCypher platform — <Link href="/platform" className="text-accent hover:underline">see how one API unlocks seven products</Link>.</p>`

All inline SVGs: w-6 h-6, stroke-current, strokeWidth 2, no fill. Use semantic paths (same approach as AudiencesSection.tsx in homepage).
Import `Link` from `'next/link'`.
  </action>
  <verify>Run `npm run lint` — zero lint errors. Run `npx tsc --noEmit` — zero type errors. In `npm run dev` at /dynamic-security-codes, DscHeroSection renders problem frame with badge + eyebrow + headline + 3 paragraphs; DscSolutionSection renders 4 highlight cards and cross-link to /platform.</verify>
  <done>DscHeroSection renders the static CVV problem frame (badge, eyebrow, headline, 3 paragraphs). DscSolutionSection renders the solution with 4 feature cards and a link back to /platform. No TypeScript errors. No lint errors.</done>
</task>

<task type="auto">
  <name>Task 3: Build HowItWorksSection — six-step card grid with placeholder image areas</name>
  <files>src/components/marketing/dsc/HowItWorksSection.tsx</files>
  <action>
Create `src/components/marketing/dsc/HowItWorksSection.tsx` as a pure Server Component (no 'use client'). Do NOT import `Image` from `'next/image'` — the screenshot files are not yet available and placeholder divs are used instead. Do not add a forward-declaration import that would trigger lint warnings.

Section: `<section className="bg-base-100 border-t border-base-300 py-24 lg:py-32">`
Container: `max-w-7xl`
Section header (text-center mb-16):
- Eyebrow: "The Cardholder Experience" (text-accent uppercase tracking-widest text-xs font-semibold)
- Headline: "How it works" (`text-3xl lg:text-4xl font-bold text-base-content`)
- Subheadline: "Six taps. Full protection. No friction for genuine cardholders." (`text-base-content/60 mt-4 max-w-2xl mx-auto`)

Steps data array (define above the component function):
```tsx
const steps = [
  {
    number: 1,
    heading: 'Open your banking app',
    description: 'Navigate to your card account in your issuer\'s mobile banking app — the same app cardholders use every day.',
  },
  {
    number: 2,
    heading: 'Go to Manage card',
    description: 'Find the card management section to access security and protection settings.',
  },
  {
    number: 3,
    heading: 'Enable Dynamic CVV',
    description: 'Toggle on the "3-digit Dynamic CVV" to activate fraud protection for card-not-present transactions.',
  },
  {
    number: 4,
    heading: 'Understand the protection',
    description: 'The app explains how dynamic codes eliminate CNP fraud — transparency builds cardholder trust.',
  },
  {
    number: 5,
    heading: 'New code every transaction',
    description: 'A fresh security code is generated each time you shop online. Used once, it expires immediately.',
  },
  {
    number: 6,
    heading: 'Find your code in the app',
    description: 'Your dynamic CVV is always visible in the app — ready for your next purchase, and worthless to anyone who intercepts it.',
  },
]
```

Grid: `<div className="grid grid-cols-2 md:grid-cols-3 gap-6">`

Screenshot area implementation — placeholder divs only (CRITICAL — prevents broken images). Screenshots are not yet available in public/screenshots/dsc/. Do NOT use `<Image>` pointing to potentially missing files:

```tsx
{/* TODO: Replace with <Image src={`/screenshots/dsc/step-${step.number}-*.png`} ...> once screenshots are placed in public/screenshots/dsc/ */}
<div className="relative bg-base-300 aspect-[9/16] overflow-hidden rounded-t-xl flex items-center justify-center">
  <div className="text-center px-4">
    <p className="text-xs font-mono text-base-content/30 uppercase tracking-wider">Step {step.number}</p>
    <p className="text-xs text-base-content/20 mt-1">App screenshot</p>
  </div>
</div>
```

Card structure per step:
```tsx
<div key={step.number} className="card bg-base-200 rounded-xl overflow-hidden border border-base-300">
  {/* Screenshot placeholder area */}
  ...
  {/* Copy */}
  <div className="p-5">
    <span className="text-xs font-bold text-accent uppercase tracking-widest">
      Step {step.number}
    </span>
    <h3 className="text-base font-semibold text-base-content mt-1">
      {step.heading}
    </h3>
    <p className="text-sm text-base-content/60 mt-2 leading-relaxed">
      {step.description}
    </p>
  </div>
</div>
```
  </action>
  <verify>Run `npx tsc --noEmit` — zero errors. Run `npm run lint` — zero errors. Run `npm run build` — succeeds. In `npm run dev` at /dynamic-security-codes, HowItWorksSection renders a 6-card grid with step numbers, headings, descriptions, and placeholder image areas (styled divs — no broken images). Cards wrap to 2 columns on mobile (resize browser to confirm).</verify>
  <done>HowItWorksSection renders six step cards in a grid (2-col mobile, 3-col desktop). Each card shows step number, heading, description, and a placeholder div image area — no broken images, no TypeScript errors, no lint errors. Build passes.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run lint` passes with zero errors
3. `npm run build` succeeds — /dynamic-security-codes in build output
4. /dynamic-security-codes renders DscHeroSection (badge + problem frame), DscSolutionSection (4 feature cards + cross-link to /platform), HowItWorksSection (6-card grid)
5. DscHeroSection: "Flagship Product" badge, headline "The CVV on your card never changes. That's the problem.", three body paragraphs
6. DscSolutionSection: four cards (Single-use, App-native, No card reissuance, Invisible to genuine customers), link to /platform
7. HowItWorksSection: grid of 6 cards, each with step number (Step 1–6), heading, description, placeholder div image area — no broken images
8. Three stub files (DscProofSection, ForIssuersSection, DscCtaSection) compile cleanly
9. public/screenshots/dsc/ directory exists
</verification>

<success_criteria>
- /dynamic-security-codes route exists and builds without errors
- DscHeroSection: product badge, problem frame argument (3 paragraphs), no CTA
- DscSolutionSection: 4 feature cards + cross-link to /platform
- HowItWorksSection: 6 step cards in 2×3 grid (wraps to 2-col on mobile), placeholder div image areas (no Image component, no broken images)
- Three stubs ready for plan 03-04
- Zero TypeScript errors, zero lint errors, build passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-platform-dynamic-security-codes/03-03-SUMMARY.md`
</output>
