---
phase: 03-platform-dynamic-security-codes
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/marketing/platform/ProductPortfolioSection.tsx
  - src/components/marketing/platform/CompetitiveSection.tsx
  - src/components/marketing/platform/PlatformCtaSection.tsx
  - src/components/marketing/shared/PageCtaSection.tsx
autonomous: true
requirements: [PLAT-04, PLAT-05, PLAT-06]

must_haves:
  truths:
    - "A visitor on /platform can see all seven SafeCypher products in a table with audience, description, and integration effort per product"
    - "The comparison table shows four rows — Tokenisation, 3DS, Behavioural Analytics, SafeCypher — with the gap column clearly differentiating each approach"
    - "The SafeCypher row in the comparison table is visually differentiated (highlighted) and its gap column reads 'Eliminates CNP fraud'"
    - "The page ends with two CTAs: a calculator deep-link and a demo request link"
    - "Clicking 'Request a demo' navigates to /#demo on the homepage"
  artifacts:
    - path: "src/components/marketing/platform/ProductPortfolioSection.tsx"
      provides: "Seven-product DaisyUI table with audience, description, effort columns"
      min_lines: 60
    - path: "src/components/marketing/platform/CompetitiveSection.tsx"
      provides: "Comparison table — 4 rows × 3 columns with highlighted SafeCypher row"
      min_lines: 60
    - path: "src/components/marketing/platform/PlatformCtaSection.tsx"
      provides: "CTA section — calculator link + demo request"
      min_lines: 20
    - path: "src/components/marketing/shared/PageCtaSection.tsx"
      provides: "Shared CTA section component reusable by both platform and DSC pages"
      min_lines: 20
  key_links:
    - from: "src/components/marketing/platform/PlatformCtaSection.tsx"
      to: "/portal/calculator"
      via: "Next.js Link"
      pattern: "href.*portal/calculator"
    - from: "src/components/marketing/platform/PlatformCtaSection.tsx"
      to: "/#demo"
      via: "Next.js Link"
      pattern: "href.*#demo"
    - from: "src/components/marketing/platform/ProductPortfolioSection.tsx"
      to: "/dynamic-security-codes"
      via: "Next.js Link on DSC product row"
      pattern: "href.*dynamic-security-codes"
---

<objective>
Complete the /platform page by replacing the three stub components from plan 03-01 with full implementations: the seven-product portfolio table (PLAT-04), the competitive comparison table (PLAT-05), and the CTA section (PLAT-06). Also create the shared PageCtaSection component that plan 03-04 (DSC page) will reuse.

Purpose: A prospect reading /platform needs to understand the full product scope (seven products, one API) and how SafeCypher's approach differs from existing tools — then be given a clear next step. These three sections complete the /platform page narrative arc.
Output: ProductPortfolioSection (DaisyUI table, all seven products), CompetitiveSection (DaisyUI comparison table, SafeCypher row highlighted), PlatformCtaSection (calculator + demo CTAs), and PageCtaSection in shared/ for DSC page reuse.
</objective>

<execution_context>
@/Users/markwright/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markwright/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-platform-dynamic-security-codes/03-CONTEXT.md
@.planning/phases/03-platform-dynamic-security-codes/03-RESEARCH.md
@.planning/phases/03-platform-dynamic-security-codes/03-01-SUMMARY.md
@src/app/(marketing)/platform/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ProductPortfolioSection — seven-product DaisyUI table</name>
  <files>src/components/marketing/platform/ProductPortfolioSection.tsx</files>
  <action>
Replace the stub in `src/components/marketing/platform/ProductPortfolioSection.tsx` with a full Server Component implementation. No 'use client'.

Section wrapper: `<section className="bg-base-100 border-t border-base-300 py-24 lg:py-32">`
Container: `max-w-6xl`
Section header (text-center mb-12):
- Eyebrow: "The Full Portfolio" (text-accent uppercase tracking-widest text-xs font-semibold)
- Headline: "One integration. Seven products." (text-3xl lg:text-4xl font-bold text-base-content)
- Subheadline: "Every additional product activates from the same processor-level integration — no new infrastructure, no new contracts." (text-base-content/60 mt-4 max-w-2xl mx-auto)

Table: `<div className="overflow-x-auto rounded-xl border border-base-300">` wrapping `<table className="table table-zebra w-full">` (DaisyUI v5 — do NOT use table-compact).
Table head (`<thead><tr className="bg-base-300 text-base-content/60 text-xs uppercase tracking-wider">`): four `<th className="py-4 px-6">` cells — "Product", "Audience", "What it does", "Integration effort".
Table body: map over the products data array below.

Products data array (define above the component):
```tsx
const products = [
  {
    name: 'Dynamic Security Codes',
    audience: 'Transactions',
    description: 'Time-limited CVV in the cardholder's banking app eliminates CNP fraud at the credential layer.',
    effort: 'Core integration',
    href: '/dynamic-security-codes',
  },
  {
    name: 'Safe Verify',
    audience: 'People',
    description: 'Dynamic phone-channel authentication for call centres — eliminates vishing and OTP intercept.',
    effort: 'Incremental — 1 endpoint',
    href: '/safe-verify',
  },
  {
    name: 'SafeAgent',
    audience: 'Agents',
    description: 'Authenticates AI agents initiating card transactions — the emerging agentic commerce attack surface.',
    effort: 'Incremental — 1 endpoint',
    href: null,
  },
  {
    name: 'SafePay (dCVV V2)',
    audience: 'Commerce',
    description: 'Dynamic CVV for e-commerce checkout — an upgraded credential layer for card-present equivalent assurance.',
    effort: 'Incremental — 1 endpoint',
    href: null,
  },
  {
    name: 'E-Wallet Onboarding',
    audience: 'Onboarding',
    description: 'Secure digital wallet provisioning — dynamic credential handshake at the point of card digitisation.',
    effort: 'Incremental — 1 endpoint',
    href: null,
  },
  {
    name: 'Card Issuance Protection',
    audience: 'Issuance',
    description: 'Protects credentials at the point of card issuance — closing the window between print and activation.',
    effort: 'Incremental — 1 endpoint',
    href: null,
  },
  {
    name: 'OTP Replacement',
    audience: 'Authentication',
    description: 'Replaces SMS OTP with dynamic in-app codes — eliminates SIM-swap and SS7 interception vectors.',
    effort: 'Incremental — 1 endpoint',
    href: null,
  },
]
```

Table row pattern per product:
```tsx
<tr key={product.name}>
  <td className="py-4 px-6 font-semibold text-base-content whitespace-nowrap">
    {product.href ? (
      <Link href={product.href} className="text-accent hover:underline">{product.name}</Link>
    ) : (
      product.name
    )}
  </td>
  <td className="py-4 px-6">
    <span className="badge badge-outline badge-sm text-base-content/60">{product.audience}</span>
  </td>
  <td className="py-4 px-6 text-base-content/70 text-sm">{product.description}</td>
  <td className="py-4 px-6 text-base-content/50 text-sm font-mono">{product.effort}</td>
</tr>
```

Import `Link` from `'next/link'`.
  </action>
  <verify>Run `npx tsc --noEmit` — zero errors. In `npm run dev` at /platform, the product portfolio table renders with seven rows, four columns, and DaisyUI zebra striping. DSC and Safe Verify rows are links. Audience column shows badges.</verify>
  <done>ProductPortfolioSection renders a full DaisyUI table with all seven products, four columns, zebra striping, and linked product names for DSC and Safe Verify. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build CompetitiveSection and PlatformCtaSection, create shared PageCtaSection</name>
  <files>
    src/components/marketing/platform/CompetitiveSection.tsx
    src/components/marketing/platform/PlatformCtaSection.tsx
    src/components/marketing/shared/PageCtaSection.tsx
  </files>
  <action>
**CompetitiveSection** (`src/components/marketing/platform/CompetitiveSection.tsx`) — pure Server Component:

Section: `<section className="bg-neutral border-t border-base-300 py-24 lg:py-32">`
Container: `max-w-5xl`
Section header (text-center mb-12):
- Eyebrow: "The Honest Comparison" (text-accent)
- Headline: "Existing tools reduce fraud. SafeCypher eliminates it." (text-neutral-content)
- Body: "Tokenisation, 3DS, and behavioural analytics are all legitimate tools — and none of them make stolen credentials worthless. SafeCypher does." (text-neutral-content/60 mt-4 max-w-2xl mx-auto)

Competitors data array (define above component):
```tsx
const competitors = [
  {
    approach: 'Tokenisation',
    covers: 'Replaces the PAN with a token bound to a specific merchant or device',
    gap: 'Token is static for the session — re-usable if intercepted before completion',
    isSafeCypher: false,
  },
  {
    approach: '3D Secure (3DS)',
    covers: 'Adds a cardholder authentication challenge at checkout',
    gap: 'Authentication relies on static credentials — friction introduced, credential value unchanged',
    isSafeCypher: false,
  },
  {
    approach: 'Behavioural Analytics',
    covers: 'Flags anomalous transaction patterns using ML scoring',
    gap: 'Detects fraud probability after credential theft — does not make credentials worthless',
    isSafeCypher: false,
  },
  {
    approach: 'SafeCypher',
    covers: 'Issues a new, time-limited credential per transaction via the cardholder\'s banking app',
    gap: 'Eliminates CNP fraud — stolen credentials expire before they can be used',
    isSafeCypher: true,
  },
]
```

Table: `<div className="overflow-x-auto rounded-xl border border-base-300"><table className="table table-zebra w-full">` (DaisyUI v5 — no table-compact).
Table head: `<tr className="bg-base-300 text-base-content/60 text-xs uppercase tracking-wider">` with three `<th className="py-4 px-6">` — "Approach", "What it covers", "What the gap is".
Table body row per competitor:
```tsx
<tr
  key={row.approach}
  className={row.isSafeCypher ? 'bg-primary/10' : ''}
>
  <td className="py-4 px-6 font-semibold text-base-content whitespace-nowrap">
    {row.approach}
    {row.isSafeCypher && (
      <span className="ml-2 badge badge-primary badge-sm">You are here</span>
    )}
  </td>
  <td className="py-4 px-6 text-base-content/70 text-sm">{row.covers}</td>
  <td className={`py-4 px-6 text-sm font-medium ${row.isSafeCypher ? 'text-primary font-semibold' : 'text-base-content/60'}`}>
    {row.gap}
  </td>
</tr>
```

**shared/PageCtaSection** — create directory `src/components/marketing/shared/` if it does not exist. Create `src/components/marketing/shared/PageCtaSection.tsx` as a pure Server Component:

```tsx
import Link from 'next/link'

export function PageCtaSection() {
  return (
    <section className="bg-base-100 border-t border-base-300 py-24 lg:py-32">
      <div className="mx-auto max-w-3xl px-4 sm:px-6 lg:px-8 text-center">
        <h2 className="text-3xl lg:text-4xl font-bold text-base-content">
          Ready to eliminate CNP fraud?
        </h2>
        <p className="text-base-content/60 mt-4 max-w-xl mx-auto">
          See the financial impact for your portfolio, or talk to the team directly.
        </p>
        <div className="flex flex-wrap justify-center gap-4 mt-8">
          <Link href="/portal/calculator" className="btn btn-outline btn-accent btn-lg">
            See the value for your portfolio
          </Link>
          <Link href="/#demo" className="btn btn-primary btn-lg">
            Request a demo
          </Link>
        </div>
      </div>
    </section>
  )
}
```

**PlatformCtaSection** (`src/components/marketing/platform/PlatformCtaSection.tsx`): Replace stub by importing and re-exporting from shared:
```tsx
import { PageCtaSection } from '@/components/marketing/shared/PageCtaSection'

export function PlatformCtaSection() {
  return <PageCtaSection />
}
```

Note on `/#demo` link: This cross-page hash anchor works in Next.js App Router with `scroll-behavior: smooth` already set in globals.css (confirmed in Phase 1). It navigates to the homepage and scrolls to `id="demo"` on DemoFormSection. No fix needed — verify in dev that clicking the link from /platform lands at the homepage demo form.
  </action>
  <verify>Run `npx tsc --noEmit` — zero errors. Run `npm run lint` — zero errors. Run `npm run build` — succeeds. In `npm run dev` at /platform: CompetitiveSection renders 4-row comparison table with SafeCypher row highlighted; PlatformCtaSection renders two CTAs (calculator + demo). Clicking "Request a demo" navigates to /#demo.</verify>
  <done>CompetitiveSection renders a 4-row, 3-column DaisyUI table with SafeCypher row visually differentiated and gap column reading 'Eliminates CNP fraud'. PlatformCtaSection and shared/PageCtaSection exist. /platform page is fully complete with all six sections rendering. Zero TypeScript errors. Zero lint errors. Build passes.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run lint` passes with zero errors
3. `npm run build` succeeds — /platform in output routes
4. /platform renders all six sections end-to-end: PlatformHeroSection → ApproachSection → ArchitectureDiagram → ProductPortfolioSection → CompetitiveSection → PlatformCtaSection
5. ProductPortfolioSection: seven rows visible, DSC and Safe Verify are links, Audience column shows badges, effort column shows "Core integration" for DSC and "Incremental — 1 endpoint" for others
6. CompetitiveSection: four rows, SafeCypher row has "You are here" badge and bg-primary/10 highlight, gap column for SafeCypher reads "Eliminates CNP fraud"
7. PlatformCtaSection: two buttons — "See the value for your portfolio" (→ /portal/calculator) and "Request a demo" (→ /#demo)
8. src/components/marketing/shared/PageCtaSection.tsx exists and exports PageCtaSection
</verification>

<success_criteria>
- ProductPortfolioSection: full DaisyUI table with all 7 products, 4 columns, zebra striping
- CompetitiveSection: 4-row comparison table, SafeCypher row highlighted, tone is honest not attack copy
- PlatformCtaSection: wraps shared PageCtaSection, renders calculator + demo CTAs
- shared/PageCtaSection.tsx created and ready for plan 03-04 (DSC page) to import
- /platform page is end-to-end complete with all 6 sections
- Zero TypeScript errors, zero lint errors, build passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-platform-dynamic-security-codes/03-02-SUMMARY.md`
</output>
