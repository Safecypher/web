---
phase: 03-platform-dynamic-security-codes
plan: "04"
type: execute
wave: 2
depends_on: ["03-03"]
files_modified:
  - src/components/marketing/dsc/DscProofSection.tsx
  - src/components/marketing/dsc/ForIssuersSection.tsx
  - src/components/marketing/dsc/DscCtaSection.tsx
autonomous: true
requirements: [DSC-04, DSC-05, DSC-06]

must_haves:
  truths:
    - "A visitor scrolling past the how-it-works section on /dynamic-security-codes sees the An Post proof metrics (800,000+ transactions, 18 months, zero CNP fraud)"
    - "The An Post logo placeholder and Irish Fintech Award badge are visible in the proof section"
    - "The for-issuers section renders a clear list of integration facts: single API, no card reissuance, major processor compatibility, live in weeks"
    - "The page ends with two CTAs: calculator deep-link and demo request"
    - "The /dynamic-security-codes page is fully complete — all six sections rendering end-to-end"
  artifacts:
    - path: "src/components/marketing/dsc/DscProofSection.tsx"
      provides: "An Post metrics, logo placeholder, Irish Fintech Award badge — adapted from homepage ProofSection pattern"
      min_lines: 60
    - path: "src/components/marketing/dsc/ForIssuersSection.tsx"
      provides: "Integration facts for issuers: single API, no reissuance, weeks to live, processor compatibility"
      min_lines: 50
    - path: "src/components/marketing/dsc/DscCtaSection.tsx"
      provides: "CTA section — wraps shared PageCtaSection from plan 03-02"
      min_lines: 10
  key_links:
    - from: "src/components/marketing/dsc/DscCtaSection.tsx"
      to: "src/components/marketing/shared/PageCtaSection.tsx"
      via: "named import"
      pattern: "import.*PageCtaSection"
    - from: "src/components/marketing/dsc/DscProofSection.tsx"
      to: "/proof/an-post"
      via: "Next.js Link"
      pattern: "href.*proof/an-post"
---

<objective>
Complete the /dynamic-security-codes page by replacing its three remaining stub components: the An Post proof section (DSC-04), the for-issuers integration facts section (DSC-05), and the CTA section (DSC-06) which reuses the shared PageCtaSection created in plan 03-02.

Purpose: The proof section closes the "does it work?" question with verified production results. The for-issuers section answers the "what does integration look like?" question directly for the Heads of Fraud / Digital audience. Together they earn the final CTAs — without these two sections, the page convinces but doesn't convert.
Output: DscProofSection (An Post metrics + award badge), ForIssuersSection (integration facts), DscCtaSection (shared CTA component). /dynamic-security-codes page fully complete.
</objective>

<execution_context>
@/Users/markwright/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markwright/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-platform-dynamic-security-codes/03-CONTEXT.md
@.planning/phases/03-platform-dynamic-security-codes/03-RESEARCH.md
@.planning/phases/03-platform-dynamic-security-codes/03-03-SUMMARY.md
@.planning/phases/03-platform-dynamic-security-codes/03-02-SUMMARY.md
@src/components/marketing/home/ProofSection.tsx
@src/components/marketing/shared/PageCtaSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build DscProofSection — An Post metrics adapted from homepage pattern</name>
  <files>src/components/marketing/dsc/DscProofSection.tsx</files>
  <action>
Replace the stub in `src/components/marketing/dsc/DscProofSection.tsx` with a full Server Component. Pattern: adapt from `src/components/marketing/home/ProofSection.tsx` — same data, same visual approach, same brand assets. Do NOT move ProofSection to shared/ (research recommendation: duplicate for Phase 3 simplicity; extract to shared/ only if a third usage emerges in Phase 4+).

Section: `<section className="bg-neutral border-t border-base-300 py-24 lg:py-32">`
Container: `max-w-7xl`

Section header (text-center mb-16):
- Eyebrow: "Proven in Production" (text-accent uppercase tracking-widest text-xs font-semibold)
- Headline: "Real results at scale." (`text-3xl lg:text-4xl font-bold text-neutral-content`)
- Body: "Not a pilot. Not a proof of concept. A live deployment protecting hundreds of thousands of real transactions across An Post's entire card-not-present portfolio." (`text-neutral-content/60 mt-4 max-w-2xl mx-auto`)

Two-column grid (`grid lg:grid-cols-2 gap-12 items-start`):

Left column — stat blocks (`space-y-8`):
- "800,000+" in `text-5xl font-bold text-primary` + "card-not-present transactions processed" in `text-neutral-content/70 text-base mt-1`
- "18 months" in `text-5xl font-bold text-primary` + "of live production operation" in `text-neutral-content/70 text-base mt-1`
- "Zero" in `text-5xl font-bold text-accent` + "CNP fraud incidents across the entire portfolio" in `text-neutral-content/70 text-base mt-1`
- `<Link href="/proof/an-post" className="btn btn-outline btn-primary mt-8">Read the An Post case study →</Link>`

Right column — proof card (`card bg-base-100 rounded-xl p-8 shadow-lg`):
- An Post logo: `<span className="font-bold text-2xl" style={{ color: '#006229' }}>An Post</span>` + `<p className="text-neutral-content/50 text-sm mt-1">Ireland's national postal service — card issuer and operator</p>`
- `<hr className="border-base-300 my-6" />`
- Blockquote: `<blockquote className="text-neutral-content/80 text-base italic leading-relaxed">"SafeCypher's Dynamic Security Codes gave us complete confidence in our card-not-present transactions. Zero fraud incidents across a live production portfolio — not a test, not a pilot."<cite className="text-neutral-content/50 text-sm not-italic mt-3 block">— An Post Financial Services</cite></blockquote>`
- Irish Fintech Award badge (identical to homepage implementation): `flex items-center gap-3 mt-6 p-3 bg-base-200 rounded-lg border border-base-300` containing a `w-10 h-10 rounded-full bg-warning/20 flex items-center justify-center` div with the star SVG (`fill="currentColor" className="text-warning"` with path `"M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"`) + text block "Irish Fintech Award" (text-xs font-semibold text-neutral-content) + "Fraud Prevention Category" (text-xs text-neutral-content/50)

Import `Link` from `'next/link'`.
  </action>
  <verify>Run `npx tsc --noEmit` — zero errors. In `npm run dev` at /dynamic-security-codes: DscProofSection renders with three stat blocks (800,000+, 18 months, Zero) on the left and the An Post proof card with blockquote and award badge on the right. Stats, logo, and badge match the homepage ProofSection visual treatment.</verify>
  <done>DscProofSection renders An Post stats, logo placeholder (brand green text), blockquote, and Irish Fintech Award badge. Link to /proof/an-post present. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build ForIssuersSection and DscCtaSection</name>
  <files>
    src/components/marketing/dsc/ForIssuersSection.tsx
    src/components/marketing/dsc/DscCtaSection.tsx
  </files>
  <action>
**ForIssuersSection** (`src/components/marketing/dsc/ForIssuersSection.tsx`) — pure Server Component:
Section: `<section className="bg-base-100 border-t border-base-300 py-24 lg:py-32">`
Container: `max-w-6xl`

Section header (text-center mb-14):
- Eyebrow: "For Issuers" (text-accent uppercase tracking-widest text-xs font-semibold)
- Headline: "Live in weeks. No disruption to your card programme." (`text-3xl lg:text-4xl font-bold text-base-content`)
- Body: "SafeCypher integrates at the processor level — meaning a single integration serves every issuer on that processor's network. For an individual issuer, activation is incremental: one API endpoint, no card reissuance, no customer re-enrolment." (`text-base-content/60 mt-4 max-w-2xl mx-auto`)

Four integration fact cards (`grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6`), each `card bg-base-200 rounded-xl p-6 border border-base-300`:

Card 1 — inline SVG (lightning bolt / zap, w-6 h-6 text-accent stroke-current strokeWidth 2) + "Single API endpoint" heading (`text-base font-semibold text-base-content mt-3`) + "One integration unlocks the full Dynamic Security Codes product. No additional infrastructure required." body (`text-sm text-base-content/60 mt-2 leading-relaxed`)

Card 2 — inline SVG (credit card icon) + "No card reissuance" heading + "Dynamic codes work with the existing card in the cardholder's wallet. No plastic replacement cycle, no cardholder disruption." body

Card 3 — inline SVG (clock/timer icon) + "Live in weeks" heading + "From signed contract to production deployment — weeks, not quarters. SafeCypher's processor-level model removes integration complexity." body

Card 4 — inline SVG (check-circle icon) + "Major processor compatible" heading + "Built to work with the processors already serving your card programme. TSYS compatibility confirmed." body

Below the four cards, add a detail row (`border border-base-300 rounded-xl p-6 mt-8 bg-base-200`):
- Heading: "What the integration looks like for your technical team" (`text-base font-semibold text-base-content mb-4`)
- Four-item grid (`grid grid-cols-1 sm:grid-cols-2 gap-3`):
  Each item is `flex items-start gap-3` with a checkmark SVG (`w-5 h-5 text-accent flex-shrink-0 mt-0.5`, strokeWidth 2.5, polyline "20 6 9 17 4 12") and a text span (`text-sm text-base-content/70`):
  1. "REST API — standard JSON request/response, documented via OpenAPI spec"
  2. "Processor-level implementation — your issuer team needs zero changes to existing card management systems"
  3. "Banking app integration — your mobile team adds a single screen using SafeCypher's SDK"
  4. "Sandbox environment available — test the full cardholder flow before go-live"

All inline SVGs use `stroke-current`, `strokeWidth={2}`, no fill, `text-accent`.

**DscCtaSection** (`src/components/marketing/dsc/DscCtaSection.tsx`): Replace stub by importing PageCtaSection from shared:
```tsx
import { PageCtaSection } from '@/components/marketing/shared/PageCtaSection'

export function DscCtaSection() {
  return <PageCtaSection />
}
```

This reuses the shared component created in plan 03-02 — the same "See the value for your portfolio" (→ /portal/calculator) + "Request a demo" (→ /#demo) CTA pair.
  </action>
  <verify>Run `npx tsc --noEmit` — zero errors. Run `npm run lint` — zero errors. Run `npm run build` — succeeds. In `npm run dev` at /dynamic-security-codes: ForIssuersSection renders four integration fact cards and the detail row. DscCtaSection renders the two CTAs. Scroll end-to-end through /dynamic-security-codes — all six sections render in sequence without layout breaks.</verify>
  <done>ForIssuersSection renders four integration fact cards (Single API endpoint, No card reissuance, Live in weeks, Major processor compatible) with inline SVG icons, plus the detail row with four integration specifics. DscCtaSection renders two CTAs (calculator + demo). /dynamic-security-codes page is fully complete, all six sections rendering end-to-end. Zero TypeScript errors. Zero lint errors. Build passes.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run lint` passes with zero errors
3. `npm run build` succeeds — both /platform and /dynamic-security-codes in build output
4. /dynamic-security-codes full end-to-end render (all six sections):
   - DscHeroSection: "Flagship Product" badge + problem frame
   - DscSolutionSection: 4 feature cards + cross-link to /platform
   - HowItWorksSection: 6 step cards grid
   - DscProofSection: three stat blocks (800,000+, 18 months, Zero) + An Post proof card
   - ForIssuersSection: 4 integration fact cards + technical detail row
   - DscCtaSection: calculator + demo CTA buttons
5. /platform full end-to-end render (all six sections) — both pages remain buildable after plan 03-04 changes
6. DscCtaSection imports from shared/PageCtaSection — same CTA component as PlatformCtaSection
7. Link from DscProofSection to /proof/an-post is present
</verification>

<success_criteria>
- DscProofSection: An Post stats (800,000+, 18 months, Zero), logo placeholder, award badge, link to case study
- ForIssuersSection: 4 integration fact cards with SVG icons + technical detail row with 4 checklist items
- DscCtaSection: renders shared PageCtaSection (calculator + demo CTAs)
- /dynamic-security-codes page fully complete — all 6 sections render in sequence
- Phase 3 complete: both /platform and /dynamic-security-codes are fully built
- Zero TypeScript errors, zero lint errors, build passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-platform-dynamic-security-codes/03-04-SUMMARY.md`
</output>
