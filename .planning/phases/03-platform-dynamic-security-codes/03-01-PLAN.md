---
phase: 03-platform-dynamic-security-codes
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/platform/page.tsx
  - src/components/marketing/platform/PlatformHeroSection.tsx
  - src/components/marketing/platform/ApproachSection.tsx
  - src/components/marketing/platform/ArchitectureDiagram.tsx
autonomous: true
requirements: [PLAT-01, PLAT-02, PLAT-03]

must_haves:
  truths:
    - "A visitor navigating to /platform sees a page headline and above-fold problem frame about static credentials being the root cause of CNP fraud"
    - "The SafeCypher dynamic approach section renders below the problem frame, explaining time-limited credentials through the cardholder's banking app"
    - "The architecture diagram renders labeled boxes for all seven products with connector lines — not a blank spacer"
    - "DSC and Safe Verify appear at primary visual weight in the diagram; the other five appear grouped at secondary weight"
    - "The page shares the marketing layout (Nav + Footer) inherited from (marketing)/layout.tsx"
  artifacts:
    - path: "src/app/(marketing)/platform/page.tsx"
      provides: "/platform route with metadata and section imports"
      exports: ["default function PlatformPage", "metadata"]
    - path: "src/components/marketing/platform/PlatformHeroSection.tsx"
      provides: "Problem frame — static credentials root cause argument"
      min_lines: 30
    - path: "src/components/marketing/platform/ApproachSection.tsx"
      provides: "SafeCypher dynamic approach section with link to /dynamic-security-codes"
      min_lines: 30
    - path: "src/components/marketing/platform/ArchitectureDiagram.tsx"
      provides: "One-API seven-products two-tier CSS diagram"
      min_lines: 60
  key_links:
    - from: "src/app/(marketing)/platform/page.tsx"
      to: "src/components/marketing/platform/PlatformHeroSection.tsx"
      via: "named import"
      pattern: "import.*PlatformHeroSection"
    - from: "src/app/(marketing)/platform/page.tsx"
      to: "src/components/marketing/platform/ArchitectureDiagram.tsx"
      via: "named import"
      pattern: "import.*ArchitectureDiagram"
    - from: "src/components/marketing/platform/ApproachSection.tsx"
      to: "/dynamic-security-codes"
      via: "Next.js Link"
      pattern: "href.*dynamic-security-codes"
---

<objective>
Build the /platform route (page.tsx with metadata) and its first three section components: the problem frame establishing static credentials as the CNP fraud root cause, the SafeCypher dynamic approach section linking to /dynamic-security-codes as the flagship example, and the two-tier CSS architecture diagram showing one SafeCypher API connecting to all seven products.

Purpose: Prospects arriving at /platform need an immediate argument — not a product pitch — that explains WHY the problem exists and HOW SafeCypher addresses it structurally. This is the architectural umbrella page that contextualises every product.
Output: /platform route with PlatformHeroSection, ApproachSection, and ArchitectureDiagram as Server Components. The page is importable and buildable; ProductPortfolioSection, CompetitiveSection, and PlatformCtaSection are stub-imported (plan 03-02 fills them).
</objective>

<execution_context>
@/Users/markwright/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markwright/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-platform-dynamic-security-codes/03-CONTEXT.md
@.planning/phases/03-platform-dynamic-security-codes/03-RESEARCH.md
@src/app/(marketing)/page.tsx
@src/components/marketing/home/OneIntegrationSection.tsx
@src/components/marketing/home/UrgencySection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold /platform route and stub imports for all six sections</name>
  <files>
    src/app/(marketing)/platform/page.tsx
    src/components/marketing/platform/ProductPortfolioSection.tsx
    src/components/marketing/platform/CompetitiveSection.tsx
    src/components/marketing/platform/PlatformCtaSection.tsx
  </files>
  <action>
Create `src/app/(marketing)/platform/page.tsx` as a Server Component (no 'use client'). Export a `metadata` object with `title: 'Platform Overview — SafeCypher'` and `description: 'One API integration. Seven fraud-prevention products. Built on dynamic credentials that make stolen card data worthless.'`. Import and render six section components in this order: PlatformHeroSection, ApproachSection, ArchitectureDiagram, ProductPortfolioSection, CompetitiveSection, PlatformCtaSection.

Create stub files for the three sections that plan 03-02 will fill:
- `src/components/marketing/platform/ProductPortfolioSection.tsx` — export `function ProductPortfolioSection() { return null }` (bare stub, no JSX overhead)
- `src/components/marketing/platform/CompetitiveSection.tsx` — export `function CompetitiveSection() { return null }`
- `src/components/marketing/platform/PlatformCtaSection.tsx` — export `function PlatformCtaSection() { return null }`

Do NOT add 'use client' to stubs — they must stay Server Components. Import pattern follows src/app/(marketing)/page.tsx exactly (named imports, @/ path alias).
  </action>
  <verify>Run `npx tsc --noEmit` from the project root. Zero type errors. Run `npm run build` — build succeeds with /platform in the output routes list.</verify>
  <done>page.tsx compiles cleanly with all six section imports. /platform route is in the Next.js build output. Three stub files exist and export their functions.</done>
</task>

<task type="auto">
  <name>Task 2: Build PlatformHeroSection — static credentials problem frame</name>
  <files>src/components/marketing/platform/PlatformHeroSection.tsx</files>
  <action>
Create `src/components/marketing/platform/PlatformHeroSection.tsx` as a pure Server Component (no 'use client'). This is the above-fold problem frame for /platform.

Structure:
- `<section>` with `bg-base-100 py-24 lg:py-32` (dark section, matches hero treatment)
- `max-w-5xl` container for editorial weight (narrower than standard 7xl — this is an argument, not a gallery)
- Eyebrow: `text-xs uppercase tracking-widest text-accent font-semibold` — "The Root Cause"
- Headline: `text-4xl lg:text-5xl font-bold text-base-content` — "Static credentials are the problem. Every tool that doesn't fix that is managing symptoms."
- Three-paragraph argument, each `text-base-content/70 text-lg leading-relaxed`:
  1. "Every card-not-present transaction relies on a credential that never changes — the 16-digit PAN, the expiry date, the CVV printed on the back of the card. Once stolen, those credentials work forever. Not until the card expires. Not until the next cycle. Forever."
  2. "Fraudsters don't need the physical card. They need the numbers. CNP fraud is not a verification failure — it is a credential design failure. Static credentials cannot be secured. They can only be monitored, flagged, and occasionally intercepted."
  3. "SafeCypher's position is simple: the only fix is a credential that expires before it can be used. Dynamic, time-sensitive, single-use. Everything else — tokenisation, 3DS, behavioural scoring — is valuable, and insufficient."
- No CTA in this section — the argument leads naturally into ApproachSection below.

Follow the exact section container pattern from existing components: `<section className="..."><div className="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8">...</div></section>`.
  </action>
  <verify>Run `npm run lint` — zero lint errors. Inspect the rendered section at /platform in `npm run dev` — eyebrow, headline, and three body paragraphs visible.</verify>
  <done>PlatformHeroSection renders the static credentials argument with eyebrow + headline + three paragraphs. No TypeScript errors. No lint errors.</done>
</task>

<task type="auto">
  <name>Task 3: Build ApproachSection and ArchitectureDiagram</name>
  <files>
    src/components/marketing/platform/ApproachSection.tsx
    src/components/marketing/platform/ArchitectureDiagram.tsx
  </files>
  <action>
**ApproachSection** (`src/components/marketing/platform/ApproachSection.tsx`) — pure Server Component:
- `<section className="bg-neutral border-t border-base-300 py-24 lg:py-32">` (lighter background, contrasts with dark hero above)
- `max-w-5xl` container
- Eyebrow: "The SafeCypher Approach" (text-accent uppercase tracking-widest text-xs font-semibold)
- Headline: "A credential that expires before it can be used." (text-3xl lg:text-4xl font-bold text-neutral-content)
- Body paragraph: "SafeCypher issues a new, time-limited security code for each transaction — delivered through the cardholder's existing banking app. No new card. No new device. No friction for genuine customers. And no usable credential for an attacker who intercepts it." (text-neutral-content/70 leading-relaxed)
- Feature row: three small cards (card bg-base-100 rounded-xl p-6) side-by-side (grid grid-cols-1 md:grid-cols-3 gap-6 mt-10):
  1. Icon (clock SVG inline, w-6 h-6 text-accent) + "Time-limited" heading + "Codes expire. Stolen credentials are worthless by the time they're used."
  2. Icon (smartphone SVG inline) + "App-native" heading + "Delivered through the cardholder's banking app. Zero new infrastructure for end users."
  3. Icon (link/chain SVG inline) + "One integration" heading + "A single processor-level API unlocks the full product suite." + Next.js Link to "/dynamic-security-codes" with text "See Dynamic Security Codes →" and className "text-accent text-sm mt-2 block hover:underline"
- Use inline SVGs (no icon package — locked Phase 1 decision). All icons: w-6 h-6 stroke-current, strokeWidth 2, no fill, text-accent.

**ArchitectureDiagram** (`src/components/marketing/platform/ArchitectureDiagram.tsx`) — pure Server Component:
Implement the two-tier CSS architecture diagram per the locked CONTEXT.md decision. All seven products named and labeled. DSC and Safe Verify at primary visual weight; five others at secondary weight.

Structure (flex column, items-center):
- Section wrapper: `<section className="bg-base-200 border-t border-base-300 py-16 lg:py-24">`
- Section header (text-center mb-10): eyebrow "One Integration" + h2 "One API. Seven products. Three audiences." (text-2xl lg:text-3xl font-bold)
- Top node: `<div className="bg-base-300 rounded-lg px-6 py-3 text-sm font-mono text-base-content/60 border border-base-300">Your Card Issuer / Processor</div>`
- Vertical connector: `<div className="border-l-2 border-accent h-8" />`
- API node: `<div className="bg-accent/20 border-2 border-accent rounded-xl px-8 py-4 text-sm font-mono text-accent font-bold text-center">SafeCypher Core API</div>`
- Vertical connector: `<div className="border-l-2 border-accent h-8" />`
- Primary tier label: `<p className="text-xs uppercase tracking-widest text-base-content/40 mb-3">Primary Products</p>`
- Primary tier: `<div className="flex flex-col sm:flex-row gap-4">` — two cards, `bg-primary/10 border-2 border-primary rounded-xl px-6 py-4 text-sm font-semibold text-primary text-center min-w-[11rem]`:
  - "Dynamic Security Codes" with `<p className="text-xs font-normal text-primary/60 mt-1">Transactions</p>` and `<Link href="/dynamic-security-codes" className="text-xs text-accent underline mt-2 block">Learn more →</Link>`
  - "Safe Verify" with `<p className="text-xs font-normal text-primary/60 mt-1">People</p>` and `<Link href="/safe-verify" className="text-xs text-accent underline mt-2 block">Learn more →</Link>`
- Vertical connector: `<div className="border-l-2 border-base-300 h-6" />`
- Secondary tier label: `<p className="text-xs uppercase tracking-widest text-base-content/40 mb-3">Additional Products</p>`
- Secondary tier: `<div className="flex flex-wrap justify-center gap-3">` — five nodes, `bg-base-300 rounded-lg px-4 py-3 text-xs font-mono text-base-content/60 text-center border border-base-300 min-w-[9rem]`:
  - SafeAgent / Agents
  - SafePay (dCVV V2) / Commerce
  - E-Wallet Onboarding / Onboarding
  - Card Issuance Protection / Issuance
  - OTP Replacement / Authentication
- Hide connector lines on mobile: add `hidden sm:block` to all vertical connector `<div>`s so the diagram degrades cleanly on narrow viewports (product cards still visible in stacked layout).
- Footer note: `<p className="text-xs text-base-content/30 text-center mt-8">Architecture diagram — integration detail available on request</p>`

Import `Link` from `'next/link'` for the product links in both components.
  </action>
  <verify>Run `npx tsc --noEmit` — zero errors. Run `npm run build` — succeeds. In `npm run dev`, navigate to /platform: ApproachSection renders three feature cards; ArchitectureDiagram renders the two-tier structure with all seven product labels visible.</verify>
  <done>ApproachSection shows three feature cards with inline SVG icons and a link to /dynamic-security-codes. ArchitectureDiagram shows SafeCypher Core API node connected to two primary product nodes (DSC, Safe Verify) and five secondary product nodes — all named and labeled with audience. No TypeScript or lint errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run lint` passes with zero errors
3. `npm run build` succeeds with /platform in the output routes
4. In `npm run dev`, /platform renders three sections: PlatformHeroSection (problem frame), ApproachSection (three feature cards), ArchitectureDiagram (two-tier labeled box diagram)
5. All seven products named in the diagram: Dynamic Security Codes, Safe Verify, SafeAgent, SafePay (dCVV V2), E-Wallet Onboarding, Card Issuance Protection, OTP Replacement
6. Link from ApproachSection to /dynamic-security-codes is present and correct
7. Stub files for ProductPortfolioSection, CompetitiveSection, PlatformCtaSection exist and return null without TypeScript errors
</verification>

<success_criteria>
- /platform route exists and builds without errors
- PlatformHeroSection renders the static credentials problem frame argument (eyebrow + headline + 3 paragraphs)
- ApproachSection renders the dynamic credential approach with 3 feature cards and a link to /dynamic-security-codes
- ArchitectureDiagram renders all 7 labeled product nodes in two-tier layout; DSC and Safe Verify at primary weight
- Three stub components exist for plan 03-02 to fill
- Zero TypeScript errors, zero lint errors, build passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-platform-dynamic-security-codes/03-01-SUMMARY.md`
</output>
