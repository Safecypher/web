---
phase: 04-safe-verify
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/components/marketing/safe-verify/SvNuclearKeySection.tsx
  - src/components/marketing/safe-verify/SvBenefitsSection.tsx
autonomous: true
requirements:
  - SV-03
  - SV-06

must_haves:
  truths:
    - "A visitor can see three key exchange cards labelled 01 Customer identification, 02 Agent identification, 03 Cementing the trust"
    - "The nuclear key section uses bg-neutral background creating dark contrast"
    - "The benefits section renders four benefit items with icons on the left and a metrics panel on the right"
    - "The metrics panel contains five rows including one with strikethrough text for traditional IDV"
    - "Copy for both sections is sourced from the source HTML with Claude discretion to tighten descriptions"
  artifacts:
    - path: "src/components/marketing/safe-verify/SvNuclearKeySection.tsx"
      provides: "Three-card nuclear key / key exchange section"
      min_lines: 80
    - path: "src/components/marketing/safe-verify/SvBenefitsSection.tsx"
      provides: "Four benefit items + metrics table panel"
      min_lines: 100
  key_links:
    - from: "SvBenefitsSection.tsx"
      to: "metrics panel"
      via: "flex row layout with strikethrough row"
      pattern: "line-through"
    - from: "SvNuclearKeySection.tsx"
      to: "three exchange cards"
      via: "grid grid-cols-3"
      pattern: "exchange-card"
---

<objective>
Implement `SvNuclearKeySection` (three-key exchange concept) and `SvBenefitsSection` (four benefits + metrics table). Both are Server Components porting content from the source HTML. The nuclear key section fulfils SV-03; the benefits section fulfils SV-06.

Purpose: These two sections together form the "why Safe Verify works" argument — the nuclear key framing explains the architecture, and the benefits section quantifies the value.

Output: Two fully-implemented Server Components with source HTML copy, DaisyUI token styling, and inline SVG icons.
</objective>

<execution_context>
@/Users/markwright/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markwright/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-safe-verify/04-CONTEXT.md
@.planning/phases/04-safe-verify/04-RESEARCH.md
@.planning/phases/04-safe-verify/04-01-SUMMARY.md
@_archive/safe-verify-landing.html
@src/components/marketing/dsc/ForIssuersSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: SvNuclearKeySection — three key exchange cards</name>
  <files>src/components/marketing/safe-verify/SvNuclearKeySection.tsx</files>
  <action>
    Replace the stub with the full `SvNuclearKeySection` implementation. Server Component — no 'use client'.

    **Section wrapper:** `<section className="bg-neutral border-t border-base-300 py-24 lg:py-32">`

    Rationale: bg-neutral creates the dark mid-page contrast in the rhythm: bg-base-100 (benefits above) → bg-neutral (nuclear key) → bg-base-100 (benefits below). This matches the locked section rhythm in CONTEXT.md.

    Actually the locked rhythm from CONTEXT.md is:
    Hero (bg-base-100) → Stats (bg-base-200) → Use-case tabs (bg-base-100) → Flow diagram (bg-base-200) → Nuclear key (bg-neutral) → Benefits (bg-base-100) → Integration (bg-base-200) → CTA.

    **Header:**
    ```jsx
    <div className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
      <p className="text-xs uppercase tracking-widest text-accent font-semibold mb-4">// The Nuclear Key Approach</p>
      <h2 className="text-3xl lg:text-4xl font-bold text-neutral-content">
        Three key exchanges.{' '}
        <span className="font-serif italic font-normal">One seamless moment.</span>
      </h2>
      <div className="w-16 h-0.5 bg-neutral-content/20 mt-6 mb-14" />
    ```

    **Three cards grid:** `<div className="grid grid-cols-1 md:grid-cols-3 gap-6">`

    Each card uses gradient border treatment from source HTML translated to DaisyUI tokens:
    ```jsx
    <div className="p-8 rounded-2xl bg-gradient-to-br from-primary/8 to-transparent border border-primary/15 hover:border-primary/40 transition-colors">
    ```

    **Card 1 — Customer identification** (source HTML lines 603–612):
    ```jsx
    <div className="text-6xl font-extrabold text-primary/15 leading-none mb-6 tracking-tight">01</div>
    <h3 className="text-lg font-semibold text-neutral-content mb-3">
      Customer <span className="font-serif italic font-normal">identification</span>
    </h3>
    <p className="text-sm text-neutral-content/60 leading-relaxed mb-5">
      Three layers of security achieved in a single, frictionless action through the banking app.
    </p>
    <ul className="space-y-2">
      {['Logs into phone (biometric)', 'Logs into banking app', 'Opens Safe Verify via deep link'].map((item) => (
        <li key={item} className="flex items-center gap-3 text-sm text-neutral-content/70">
          <span className="w-1.5 h-1.5 rounded-full bg-primary flex-shrink-0" />
          {item}
        </li>
      ))}
    </ul>
    ```

    **Card 2 — Agent identification** (source HTML lines 614–619 — Claude has discretion to tighten):
    ```jsx
    <div className="text-6xl font-extrabold text-primary/15 leading-none mb-6 tracking-tight">02</div>
    <h3 className="text-lg font-semibold text-neutral-content mb-3">
      Agent <span className="font-serif italic font-normal">identification</span>
    </h3>
    <p className="text-sm text-neutral-content/60 leading-relaxed">
      The in-app display shows the agent&rsquo;s name and department — triggered only by a legitimate call originating from the bank. A spoof call cannot replicate it. The customer sees proof, not hope.
    </p>
    ```

    **Card 3 — Cementing the trust** (source HTML lines 621–625 — Claude has discretion to tighten):
    ```jsx
    <div className="text-6xl font-extrabold text-primary/15 leading-none mb-6 tracking-tight">03</div>
    <h3 className="text-lg font-semibold text-neutral-content mb-3">
      Cementing <span className="font-serif italic font-normal">the trust</span>
    </h3>
    <p className="text-sm text-neutral-content/60 leading-relaxed">
      A final visible in-app interaction — the question/answer validation — confirms to the customer that they are engaging with their bank. Bidirectional trust, completed in one seamless moment.
    </p>
    ```
  </action>
  <verify>
    1. `npm run build` exits 0
    2. /safe-verify in dev: three cards visible with large ghost numbers 01 / 02 / 03
    3. bg-neutral background contrasts with the bg-base-200 flow diagram above and bg-base-100 benefits below
    4. Card 1 shows the three bullet points (biometric, app login, deep link)
    5. All text uses neutral-content tokens (not hardcoded colours)
  </verify>
  <done>
    - Three exchange cards render with source HTML copy
    - bg-neutral section background
    - Ghost large numerals 01 / 02 / 03 in primary/15 opacity
    - Card 1 includes three bullet layer items
    - Serif italic on "identification" (×2) and "the trust"
  </done>
</task>

<task type="auto">
  <name>Task 2: SvBenefitsSection — four benefits + metrics panel</name>
  <files>src/components/marketing/safe-verify/SvBenefitsSection.tsx</files>
  <action>
    Replace the stub with the full `SvBenefitsSection` implementation. Server Component — no 'use client'.

    **Section wrapper:** `<section className="bg-base-100 border-t border-base-300 py-24 lg:py-32">`

    **Header:**
    ```jsx
    <div className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
      <p className="text-xs uppercase tracking-widest text-accent font-semibold mb-4">// Why Safe Verify</p>
      <h2 className="text-3xl lg:text-4xl font-bold text-base-content">
        Easier for customers.{' '}
        <span className="font-serif italic font-normal">Impossible for fraudsters.</span>
      </h2>
      <div className="w-16 h-0.5 bg-base-content/20 mt-6 mb-14" />
    ```

    **Two-column grid:** `<div className="grid grid-cols-1 lg:grid-cols-2 gap-16 items-start">`

    **Left column — four benefit items** (source HTML lines 640–667, verbatim):

    Each item:
    ```jsx
    <div className="flex gap-5 items-start">
      <div className="w-12 h-12 rounded-xl flex-shrink-0 bg-primary/10 border border-primary/20 flex items-center justify-center">
        {/* inline SVG */}
      </div>
      <div>
        <h3 className="text-base font-semibold text-base-content mb-1">{title}</h3>
        <p className="text-sm text-base-content/60 leading-relaxed">{desc}</p>
      </div>
    </div>
    ```

    Benefit 1 — Lightning icon (SVG: `<path strokeLinecap="round" strokeLinejoin="round" d="M13 10V3L4 14h7v7l9-11h-7z" />`):
    - Title: "3–5 minutes saved per call"
    - Desc: "Eliminate repetitive security questions. Agents start serving customers immediately after bidirectional verification."

    Benefit 2 — Shield-check icon (SVG: `<path strokeLinecap="round" strokeLinejoin="round" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />`):
    - Title: "Vishing protection built in"
    - Desc: "Customers see verified caller identity in-app. Spoof calls that don&rsquo;t originate from the bank cannot trigger the verification display."

    Benefit 3 — Lock icon (SVG: `<path strokeLinecap="round" strokeLinejoin="round" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />`):
    - Title: "Zero data at rest"
    - Desc: "Semi-stateless architecture with on-the-fly calculations. No PII stored. No database to breach. GDPR compliant by design."

    Benefit 4 — Arrows-exchange icon (SVG: `<path strokeLinecap="round" strokeLinejoin="round" d="M8 7h12m0 0l-4-4m4 4l-4 4m0 6H4m0 0l4 4m-4-4l4-4" />`):
    - Title: "Bidirectional verification"
    - Desc: "Works for both inbound and outbound calls. Customer verifies the bank, bank verifies the customer — simultaneously."

    Wrap all four in `<div className="flex flex-col gap-10">`.

    **Right column — metrics panel** (source HTML lines 670–692, verbatim values):

    ```jsx
    <div className="relative rounded-2xl border border-base-300 bg-base-200 p-8 overflow-hidden">
      {/* Glow */}
      <div className="absolute -top-24 -right-24 w-64 h-64 rounded-full bg-primary/5 blur-3xl pointer-events-none" />

      {/* Five metric rows */}
      <div className="relative space-y-0 divide-y divide-base-300">
    ```

    Row structure:
    ```jsx
    <div className="flex justify-between items-center py-5">
      <span className="text-sm text-base-content/60">{label}</span>
      <span className={`text-2xl font-bold ${valueClass}`}>{value}</span>
    </div>
    ```

    Five rows:
    1. Label: "Call handling time reduction" | Value: "3–5 min" | valueClass: `text-primary`
    2. Label: "PII stored" | Value: "None" | valueClass: `text-success`
    3. Label: "Security layers" | Value: "3" | valueClass: `text-base-content`
    4. Label: "Integration time" | Value: "Weeks" | valueClass: `text-warning`
    5. Label: "Traditional IDV integration" | Value: "6–12 months" | valueClass: `text-base-content/30 line-through` (strikethrough — verbatim from source)

    Note: Row 5's value has BOTH `text-base-content/30` AND `line-through` Tailwind classes — this is the strikethrough row per the locked decision.

    All icons: `className="w-6 h-6 text-accent"`, `viewBox="0 0 24 24"`, `fill="none"`, `stroke="currentColor"`, `strokeWidth={2}`, `aria-hidden="true"`.
  </action>
  <verify>
    1. `npm run build` exits 0
    2. /safe-verify in dev: four benefit items with icons visible on the left
    3. Metrics panel visible on the right with five rows
    4. Row 5 ("Traditional IDV integration") has strikethrough text visible in browser
    5. "None" (row 2) appears in text-success colour
    6. "3–5 min" (row 1) appears in text-primary colour
  </verify>
  <done>
    - Four benefit items with inline SVG icons and verbatim copy from source HTML
    - Metrics panel with five rows, correct colour tokens per row
    - Strikethrough on "6–12 months" row (line-through Tailwind class)
    - bg-base-100 background per section rhythm
    - No hardcoded hex colours — all DaisyUI semantic tokens
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
- `npm run build` and `npm run lint` pass cleanly
- Nuclear key section: three cards with bg-neutral background, ghost numerals, serif italic headings
- Benefits section: four benefit items + five-row metrics panel with strikethrough row
- Section background rhythm confirmed: bg-base-200 (flow diagram) → bg-neutral (nuclear key) → bg-base-100 (benefits)
</verification>

<success_criteria>
- SvNuclearKeySection: bg-neutral, three cards, source HTML copy, three bullet layers in card 1
- SvBenefitsSection: bg-base-100, four items left, metrics panel right, strikethrough row, correct DaisyUI token colours
- Build clean, no TypeScript errors
- All Bootstrap Icons replaced with inline SVGs
</success_criteria>

<output>
After completion, create `.planning/phases/04-safe-verify/04-03-SUMMARY.md` following the summary template.
</output>
