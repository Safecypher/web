---
phase: 04-safe-verify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/safe-verify/page.tsx
  - src/components/marketing/safe-verify/SvHeroSection.tsx
  - src/components/marketing/safe-verify/SvUseCaseTabs.tsx
  - src/components/marketing/safe-verify/SvFlowDiagram.tsx
  - src/components/marketing/safe-verify/SvNuclearKeySection.tsx
  - src/components/marketing/safe-verify/SvBenefitsSection.tsx
  - src/components/marketing/safe-verify/SvIntegrationSection.tsx
  - src/components/marketing/safe-verify/SvCtaSection.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - SV-01

must_haves:
  truths:
    - "Visiting /safe-verify returns a Next.js page that builds without TypeScript errors"
    - "The hero section renders with the animated phone mockup floating and the notification card visible"
    - "The stats strip (Bidirectional trust / 3-5 min / Zero data / 3 layers) renders with bg-base-200 background"
    - "CSS animations sv-phone-float and sv-notif-slide are defined in globals.css and referenced in SvHeroSection"
    - "All seven safe-verify component stubs export named functions matching page.tsx imports"
  artifacts:
    - path: "src/app/(marketing)/safe-verify/page.tsx"
      provides: "Page compositor importing all seven SV components"
      contains: "SvHeroSection"
    - path: "src/components/marketing/safe-verify/SvHeroSection.tsx"
      provides: "Hero split-layout with animated phone mockup and stats strip"
      min_lines: 120
    - path: "src/app/globals.css"
      provides: "Animation keyframes for phone float and notification slide-in"
      contains: "sv-phone-float"
  key_links:
    - from: "src/app/(marketing)/safe-verify/page.tsx"
      to: "src/components/marketing/safe-verify/SvHeroSection.tsx"
      via: "named import"
      pattern: "import.*SvHeroSection"
    - from: "src/components/marketing/safe-verify/SvHeroSection.tsx"
      to: "src/app/globals.css"
      via: "className referencing animation name"
      pattern: "sv-phone-float"
---

<objective>
Scaffold the /safe-verify page and build its first section: SvHeroSection (hero split-layout with animated phone mockup + stats strip). Add required animation keyframes to globals.css. Create stub files for all remaining six components so the page compiles from the first commit — following the stub-first pattern established in plans 03-01 and 03-03.

Purpose: Establishes the page route and gives every subsequent plan a compilable base to add into. SvHeroSection is the first thing a visitor sees — porting the phone mockup faithfully is the highest-fidelity piece of work in this phase.

Output: Compilable /safe-verify page; fully implemented SvHeroSection with float animation and notification card; stub files for plans 04-02 through 04-04 to fill in.
</objective>

<execution_context>
@/Users/markwright/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markwright/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-safe-verify/04-CONTEXT.md
@.planning/phases/04-safe-verify/04-RESEARCH.md
@src/app/globals.css
@src/components/marketing/home/HeroCvvCard.tsx
@src/app/(marketing)/dynamic-security-codes/page.tsx
@_archive/safe-verify-landing.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Page scaffold — page.tsx + seven component stubs</name>
  <files>
    src/app/(marketing)/safe-verify/page.tsx
    src/components/marketing/safe-verify/SvHeroSection.tsx
    src/components/marketing/safe-verify/SvUseCaseTabs.tsx
    src/components/marketing/safe-verify/SvFlowDiagram.tsx
    src/components/marketing/safe-verify/SvNuclearKeySection.tsx
    src/components/marketing/safe-verify/SvBenefitsSection.tsx
    src/components/marketing/safe-verify/SvIntegrationSection.tsx
    src/components/marketing/safe-verify/SvCtaSection.tsx
  </files>
  <action>
    Create the directory `src/components/marketing/safe-verify/`.

    Create `src/app/(marketing)/safe-verify/page.tsx` — Server Component (no 'use client') with:
    - Metadata export: title "Safe Verify — SafeCypher", description "Safe Verify transforms your banking app into a secure verification channel. Eliminate vishing and bidirectionally verify every call."
    - Default export `SafeVerifyPage` composing all seven section components in this order:
      `<SvHeroSection />` → `<SvUseCaseTabs />` → `<SvFlowDiagram />` → `<SvNuclearKeySection />` → `<SvBenefitsSection />` → `<SvIntegrationSection />` → `<SvCtaSection />`
    - Import each from `@/components/marketing/safe-verify/{ComponentName}`

    Create stub files for the six components NOT being fully implemented in this plan. Each stub is a Server Component (no 'use client') with a named export returning `<section className="py-24"><div className="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8" /></section>`. Stubs:
    - `SvUseCaseTabs.tsx` — export name `SvUseCaseTabs`
    - `SvFlowDiagram.tsx` — export name `SvFlowDiagram`
    - `SvNuclearKeySection.tsx` — export name `SvNuclearKeySection`
    - `SvBenefitsSection.tsx` — export name `SvBenefitsSection`
    - `SvIntegrationSection.tsx` — export name `SvIntegrationSection`
    - `SvCtaSection.tsx` — export name `SvCtaSection`

    SvUseCaseTabs stub MUST NOT have 'use client' at this stage — it will be added in plan 04-02 when the real implementation replaces it.

    Run `npm run build` after creating stubs to confirm the page compiles. Fix any TypeScript errors before proceeding to Task 2.
  </action>
  <verify>npm run build exits 0 with no TypeScript errors related to safe-verify files</verify>
  <done>Visiting /safe-verify in dev server shows the page without runtime errors; seven stub sections render as empty areas</done>
</task>

<task type="auto">
  <name>Task 2: globals.css animations + SvHeroSection full implementation</name>
  <files>
    src/app/globals.css
    src/components/marketing/safe-verify/SvHeroSection.tsx
  </files>
  <action>
    STEP A — Add animation keyframes to `src/app/globals.css` (append after the existing `.hero-credit-card` block):

    ```css
    /* Safe Verify — phone mockup float */
    @keyframes sv-phone-float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-12px); }
    }
    .sv-phone-mockup {
      animation: sv-phone-float 6s ease-in-out infinite;
    }

    /* Safe Verify — notification card slide-in */
    @keyframes sv-notif-slide {
      from { opacity: 0; transform: translateY(-20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .sv-notif-card {
      animation: sv-notif-slide 0.8s ease-out 0.5s both;
    }
    ```

    STEP B — Implement `SvHeroSection.tsx` as a Server Component (no 'use client'). Structure:

    **Section wrapper:** `<section className="bg-base-100 py-24 lg:py-32 overflow-hidden relative">`
    - Radial glow div (absolute, pointer-events-none): `<div className="absolute top-1/4 left-1/2 -translate-x-1/2 w-[600px] h-[600px] rounded-full bg-primary/5 blur-3xl pointer-events-none" />`

    **Inner grid:** `<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">`

    **Left column (hero text):**
    - Product eyebrow: `<p className="text-xs uppercase tracking-widest text-accent font-semibold mb-4">Safe Verify</p>`
    - H1 (verbatim from source): Two lines rendered as:
      ```
      <h1 className="text-4xl lg:text-6xl font-bold text-base-content leading-tight tracking-tight">
        Vishing calls erode{' '}
        <span className="font-serif italic font-normal text-primary">customer trust.</span>
      </h1>
      ```
    - Accent line with italic text:
      ```
      <div className="flex items-center gap-4 mt-6">
        <div className="w-12 h-px bg-primary flex-shrink-0" />
        <p className="font-serif italic text-primary text-xl lg:text-2xl">Restore it instantly.</p>
      </div>
      ```
    - Description paragraph (verbatim from source HTML): "Safe Verify transforms your banking app into a secure verification channel. When your bank calls a customer, they know it&rsquo;s really you — confirmed in-app with biometric authentication. No more security questions. No more doubt."
      Classes: `text-base-content/60 text-lg leading-relaxed mt-4 max-w-xl`
    - CTA pair:
      ```
      <div className="flex flex-wrap gap-4 mt-8">
        <Link href="/#demo" className="btn btn-primary btn-lg">Request Demo</Link>
        <a href="#use-cases" className="btn btn-ghost btn-lg border border-base-content/20">See How It Works</a>
      </div>
      ```
      Import `Link` from `next/link`.

    **Right column (phone mockup):**
    Phone mockup is a pure CSS/HTML approximation of the source HTML — NO inline style attributes except where Tailwind cannot cover the value. Use the `.sv-phone-mockup` CSS class for the float animation.

    Phone mockup structure (translate Bootstrap Icons to inline SVGs — see SVG list below):
    ```jsx
    <div className="flex justify-center lg:justify-end">
      <div className="sv-phone-mockup w-[300px] bg-[#111] rounded-[36px] p-2 shadow-[0_40px_80px_rgba(0,0,0,0.5),0_0_0_1px_rgba(255,255,255,0.08)]">
        <div className="bg-[#0a0a0a] rounded-[28px] overflow-hidden">

          {/* Status bar */}
          <div className="flex justify-between px-6 pt-4 pb-2 text-[13px] font-semibold text-white">
            <span>9:41</span>
            <span className="flex items-center gap-1">
              {/* Lightning SVG for 5G indicator */}
              <svg xmlns="http://www.w3.org/2000/svg" className="w-3 h-3" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                <path d="M13 2L4.5 13.5H11L10 22L20 10H13L13 2Z"/>
              </svg>
              5G
            </span>
          </div>

          {/* Notch */}
          <div className="w-[120px] h-7 bg-[#111] rounded-b-2xl mx-auto -mt-2" />

          {/* Notification card */}
          <div className="sv-notif-card mx-4 my-3 p-4 bg-white/5 border border-primary/30 rounded-2xl">

            {/* Notif header: bank icon + text */}
            <div className="flex items-center gap-3 mb-3">
              <div className="w-10 h-10 bg-primary rounded-[10px] flex items-center justify-center flex-shrink-0">
                {/* Bank/building SVG */}
                <svg xmlns="http://www.w3.org/2000/svg" className="w-5 h-5 text-primary-content" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                  <path d="M2 10h20v2H2zm2 4h2v4H4zm4 0h2v4H8zm4 0h2v4h-2zm4 0h2v4h-2zM12 2L2 8h20L12 2zm0 2.5L18.5 8h-13L12 4.5z"/>
                </svg>
              </div>
              <div>
                <div className="text-[14px] font-semibold text-white">Safe Bank is calling</div>
                <div className="text-[12px] text-white/50">Verify this call in your app</div>
              </div>
            </div>

            {/* Verify box */}
            <div className="bg-primary/10 rounded-xl p-5 text-center">
              {/* Shield check SVG */}
              <svg xmlns="http://www.w3.org/2000/svg" className="w-12 h-12 text-primary mx-auto mb-2" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                <path d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4zm-2 16l-4-4 1.41-1.41L10 14.17l6.59-6.59L18 9l-8 8z"/>
              </svg>
              <div className="text-[16px] font-bold text-white">Ben Jordan</div>
              <div className="text-[13px] text-white/50 mb-4">Safe Bank &middot; Account Services</div>
              <span className="inline-flex items-center gap-1.5 bg-success/15 text-success px-3 py-1.5 rounded-full text-[12px] font-semibold">
                {/* Check circle SVG */}
                <svg xmlns="http://www.w3.org/2000/svg" className="w-3.5 h-3.5" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                  <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
                </svg>
                Verified Caller
              </span>
            </div>
          </div>

          {/* Face ID action button */}
          <div className="mx-4 mb-4 p-4 bg-primary rounded-2xl flex items-center justify-center gap-2 font-semibold text-[15px] text-primary-content">
            {/* Lock SVG */}
            <svg xmlns="http://www.w3.org/2000/svg" className="w-4 h-4" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
              <path d="M18 8h-1V6c0-2.76-2.24-5-5-5S7 3.24 7 6v2H6c-1.1 0-2 .9-2 2v10c0 1.1.9 2 2 2h12c1.1 0 2-.9 2-2V10c0-1.1-.9-2-2-2zm-6 9c-1.1 0-2-.9-2-2s.9-2 2-2 2 .9 2 2-.9 2-2 2zm3.1-9H8.9V6c0-1.71 1.39-3.1 3.1-3.1 1.71 0 3.1 1.39 3.1 3.1v2z"/>
            </svg>
            Confirm with Face ID
          </div>

          {/* Secured footer */}
          <div className="pb-3 flex items-center justify-center gap-1.5 text-[12px] text-white/30">
            {/* Shield lock SVG */}
            <svg xmlns="http://www.w3.org/2000/svg" className="w-3.5 h-3.5" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
              <path d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4z"/>
            </svg>
            Secured by SafeCypher
          </div>

        </div>
      </div>
    </div>
    ```

    **Stats strip** (separate section, `bg-base-200`, placed inside SvHeroSection below the grid, or as a sibling section — either is fine as long as background rhythm holds):

    This is a SECOND section element within SvHeroSection's return (or a wrapping fragment). Use `bg-base-200`:
    ```jsx
    <section className="bg-base-200 border-t border-base-300 py-16">
      <div className="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8 text-center">
        <p className="font-serif italic text-3xl lg:text-5xl text-primary mb-2">Bidirectional trust.</p>
        <p className="text-2xl font-bold text-base-content tracking-tight mb-1">Inbound &amp; outbound. Verified both ways.</p>
        <p className="text-xs uppercase tracking-widest font-semibold text-base-content/40 mb-10">Eliminating vishing fraud at the source</p>
        <div className="grid grid-cols-1 sm:grid-cols-3 gap-8 max-w-3xl mx-auto">
          <div>
            <div className="text-4xl font-extrabold text-base-content tracking-tight">3&ndash;5<span className="text-2xl text-primary"> min</span></div>
            <p className="text-sm text-base-content/50 mt-2">Saved per call by eliminating security questions</p>
          </div>
          <div>
            <div className="text-4xl font-extrabold text-base-content tracking-tight">Zero<span className="text-2xl text-primary"> data</span></div>
            <p className="text-sm text-base-content/50 mt-2">No PII stored. Semi-stateless architecture. GDPR compliant by design.</p>
          </div>
          <div>
            <div className="text-4xl font-extrabold text-base-content tracking-tight">3<span className="text-2xl text-primary"> layers</span></div>
            <p className="text-sm text-base-content/50 mt-2">Of security in a single frictionless interaction</p>
          </div>
        </div>
      </div>
    </section>
    ```

    SvHeroSection returns a React fragment (`<>...</>`) wrapping both the hero section and the stats section.

    Important: `#111` and `#0a0a0a` in the phone mockup are acceptable as inline style values only for the physical phone body and screen — those are literal device colours, not brand tokens. All other colours must use DaisyUI tokens (text-primary, bg-primary, text-primary-content, text-success, bg-success, etc.) replacing the source HTML's `--brand-blue: #0066FF`.
  </action>
  <verify>
    1. `npm run build` exits 0 — no TypeScript or JSX errors
    2. `npm run dev` — visit http://localhost:3000/safe-verify: phone mockup visible, floats smoothly, notification card slides in on load
    3. Stats strip shows bg-base-200 background (visually lighter than hero)
    4. No Bootstrap Icons CDN references anywhere in the component (all inline SVGs)
  </verify>
  <done>
    - /safe-verify loads without errors
    - Animated phone mockup with float + notification slide-in animations renders correctly
    - Stats strip (Bidirectional trust headline + three metrics) renders with bg-base-200 contrast
    - Two new CSS keyframe blocks appear in globals.css (sv-phone-float, sv-notif-slide)
    - All inline SVGs replace Bootstrap Icons
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
- `npm run build` passes with zero errors
- `npm run lint` passes (no ESLint warnings or errors for safe-verify files)
- `npx tsc --noEmit` passes
- /safe-verify page route is accessible
- Seven named exports exist in `src/components/marketing/safe-verify/`
- globals.css contains `sv-phone-float` and `sv-notif-slide` keyframe definitions
</verification>

<success_criteria>
- Build passes cleanly (TypeScript strict mode, no any types)
- /safe-verify page renders in dev mode with the hero section fully implemented
- Phone mockup float animation and notification slide-in animation visible in browser
- Stats strip (Bidirectional trust + 3 metrics) present with bg-base-200 contrast
- Six stub components exist and accept no props (Server Components)
- No Bootstrap Icon CDN dependency introduced
</success_criteria>

<output>
After completion, create `.planning/phases/04-safe-verify/04-01-SUMMARY.md` following the summary template at @/Users/markwright/.claude/get-shit-done/templates/summary.md
</output>
