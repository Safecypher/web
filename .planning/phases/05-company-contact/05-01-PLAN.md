---
phase: 05-company-contact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/company/page.tsx
  - src/components/marketing/company/CompanyHeroSection.tsx
  - src/components/marketing/company/CompanyMissionSection.tsx
  - src/components/marketing/company/CompanyBeliefsSection.tsx
  - src/components/marketing/company/CompanyHumanCostSection.tsx
  - src/components/marketing/company/CompanyTeamSection.tsx
  - src/components/marketing/company/CompanyCtaSection.tsx
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04

must_haves:
  truths:
    - "A visitor can navigate to /company and read a mission statement with 'eliminate fraud' framing"
    - "All five beliefs are visible as numbered cards with verbatim text from the archive"
    - "The Human Cost section renders with dedicated copy distinct from the homepage variant"
    - "A 3-column team grid shows 6 placeholder members with silhouette headshots, names, titles, and 1-line bios"
    - "The page ends with a shared CTA section linking to the calculator and demo"
  artifacts:
    - path: "src/app/(marketing)/company/page.tsx"
      provides: "/company route — Server Component compositor"
      exports: ["default CompanyPage"]
    - path: "src/components/marketing/company/CompanyBeliefsSection.tsx"
      provides: "Five numbered belief cards with verbatim archive text"
      contains: "Don't reduce"
    - path: "src/components/marketing/company/CompanyHumanCostSection.tsx"
      provides: "Dedicated Human Cost variant — not imported from home/"
      exports: ["CompanyHumanCostSection"]
    - path: "src/components/marketing/company/CompanyTeamSection.tsx"
      provides: "3-column team grid with 6 placeholder members and SVG silhouettes"
      exports: ["CompanyTeamSection"]
    - path: "src/components/marketing/company/CompanyCtaSection.tsx"
      provides: "Thin wrapper over shared PageCtaSection"
      exports: ["CompanyCtaSection"]
  key_links:
    - from: "src/app/(marketing)/company/page.tsx"
      to: "src/components/marketing/company/*"
      via: "named imports of each section component"
      pattern: "import.*CompanyHeroSection|CompanyMissionSection|CompanyBeliefsSection|CompanyHumanCostSection|CompanyTeamSection|CompanyCtaSection"
    - from: "src/components/marketing/company/CompanyCtaSection.tsx"
      to: "src/components/marketing/shared/PageCtaSection.tsx"
      via: "re-export wrapper"
      pattern: "import.*PageCtaSection"
---

<objective>
Build the complete /company page — all six sections as pure Server Components.

Purpose: Gives visitors the Company/About page that builds trust through the mission, beliefs, Human Cost narrative, and team.
Output: /company route with CompanyHeroSection, CompanyMissionSection, CompanyBeliefsSection, CompanyHumanCostSection, CompanyTeamSection, CompanyCtaSection — all wired through page.tsx.
</objective>

<execution_context>
@/Users/markwright/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markwright/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-company-contact/05-CONTEXT.md
@.planning/phases/05-company-contact/05-RESEARCH.md
@src/components/marketing/shared/PageCtaSection.tsx
@src/components/marketing/home/HumanCostSection.tsx
@src/app/(marketing)/safe-verify/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold company route and six component stubs</name>
  <files>
    src/app/(marketing)/company/page.tsx
    src/components/marketing/company/CompanyHeroSection.tsx
    src/components/marketing/company/CompanyMissionSection.tsx
    src/components/marketing/company/CompanyBeliefsSection.tsx
    src/components/marketing/company/CompanyHumanCostSection.tsx
    src/components/marketing/company/CompanyTeamSection.tsx
    src/components/marketing/company/CompanyCtaSection.tsx
  </files>
  <action>
Create the company directory and all seven files so /company compiles immediately from this task onward.

`src/app/(marketing)/company/page.tsx`:
- Export `metadata` with `title: 'Company — SafeCypher'` and `description: 'We built SafeCypher to eliminate card-not-present fraud — not manage it. Our mission, beliefs, and team.'`
- Default export `CompanyPage()` that imports and renders all six section components in this exact order: CompanyHeroSection, CompanyMissionSection, CompanyBeliefsSection, CompanyHumanCostSection, CompanyTeamSection, CompanyCtaSection
- Page is a Server Component — no 'use client'

Each of the six component stubs (`src/components/marketing/company/`):
- Named export matching the filename
- Return a `<section>` with a `data-stub` attribute and the component name as text content
- Pattern: `export function CompanyHeroSection() { return <section data-stub>CompanyHeroSection</section> }`

This stub-first pattern is established in prior phases (02-01, 03-01, 03-03) — it lets page.tsx compile and the route resolve before any section is built.
  </action>
  <verify>npm run build (or npx tsc --noEmit) — /company route must appear in build output with no TypeScript errors</verify>
  <done>Running `npx tsc --noEmit` reports zero errors; `src/app/(marketing)/company/page.tsx` imports all six stubs successfully</done>
</task>

<task type="auto">
  <name>Task 2: Implement CompanyHeroSection, CompanyMissionSection, CompanyBeliefsSection</name>
  <files>
    src/components/marketing/company/CompanyHeroSection.tsx
    src/components/marketing/company/CompanyMissionSection.tsx
    src/components/marketing/company/CompanyBeliefsSection.tsx
  </files>
  <action>
Replace the three stubs with full implementations. All three are pure Server Components — no 'use client'.

**CompanyHeroSection** (`bg-base-100 border-t border-base-300 py-24 lg:py-32`):
- Pattern matches other product page heroes (SvHeroSection, PlatformHeroSection): eyebrow tag + h1 + subhead paragraph
- Eyebrow: `text-xs uppercase tracking-widest text-accent font-semibold` — text "About SafeCypher"
- h1: bold, large (`text-4xl lg:text-5xl font-bold text-base-content`), uses serif italic pattern for the second clause. E.g.: "Built to eliminate fraud." with second part in `<span className="font-serif italic font-normal text-primary">` — Claude has discretion on exact copy
- Subhead: one paragraph at `text-xl text-base-content/60 max-w-2xl` describing the mission briefly
- Max width: `max-w-5xl` (matching argument-section pattern from Phase 3)

**CompanyMissionSection** (`bg-base-100 border-t border-base-300 py-24 lg:py-32`):
- Implements COMP-01: "eliminate fraud" framing elevated from archive copy
- Source framing from archive (RESEARCH.md): "Zero fraud. Not reduced. Eliminated." + award context
- Layout: eyebrow + large pull-quote or styled blockquote + supporting prose paragraphs
- Claude has discretion on prose vs blockquote layout — use `max-w-4xl` container for editorial feel
- Must include the "eliminate, not reduce" core message prominently; award reference (Irish Fintech Awards 2025) is appropriate to include
- No external dependencies

**CompanyBeliefsSection** (`bg-base-200 border-t border-base-300 py-24 lg:py-32`):
- Implements COMP-03: five beliefs as numbered cards — VERBATIM text from archive, no paraphrasing
- `const beliefs` array at module scope with exactly these five entries (copy verbatim):
  ```
  { num: '01', plain: "Don't reduce.", serif: 'Eliminate.' }
  { num: '02', plain: 'Dynamic data.', serif: 'Applied intelligence.' }
  { num: '03', plain: 'Nothing there.', serif: 'Nothing to steal.' }
  { num: '04', plain: 'Not probability, but', serif: 'certainty.' }
  { num: '05', plain: 'Easier for users.', serif: 'Impossible for fraudsters.' }
  ```
- Section heading: eyebrow "What We Believe" + h2 with serif italic pattern
- Card layout: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6`
- Each card: `bg-base-100 border border-base-300 rounded-2xl p-8`
- Large ghost number: `font-mono text-5xl font-bold text-primary/20 leading-none mb-6` (same ghost numeral pattern as Phase 4's SvBenefitsSection)
- Belief text: `text-xl font-semibold text-base-content` with plain text + `<span className="font-serif italic font-normal text-primary">` for the serif clause
- 5 cards in a 3-col grid = 3+2 layout; accept natural grid fill (no special last-row treatment needed)
  </action>
  <verify>Visit http://localhost:3000/company — Hero headline visible, Mission section has "eliminate" / "Zero fraud" framing, Beliefs section shows 5 numbered cards with verbatim text including "Don't reduce. Eliminate."</verify>
  <done>All five belief cards render with verbatim text; ghost numerals visible; Mission section contains "eliminate" framing; Hero section has serif italic headline</done>
</task>

<task type="auto">
  <name>Task 3: Implement CompanyHumanCostSection, CompanyTeamSection, CompanyCtaSection</name>
  <files>
    src/components/marketing/company/CompanyHumanCostSection.tsx
    src/components/marketing/company/CompanyTeamSection.tsx
    src/components/marketing/company/CompanyCtaSection.tsx
  </files>
  <action>
Replace the three remaining stubs with full implementations.

**CompanyHumanCostSection** (`bg-base-100 border-t border-base-300 py-24 lg:py-32`):
- Implements COMP-02: dedicated Human Cost variant — do NOT import from `src/components/marketing/home/HumanCostSection.tsx`
- This is a Company-context variant: framing expands on WHY SafeCypher exists, not just what fraud costs
- Structure follows the homepage variant pattern (read `src/components/marketing/home/HumanCostSection.tsx` for reference): eyebrow tag + h2 + pull stats with `border-l-4 border-error` + prose paragraphs
- Pull stats should include the human dimension: elderly victims, families, psychological impact (Claude has discretion on specific stats/copy — expand beyond the homepage variant's product pitch framing)
- Use `max-w-4xl` container for editorial feel (same as homepage HumanCostSection)
- The link at the bottom should point to `/contact` (Company page context: "Talk to us" → contact), not to `/#demo` like the homepage variant

**CompanyTeamSection** (`bg-base-100 border-t border-base-300 py-24 lg:py-32`):
- Implements COMP-04: 3-column team grid with 6 placeholder members and inline SVG silhouettes
- `type TeamMember = { name: string; title: string; bio: string }` defined at module scope
- `const team: TeamMember[]` array seeded with 6 placeholder members:
  ```
  { name: 'Mark Wright', title: 'CEO & Co-founder', bio: 'Fintech entrepreneur with a decade building fraud prevention infrastructure for card issuers.' }
  { name: '[Name]', title: 'CTO & Co-founder', bio: 'Placeholder bio — one sentence about technical background.' }
  { name: '[Name]', title: 'Head of Product', bio: 'Placeholder bio — one sentence about product background.' }
  { name: '[Name]', title: 'Head of Engineering', bio: 'Placeholder bio — one sentence about engineering background.' }
  { name: '[Name]', title: 'Head of Sales', bio: 'Placeholder bio — one sentence about sales background.' }
  { name: '[Name]', title: 'Head of Operations', bio: 'Placeholder bio — one sentence about operations background.' }
  ```
- `PersonSilhouette` component: inline SVG, `viewBox="0 0 80 80"`, neutral person icon. Use the pattern from RESEARCH.md Pattern 3: rect background + circle for head + path for shoulders. `aria-hidden="true"`
- Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8`
- Each card: `flex flex-col items-center text-center` — photo area `w-24 h-24 rounded-full overflow-hidden mb-4 bg-base-200`, name `font-semibold text-base-content`, title `text-sm text-primary mb-2`, bio `text-sm text-base-content/60 leading-relaxed`
- Section heading: eyebrow "The Team" + h2 with serif italic pattern

**CompanyCtaSection**:
- MUST be a thin 5-line wrapper over `PageCtaSection` — do NOT re-implement CTA content
- Import `PageCtaSection` from `@/components/marketing/shared/PageCtaSection` and render it directly
- Pattern mirrors SvCtaSection and DscCtaSection from Phase 4:
  ```tsx
  import { PageCtaSection } from '@/components/marketing/shared/PageCtaSection'
  export function CompanyCtaSection() { return <PageCtaSection /> }
  ```
  </action>
  <verify>
    1. Visit http://localhost:3000/company — scroll to Human Cost section: pull stats with border-error visible, copy references human impact (not just numbers)
    2. Team section: 3-column grid on desktop, 6 cards each with SVG silhouette + name + title + bio sentence
    3. CTA section at bottom: "Ready to eliminate CNP fraud?" heading + calculator + demo CTA links (from PageCtaSection)
    4. npm run build — zero TypeScript errors, /company in build output
  </verify>
  <done>
    - /company renders all six sections end-to-end without errors
    - Team grid shows 6 placeholder members with SVG silhouettes on 3-column desktop layout
    - CTA section matches other product page CTA sections (shared PageCtaSection content)
    - `npx tsc --noEmit` and `npm run lint` both pass with zero errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero TypeScript errors
2. `npm run lint` — zero ESLint errors
3. `npm run build` — /company appears in build output, no build failures
4. Visit http://localhost:3000/company:
   - Hero section with serif italic headline
   - Mission section with "eliminate"/"Zero fraud" framing
   - Beliefs section: 5 numbered cards, verbatim text ("Don't reduce. Eliminate." through "Easier for users. Impossible for fraudsters.")
   - Human Cost section with pull stats and border-error styling
   - Team section: 6 cards in 3-col grid with SVG silhouettes
   - CTA section with calculator + demo links
</verification>

<success_criteria>
- All six company sections render at /company without errors or console warnings
- Five belief cards contain verbatim archive text — no paraphrasing (COMP-03)
- CompanyHumanCostSection is a self-contained component — no import from home/HumanCostSection (COMP-02)
- CompanyCtaSection is a thin wrapper over PageCtaSection (5 lines max, no duplication)
- Build and lint pass clean
</success_criteria>

<output>
After completion, create `.planning/phases/05-company-contact/05-01-SUMMARY.md`
</output>
