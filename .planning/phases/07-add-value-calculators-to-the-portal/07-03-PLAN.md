---
phase: 07-add-value-calculators-to-the-portal
plan: "03"
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - src/app/(portal)/layout.tsx
  - src/app/(portal)/portal/page.tsx
  - src/app/(portal)/portal/calculator/page.tsx
  - src/components/portal/PortalSidebar.tsx
  - src/components/portal/calculator/CalculatorPage.tsx
  - src/components/portal/calculator/InputSlider.tsx
  - src/components/portal/calculator/ResultsPanel.tsx
  - src/components/portal/calculator/SavingsBarChart.tsx
  - src/components/portal/calculator/SensitivityTable.tsx
  - src/components/portal/calculator/PdfExportButton.tsx
  - src/app/actions/attio.ts
autonomous: true

requirements:
  - PORT-03
  - PORT-05

must_haves:
  truths:
    - "Authenticated user sees the portal dashboard at /portal with a link to /portal/calculator"
    - "The calculator at /portal/calculator renders sliders for debit accounts, credit accounts, fraud rate, and adoption rate in simple mode"
    - "Changing a slider updates the Year 1 Net Savings and Breakeven Days headline numbers in real time"
    - "The 'Advanced mode' toggle reveals the full set of inputs from the spreadsheet"
    - "The savings bar chart (Year 1 vs Ongoing) renders below the headline metrics"
    - "The sensitivity table shows 4 rows at 25%, 50%, 75%, 90% adoption"
    - "Calculator state is encoded in the URL — reloading the page preserves all slider values"
    - "The 'Download report' button generates and downloads a PDF with inputs and key outputs"
    - "A calculator_run Attio event fires via Server Action within 500ms of the last input change"
    - "The currency and regional defaults update when the region selector is changed (USD / GBP / EUR)"
  artifacts:
    - path: "src/app/(portal)/layout.tsx"
      provides: "Portal shell layout with PortalSidebar + NuqsAdapter"
      contains: "NuqsAdapter"
    - path: "src/app/(portal)/portal/calculator/page.tsx"
      provides: "Calculator route — passes searchParams to CalculatorPage client component"
    - path: "src/components/portal/PortalSidebar.tsx"
      provides: "Left sidebar nav with SafeCypher logo, Calculator link, logout button"
    - path: "src/components/portal/calculator/CalculatorPage.tsx"
      provides: "Main calculator orchestrator — all useQueryState bindings, region selector, mode toggle"
      contains: "useQueryState"
    - path: "src/components/portal/calculator/InputSlider.tsx"
      provides: "Slider with click-to-edit numeric override input"
    - path: "src/components/portal/calculator/ResultsPanel.tsx"
      provides: "Year 1 Net Savings + Breakeven Days headline + expandable sections"
    - path: "src/components/portal/calculator/SavingsBarChart.tsx"
      provides: "Recharts BarChart comparing Year 1 vs Ongoing savings"
    - path: "src/components/portal/calculator/SensitivityTable.tsx"
      provides: "4-row adoption sensitivity table (25%/50%/75%/90%)"
    - path: "src/components/portal/calculator/PdfExportButton.tsx"
      provides: "jsPDF download button — dynamic import to avoid SSR window error"
    - path: "src/app/actions/attio.ts"
      provides: "Server Action that calls /api/attio/event with INTERNAL_API_SECRET server-side"
  key_links:
    - from: "src/components/portal/calculator/CalculatorPage.tsx"
      to: "src/lib/calculator/engine.ts"
      via: "calculate(inputs) called on every input change"
      pattern: "calculate\\("
    - from: "src/components/portal/calculator/CalculatorPage.tsx"
      to: "src/app/actions/attio.ts"
      via: "debounced Server Action call on input change"
      pattern: "fireCalculatorRun"
    - from: "src/app/actions/attio.ts"
      to: "/api/attio/event"
      via: "fetch with absolute URL using BASE prefix"
      pattern: "NEXT_PUBLIC_SITE_URL"
    - from: "src/components/portal/calculator/SavingsBarChart.tsx"
      to: "recharts"
      via: "BarChart + ResponsiveContainer"
      pattern: "from 'recharts'"
    - from: "src/app/(portal)/layout.tsx"
      to: "nuqs/adapters/next/app"
      via: "NuqsAdapter wrapping children"
      pattern: "NuqsAdapter"
---

<objective>
Build the portal shell layout and the complete interactive value calculator UI. This plan delivers the full portal experience: sidebar nav, dashboard landing page, calculator with sliders, real-time results, chart, sensitivity table, PDF export, URL state sharing, and debounced Attio telemetry.

Purpose: This is the centrepiece of Phase 7 — the feature that converts authenticated prospects into demo requests by showing them their exact ROI numbers.
Output: Working authenticated portal at /portal and /portal/calculator with full formula-driven interactive UI.
</objective>

<execution_context>
@/Users/markwright/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markwright/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-add-value-calculators-to-the-portal/07-CONTEXT.md
@.planning/phases/07-add-value-calculators-to-the-portal/07-RESEARCH.md
@.planning/phases/07-add-value-calculators-to-the-portal/07-01-SUMMARY.md
@.planning/phases/07-add-value-calculators-to-the-portal/07-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 — src/lib/calculator/types.ts (key subset): -->
```typescript
export interface CalculatorInputs {
  debitAccounts: number
  creditAccounts: number
  debitCNPTransactions: number
  creditCNPTransactions: number
  debitAvgTxValue: number
  creditAvgTxValue: number
  year1AdoptionRate: number   // 0–1
  ongoingAdoptionRate: number // 0–1
  debitFraudRate: number      // e.g. 0.00046
  creditFraudRate: number     // e.g. 0.00174
  debitCvvPct: number
  creditCvvPct: number
  debitLossPerCase: number
  creditLossPerCase: number
  issuerPct: number
  institutionalCostMethod: 'Multiplier' | 'Fixed Amount'
  institutionalCostMultiplier: number
  debitFixedCostPerCase: number
  creditFixedCostPerCase: number
  feePerTx: number
  upliftPerAdopter: number
  oneTimeTsysCost: number
  annualTsysFee: number
  mobileDevCost: number
  peakHaloMultiplier: number
  monthsToPeak: number
  calculationMode: 'Direct Only' | 'Direct + Halo Effect'
  debitInterchangeRate: number
  creditInterchangeRate: number
  interchangePct: number
}

export interface CalculatorOutputs {
  cvvRequiredTxDebit: number
  cvvRequiredTxCredit: number
  annualFraudCasesDebit: number
  annualFraudCasesCredit: number
  currentAnnualFraudCostDebit: number
  currentAnnualFraudCostCredit: number
  currentAnnualFraudCostCombined: number
  totalImplCost: number
  grossFraudSavingsYr1: number
  txFeesYr1: number
  netFraudSavingsYr1: number
  monthlySavingsYr1: number
  breakevenDays: number
  grossFraudSavingsOngoing: number
  txFeesOngoing: number
  netFraudSavingsOngoing: number
  monthlySavingsOngoing: number
  interchangeUpliftYr1: number
  interchangeUpliftOngoing: number
  avgYr1HaloFactor: number
  yr1HaloBonus: number
  ongoingHaloBonus: number
  totalYr1Savings: number
  totalOngoingSavings: number
  totalYr1SavingsWithoutHalo: number
  totalOngoingSavingsWithoutHalo: number
  sensitivityRows: Array<{ adoptionRate: number; annualSavings: number; breakevenDays: number }>
}
```

<!-- From Plan 02 — src/lib/calculator/defaults.ts: -->
```typescript
export function getDefaultInputs(region: 'USD' | 'GBP' | 'EUR'): CalculatorInputs
export const DEFAULTS_USD: CalculatorInputs
```

<!-- From Plan 01 — Supabase client (for logout): -->
```typescript
// src/lib/supabase/client.ts
export function createClient(): SupabaseClient
// Usage in logout: supabase.auth.signOut() then router.push('/portal/login')
```

<!-- Existing Attio event route (updated in Plan 01): -->
<!-- POST /api/attio/event — accepts { event, email?, ...genericPayload } -->
<!-- Protected by x-internal-token header = INTERNAL_API_SECRET -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install remaining packages, portal shell layout, dashboard page, and Server Action</name>
  <files>
    src/app/(portal)/layout.tsx
    src/app/(portal)/portal/page.tsx
    src/components/portal/PortalSidebar.tsx
    src/app/actions/attio.ts
  </files>
  <action>
    **Install packages:**
    Run: `npm install recharts nuqs jspdf`

    **Update src/app/(portal)/layout.tsx** — replace stub passthrough with full portal shell:
    - Import NuqsAdapter from 'nuqs/adapters/next/app' — CRITICAL: add NuqsAdapter here NOT in the root layout.tsx
    - Import PortalSidebar component
    - Layout structure: `<NuqsAdapter><div className="flex min-h-screen bg-base-100"><PortalSidebar /><main className="flex-1 overflow-auto">{children}</main></div></NuqsAdapter>`
    - Add `import type { Metadata } from 'next'` and export metadata with title "SafeCypher Portal"
    - Do NOT import or render the marketing Nav or Footer — portal has its own navigation

    **Create src/components/portal/PortalSidebar.tsx:**
    - Client Component ('use client') — needs usePathname for active link highlighting and Supabase client for sign-out
    - Left sidebar: fixed width w-64, bg-base-200, full height (h-screen sticky top-0)
    - Top: SafeCypher logo/wordmark (text-based if no SVG, same as existing nav pattern)
    - Nav links (vertical stack, middle of sidebar):
      - Calculator: href="/portal/calculator", icon (calculator inline SVG), label "Calculator"
      - Demo: href="/portal/demo", icon (play button inline SVG), label "Agentic Demo"
    - Active link: bg-primary/10 text-primary, inactive: text-base-content/70 hover:text-base-content
    - Bottom: user email display (from supabase.auth.getUser — use useEffect on mount) + Sign out button
    - Sign out: calls createClient().auth.signOut() then router.push('/portal/login') using useRouter

    **Update src/app/(portal)/portal/page.tsx** — minimal dashboard:
    - Server Component (no 'use client' needed)
    - Simple welcome card: "Welcome to SafeCypher Portal" heading, brief description "Your ROI calculator and agentic commerce demo are ready."
    - Two action buttons: "Open Calculator" (primary, href="/portal/calculator") and "View Demo" (ghost, href="/portal/demo")
    - Fire portal_login Attio event: import the Server Action `firePortalLogin` and call it on page load via a separate Client Component wrapper (or use useEffect in a client wrapper)
    - SIMPLER APPROACH for portal_login: create a thin client wrapper `PortalLoginTracker` component that uses useEffect to call a server action on mount — this avoids making the dashboard page a client component

    **Create src/app/actions/attio.ts** — Server Actions for Attio events:
    - 'use server' directive at top
    - ABSOLUTE URL REQUIRED: Server Actions run server-side where relative URLs are invalid and will throw a network error. Define at the top of the file:
      `const BASE = process.env.NEXT_PUBLIC_SITE_URL ?? 'http://localhost:3000'`
    - `fireCalculatorRun(inputs: CalculatorInputs, outputs: CalculatorOutputs, userEmail?: string)` — calls fetch(`${BASE}/api/attio/event`, { method: 'POST', headers: { 'Content-Type': 'application/json', 'x-internal-token': process.env.INTERNAL_API_SECRET! }, body: JSON.stringify({ event: 'calculator_run', email: userEmail, inputs, outputs }) })
    - `firePortalLogin(email: string)` — calls fetch(`${BASE}/api/attio/event`, ...) with { event: 'portal_login', email }
    - Both functions: fire-and-forget (no await needed from callers, but use try/catch internally to avoid unhandled rejection)
    - IMPORTANT: process.env.INTERNAL_API_SECRET is server-only here — it never reaches the browser because Server Actions run server-side
    - Do NOT use relative URL '/api/attio/event' — relative URLs are invalid in server-side fetch (no implicit host). Always use `${BASE}/api/attio/event`.

    **Verify:**
    Run `npm run build` — must pass. The portal shell must compile without error.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>Run `npm run dev`. Visit http://localhost:3000/portal — confirm redirect to login (if not authed). After auth, confirm sidebar renders with Calculator and Demo links. Check /portal page shows welcome content.</manual>
  </verify>
  <done>Portal layout renders with sidebar nav. /portal page shows dashboard content. Server Actions file exists and compiles. NuqsAdapter wraps portal layout only (not root layout). Both Server Actions use absolute fetch URL via BASE constant.</done>
</task>

<task type="auto">
  <name>Task 2: Calculator route, CalculatorPage orchestrator, InputSlider, and ResultsPanel (core interactive loop)</name>
  <files>
    src/app/(portal)/portal/calculator/page.tsx
    src/components/portal/calculator/CalculatorPage.tsx
    src/components/portal/calculator/InputSlider.tsx
    src/components/portal/calculator/ResultsPanel.tsx
  </files>
  <action>
    **Create src/app/(portal)/portal/calculator/page.tsx:**
    - Server Component — reads searchParams and passes portfolioSize to CalculatorPage if present
    - Import CalculatorPage dynamically with `{ ssr: false }` if needed, or simply: `import { CalculatorPage } from '@/components/portal/calculator/CalculatorPage'`
    - Render: `<CalculatorPage portfolioSize={searchParams.portfolioSize as string | undefined} />`
    - Export metadata: title "Value Calculator | SafeCypher Portal"

    **Create src/components/portal/calculator/InputSlider.tsx:**
    - Client Component ('use client')
    - Props: `{ label: string; value: number; min: number; max: number; step: number; unit?: string; prefix?: string; format?: (v: number) => string; onChange: (v: number) => void; tooltip?: string }`
    - Layout: label row (label + optional tooltip info icon) + slider row + value display
    - Slider: `<input type="range" .../>` styled with DaisyUI range class
    - Value display: clicking it switches to a text input for direct numeric entry; on blur or Enter: parse and clamp to [min, max] then call onChange
    - Tooltip: if tooltip prop present, show an inline `?` icon with DaisyUI tooltip (tooltip attribute on span)

    **Create src/components/portal/calculator/CalculatorPage.tsx:**
    - Client Component ('use client')
    - Props: `{ portfolioSize?: string }` — used to pre-populate debit+credit accounts on first load
    - Portfolio size pre-population: if portfolioSize passed, split 70/30 debit/credit (70% debit = debitAccounts default, 30% credit = creditAccounts default) — apply as initial defaults
    - Import useQueryState from 'nuqs'; parseAsFloat, parseAsInteger from 'nuqs'
    - All CalculatorInputs bound to URL via useQueryState with short param keys (e.g., 'da' for debitAccounts, 'ca' for creditAccounts, 'y1a' for year1AdoptionRate, 'oa' for ongoingAdoptionRate, etc.) — use getDefaultInputs('USD') values as defaults
    - Region state: useQueryState('region', parseAsString.withDefault('USD')) — on change, call getDefaultInputs(region) and update all relevant params
    - Detect browser locale on mount: if navigator.language starts with 'en-GB' set GBP, if 'fr' or 'de' or locale in EU list set EUR, else USD — only set region if it hasn't been manually overridden (check if URL has 'region' param)
    - Mode toggle state: useQueryState('mode', parseAsString.withDefault('Direct + Halo Effect'))
    - Simple/advanced toggle: local useState (not URL — this is a UX preference, not a shared state)
    - Compute outputs: `const outputs = useMemo(() => calculate(inputs), [inputs])` — construct inputs object from all useQueryState values
    - Debounced Attio: useRef for timeout; on any input change, clearTimeout + setTimeout(500ms, () => fireCalculatorRun(inputs, outputs, userEmail))
    - userEmail: useState set from supabase.auth.getUser() in useEffect on mount
    - Layout: two-column on desktop (lg:grid-cols-[1fr_1fr]), full-width on mobile
      - Left col: region selector (select dropdown) + simple/advanced toggle + InputSliders
      - Right col: ResultsPanel (sticky top-4)
    - Simple mode sliders: debitAccounts, creditAccounts, debitFraudRate (shown as % * 100), year1AdoptionRate (shown as %)
    - Advanced mode adds: creditFraudRate, debitCNPTransactions, creditCNPTransactions, debitCvvPct, creditCvvPct, debitLossPerCase, creditLossPerCase, issuerPct, institutionalCostMethod (select not slider), upliftPerAdopter, mobileDevCost, oneTimeTsysCost, annualTsysFee, peakHaloMultiplier, monthsToPeak
    - Adoption rate tooltip: "Making SafeCypher mandatory can drive 90%+ adoption. Higher adoption = dramatically higher savings."
    - Interchange uplift tooltip: "An Post saw 50% uplift in transaction volume. 1 = conservative estimate."
    - Below the two-column layout: render SavingsBarChart, SensitivityTable, and PdfExportButton (these components are built in Task 3 — import them but they will be available when this task is executed as part of the full plan)

    **Create src/components/portal/calculator/ResultsPanel.tsx:**
    - Client Component ('use client') — receives `outputs: CalculatorOutputs` and `currencySymbol: string` props
    - Headline section (prominent): Year 1 Net Savings (totalYr1Savings) in large text + Breakeven in Days
    - Format currency: helper formatCurrency(value, symbol) = `${symbol}${value.toLocaleString('en', { minimumFractionDigits: 0, maximumFractionDigits: 0 })}`
    - With/without halo comparison: small text below headline "Without halo effect: ${totalYr1SavingsWithoutHalo}"
    - Halo effect explanation inline: "Fraudsters migrate away from issuers with strong security reputations. This bonus compounds over time."
    - Expandable sections using DaisyUI collapse (or simple useState toggle):
      - Fraud Savings (netFraudSavingsYr1, netFraudSavingsOngoing, grossFraudSavingsYr1, txFeesYr1)
      - Interchange Uplift (interchangeUpliftYr1, interchangeUpliftOngoing)
      - Halo Effect Bonus (yr1HaloBonus, ongoingHaloBonus, avgYr1HaloFactor shown as %)
      - Implementation Cost (totalImplCost, with line items: TSYS setup, annual fee, mobile dev)
    - Contact CTA at bottom: Link to `/contact?from=calculator` with text "Talk to us about your results" — pass key metrics as URL params (totalYr1Savings, breakevenDays, debitAccounts, creditAccounts)

    **Verify:**
    Run `npm run build` && `npm run lint` — must both pass.
  </action>
  <verify>
    <automated>npm run build && npm run lint</automated>
    <manual>Run `npm run dev`. Log in, navigate to /portal/calculator. Verify: (1) sliders render and moving them updates results in real time, (2) URL params update as sliders move, (3) reloading preserves values, (4) advanced mode toggle shows additional inputs, (5) ResultsPanel shows headline savings and breakeven.</manual>
  </verify>
  <done>Calculator renders at /portal/calculator with working sliders. Results (Year 1 Net Savings + Breakeven Days) update in real time. URL state synced — reloading preserves values. Advanced mode toggle works. Build and lint pass.</done>
</task>

<task type="auto">
  <name>Task 3: SavingsBarChart, SensitivityTable, and PdfExportButton (visualization layer)</name>
  <files>
    src/components/portal/calculator/SavingsBarChart.tsx
    src/components/portal/calculator/SensitivityTable.tsx
    src/components/portal/calculator/PdfExportButton.tsx
  </files>
  <action>
    **Create src/components/portal/calculator/SavingsBarChart.tsx:**
    - Client Component ('use client') — REQUIRED: recharts uses browser APIs
    - Props: `{ yr1Total: number; ongoingTotal: number; currencySymbol: string }`
    - Use ResponsiveContainer + BarChart + Bar from recharts
    - Follow exact pattern from RESEARCH.md "Recharts Bar Chart" code example
    - Y-axis ticker: format in millions (${(v/1_000_000).toFixed(1)}M)
    - Bar fill: `var(--color-primary)` — uses DaisyUI/Tailwind token
    - Two bars: "Year 1" (yr1Total) and "Ongoing" (ongoingTotal)
    - Include XAxis labels: "Year 1 Savings" and "Ongoing Annual Savings"

    **Create src/components/portal/calculator/SensitivityTable.tsx:**
    - Client Component or Server Component (no hooks needed — pure display)
    - Props: `{ rows: Array<{adoptionRate: number; annualSavings: number; breakevenDays: number}>; currencySymbol: string }`
    - Renders a DaisyUI table with columns: Adoption Rate, Annual Savings, Breakeven (days)
    - Highlight the 90% row (mandatory adoption) with a note: "Mandatory adoption"
    - Adoption rate displayed as percentage (e.g., "25%")
    - Annual savings formatted with currencySymbol and toLocaleString

    **Create src/components/portal/calculator/PdfExportButton.tsx:**
    - Client Component ('use client')
    - Props: `{ inputs: CalculatorInputs; outputs: CalculatorOutputs; currencySymbol: string }`
    - Button label: "Download report"
    - onClick handler: uses dynamic import `const { jsPDF } = await import('jspdf')` — NEVER import jsPDF at module scope (causes SSR window error)
    - PDF content (plain A4 portrait, jsPDF direct draw):
      - Title: "SafeCypher Value Calculator Report"
      - Date: current date
      - Key metrics section: Year 1 Net Savings, Breakeven Days, Ongoing Annual Savings
      - Inputs summary: debitAccounts, creditAccounts, year1AdoptionRate, fraudRate
      - Implementation cost breakdown
    - Follow RESEARCH.md "jsPDF — Dynamic Import Pattern" code example exactly

    **Verify:**
    Run `npm run build` && `npm run lint` — must both pass.
  </action>
  <verify>
    <automated>npm run build && npm run lint</automated>
    <manual>Run `npm run dev`. Log in, navigate to /portal/calculator. Verify: (1) bar chart renders with two bars (Year 1, Ongoing), (2) sensitivity table shows 4 rows with the 90% row highlighted, (3) PDF download button generates and downloads a file when clicked.</manual>
  </verify>
  <done>Bar chart renders with Year 1 vs Ongoing comparison. Sensitivity table shows 4 rows (25%/50%/75%/90%). PDF download produces a file with inputs and key metrics. Build and lint pass.</done>
</task>

</tasks>

<verification>
- `npm run build` passes (no TypeScript errors, no recharts SSR errors, no nuqs errors)
- `npm run lint` passes
- Visiting /portal after login shows dashboard with sidebar
- Visiting /portal/calculator shows interactive calculator
- Moving sliders updates outputs immediately (no delay other than JS re-render)
- URL updates on slider change (can be copied and pasted to restore state)
- Bar chart renders with two bars (Year 1, Ongoing)
- Sensitivity table shows 4 rows
- PDF download button works in browser
- Attio calculator_run event fires to console (stub mode in dev)
- NuqsAdapter is in portal layout.tsx only — NOT in root layout.tsx
- Server Actions in src/app/actions/attio.ts use absolute URL: `${BASE}/api/attio/event` where BASE = `process.env.NEXT_PUBLIC_SITE_URL ?? 'http://localhost:3000'`
</verification>

<success_criteria>
- All calculator inputs bound to URL params via nuqs
- calculate() called on every change with correct inputs
- Results displayed: totalYr1Savings, breakevenDays as headline; expandable sections for breakdown
- Region selector changes currency symbol and defaults
- Advanced mode reveals full input set from RESEARCH.md locked decisions list
- Attio Server Action called with debounced 500ms delay on every input change
- Attio Server Actions use absolute fetch URL (BASE prefix) — relative URLs invalid server-side
- jsPDF loaded dynamically inside click handler only (no SSR window error)
- Recharts components all have 'use client' directive
</success_criteria>

<output>
After completion, create `.planning/phases/07-add-value-calculators-to-the-portal/07-03-SUMMARY.md`
</output>
