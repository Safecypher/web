---
phase: 07-add-value-calculators-to-the-portal
task: 1
total_tasks: 1
status: in_progress
last_updated: 2026-02-28T10:23:15.951Z
---

<current_state>
Phase 07 execution is 4/5 plans complete. Plans 07-01 through 07-04 all shipped and committed. Plan 07-05 is the human verification checkpoint — it's waiting for the user to type "approved" after manually testing the dev server.

However, before verification can complete there is a Node 24 compatibility issue: `npm run build` passes cleanly, but `npm run dev` (Turbopack) throws `TypeError: memoize is not a function` in `enhanced-resolve/lib/index.js:38` when processing `globals.css`. This happens because Turbopack evaluates `@tailwindcss/node`'s dependency `enhanced-resolve` in a browser/client context where Node.js built-ins aren't available.

A fix was just applied: `serverExternalPackages: ['enhanced-resolve', '@tailwindcss/node']` added to `next.config.ts`. This tells Turbopack to treat these as server-only externals rather than bundling/evaluating them. The fix has NOT been tested yet — the user needs to restart `npm run dev` to confirm.
</current_state>

<completed_work>

- 07-01: Supabase auth infrastructure — `src/proxy.ts` (Next.js 16 renamed middleware→proxy), `src/lib/supabase/{client,server,middleware}.ts`, `/portal/login` magic link form, `/portal/auth/callback` PKCE route, Attio route TypeError guard fixed (optional fields + `if (body.name && body.email)` guard). COMMITTED.
- 07-02: Calculator formula engine TDD — `src/lib/calculator/{types,defaults,engine,engine.test}.ts`. 14 Vitest tests all pass. Verified values: `totalYr1Savings=$3,866,043.47`, `breakevenDays=23.85`, `txFeesYr1Combined=$2,356,062.50`. COMMITTED.
- 07-03: Portal shell + calculator UI — `src/app/(portal)/layout.tsx` (NuqsAdapter portal-only), `PortalSidebar`, dashboard, `CalculatorPage` (30 URL-synced inputs via nuqs), `InputSlider`, `ResultsPanel`, `SavingsBarChart` (recharts), `SensitivityTable` (4 rows, "Mandatory adoption" badge), `PdfExportButton` (jsPDF dynamic import), `src/app/actions/attio.ts` (Server Actions with absolute BASE URL). COMMITTED.
- 07-04: Portal funnel — `/portal/demo` page (iframe + context panel + `DemoPageTracker` client island), `fireMockupViewed` added to attio.ts, homepage DemoFormSection teaser widget (number input → portfolioSize redirect), ContactFormSection calculator results summary box. COMMITTED.
- Node 24 compatibility: updated `tailwindcss` → 4.2.1, `@tailwindcss/postcss` → latest, `@types/node` → ^25.3.2 (installed 25.3.2). `npm run build` confirmed passing with Node v24.5.0.
- `next.config.ts` fix: added `serverExternalPackages: ['enhanced-resolve', '@tailwindcss/node']` to resolve Turbopack + Node 24 CSS processing error. NOT YET TESTED.
</completed_work>

<remaining_work>

1. **Confirm the `next.config.ts` fix works**: user restarts `npm run dev` — should load `localhost:3000` without the `memoize is not a function` error.
   - If fix works → proceed to 07-05 human verification
   - If fix doesn't work → investigate further (possible alternatives: `transpilePackages`, Turbopack `resolveAlias`, or pinning `enhanced-resolve` to a version that handles CJS exports differently)

2. **07-05 human verification** (checkpoint:human-verify): User runs through the full verification checklist from the 07-05 plan (auth redirect, magic link, calculator math accuracy ~$3.87M/23-24 days, URL state, bar chart, sensitivity table, PDF export, Attio stubs in console, homepage teaser, contact form results). Then types "approved".

3. **After "approved"**: Spawn continuation agent for 07-05 to create SUMMARY.md, update STATE.md and ROADMAP.md.

4. **Phase verification**: Spawn `gsd-verifier` agent for phase 07 goal achievement check → VERIFICATION.md.

5. **Roadmap update**: `gsd-tools phase complete 07` → mark phase complete, advance STATE.md.
</remaining_work>

<decisions_made>

- Next.js 16 renamed `middleware.ts` → `proxy.ts` and `export function middleware` → `export function proxy` (confirmed in Next.js 16 changelog). Agent auto-detected and applied correctly.
- `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` is the new Supabase dashboard name for the anon key (confirmed via Supabase live docs). Plan used this correct name throughout.
- `getUser()` (not `getSession()`) used in Supabase middleware — confirmed by Context7 docs as the secure pattern (getSession can be spoofed via cookie).
- `@types/node` updated to `^25.3.2` (latest) rather than `^24` — latest is compatible.
- Node 24 target: user wants to run on Node 24 (not downgrade to Node 20) for longevity.
- `serverExternalPackages` approach chosen for Turbopack fix over alternatives (transpilePackages, resolveAlias) — most semantically correct since `enhanced-resolve` is a server/build tool, not a browser module.
</decisions_made>

<blockers>

- **Turbopack + Node 24 + @tailwindcss/node**: `npm run dev` throws `TypeError: memoize is not a function` in `enhanced-resolve`. `npm run build` works fine. Fix applied (`serverExternalPackages`) but not yet confirmed working. If this approach fails, investigate:
  - Check if `@tailwindcss/node` has nested `node_modules/enhanced-resolve` with a different version
  - Try `transpilePackages: ['enhanced-resolve']` instead
  - Try Turbopack `resolveAlias` to swap the module
  - Check if Next.js 16.x.y patch releases have a Tailwind v4 + Node 24 fix
</blockers>

<context>
We're executing Phase 07 in the GSD workflow. The execute-phase orchestrator is running wave-by-wave. All code waves (1-3) are complete. Wave 4 (07-05) is a human-verify checkpoint that needs dev server to work first.

The Attio executor agent ID for 07-05 is: `ad0767e07e2fdfcf5` — this was the agent that ran the automated checks and returned the checkpoint. Do NOT resume it; spawn a fresh continuation agent for 07-05 after the user types "approved".

The 07-05 continuation agent prompt should follow the checkpoint continuation pattern: include completed tasks table (automated build+lint passed), resume task = Task 1, user_response = "approved", and instruct it to create SUMMARY.md + update STATE.md + ROADMAP.md.
</context>

<next_action>
1. User runs `npm run dev` in their terminal to test the `serverExternalPackages` fix
2. If dev server loads without `memoize` error: user proceeds through 07-05 verification checklist (auth, calculator math, PDF, Attio console stubs, homepage teaser, contact form)
3. User types "approved" in Claude
4. Spawn fresh 07-05 continuation agent to create SUMMARY.md
5. Spawn gsd-verifier for phase 07
6. Run `gsd-tools phase complete 07`
</next_action>
