---
phase: 07-add-value-calculators-to-the-portal
plan: "05"
type: execute
wave: 4
depends_on:
  - "07-04"
files_modified: []
autonomous: false

requirements:
  - PORT-02
  - PORT-03
  - PORT-04
  - PORT-05
  - PORT-06
  - PORT-07
  - PORT-08
  - HOME-07

must_haves:
  truths:
    - "The complete portal funnel works end-to-end: homepage teaser → login → calculator with pre-filled portfolio size"
    - "Unauthenticated requests to all /portal/* routes (except /portal/login) are redirected"
    - "The calculator produces results matching expected spreadsheet output for USD defaults"
    - "The PDF download generates a real file"
    - "The Attio event route accepts calculator_run payloads without error"
  artifacts:
    - path: "src/middleware.ts"
      provides: "Auth protection verified working"
    - path: "src/app/(portal)/portal/calculator/page.tsx"
      provides: "Calculator page fully functional"
    - path: "src/components/marketing/home/DemoFormSection.tsx"
      provides: "Teaser widget functional"
---

<objective>
Human verification of the complete Phase 7 feature set. Confirm the full portal funnel, calculator accuracy, PDF export, and Attio event firing before phase is marked complete.

Purpose: This is a complex multi-piece feature spanning auth, formula math, UI interactions, and external service integration. Human verification catches visual issues, broken flows, and math discrepancies that automated build checks cannot.
Output: Verified Phase 7 feature set; any issues identified for immediate fix.
</objective>

<execution_context>
@/Users/markwright/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markwright/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-add-value-calculators-to-the-portal/07-CONTEXT.md
@.planning/phases/07-add-value-calculators-to-the-portal/07-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end portal and calculator verification</name>
  <files>N/A — verification only, no files modified</files>
  <action>Run `npm run dev` and execute the 26-step verification sequence in how-to-verify below to confirm all Phase 7 requirements are met end-to-end.</action>
  <what-built>
    Complete portal: Supabase magic link auth protecting /portal/*, minimal dashboard, interactive value calculator with sliders + results + bar chart + sensitivity table + PDF export + URL state, agentic demo page, homepage teaser widget, and contact form calculator pre-fill.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and verify the following flows:

    **Auth flow (PORT-02):**
    1. Open an incognito window. Navigate to http://localhost:3000/portal/calculator
    2. Confirm redirect to http://localhost:3000/portal/login?callbackUrl=%2Fportal%2Fcalculator
    3. Enter a real email address. Confirm "Check your email" state appears.
    4. Click the magic link in the email. Confirm redirect lands on /portal/calculator with a session active.
    5. Refresh /portal/calculator — confirm you stay logged in (session preserved).

    **Dashboard (PORT-03):**
    6. Navigate to http://localhost:3000/portal
    7. Confirm: portal sidebar visible with Calculator and Demo links. Welcome content with action buttons.

    **Calculator (PORT-05):**
    8. On /portal/calculator, confirm sliders render for: Debit Accounts, Credit Accounts, Fraud Rate, Adoption Rate (Year 1).
    9. Move the Debit Accounts slider. Confirm Year 1 Net Savings headline updates immediately.
    10. Click the displayed value on a slider — confirm it becomes an editable text input.
    11. Click "Advanced mode" toggle — confirm additional inputs appear.
    12. Check that Year 1 Net Savings is a plausible number given USD defaults (should be in the millions range for 5M debit accounts, 25% adoption).
    13. Copy the URL. Open a new tab and paste it — confirm sliders restore to the same values.

    **Chart and table (PORT-05):**
    14. Confirm the bar chart shows two bars (Year 1 vs Ongoing savings).
    15. Confirm the sensitivity table shows 4 rows: 25%, 50%, 75%, 90% adoption.

    **PDF export (PORT-05):**
    16. Click "Download report". Confirm a PDF file downloads with SafeCypher branding and key metrics.

    **Attio events (PORT-04, PORT-05):**
    17. In the terminal running `npm run dev`, check console output. After loading /portal and /portal/calculator:
        - Look for `[Attio stub] { event: 'portal_login', ... }` in console
        - Move a slider — after ~500ms look for `[Attio stub] { event: 'calculator_run', ... }` in console

    **Agentic demo (PORT-07):**
    18. Navigate to http://localhost:3000/portal/demo
    19. Confirm: context panel on the right, iframe area on the left. Check console for `[Attio stub] { event: 'mockup_viewed', ... }`.

    **Homepage teaser (HOME-07, PORT-08):**
    20. Navigate to http://localhost:3000 and scroll to the demo form section.
    21. Enter "1000000" in the portfolio size field. Click "See your savings".
    22. Confirm redirect to /portal/login?callbackUrl=... containing portfolioSize=1000000.
    23. After auth, confirm /portal/calculator shows debit accounts ~700,000 and credit accounts ~300,000.

    **Contact CTA pre-fill (PORT-06):**
    24. Navigate to http://localhost:3000/contact?from=calculator&yr1=5000000&breakeven=45
    25. Confirm heading shows "Talk to us about your results".
    26. Confirm a "Your calculator results" summary box appears showing Year 1 savings and breakeven days.
  </how-to-verify>
  <verify>npm run build && npm run lint</verify>
  <done>All 26 verification steps pass. Build and lint are clean. Phase 7 is feature-complete.</done>
  <resume-signal>Type "approved" if all 26 checks pass, or describe which checks failed</resume-signal>
</task>

</tasks>

<verification>
All 26 manual verification steps above pass.
</verification>

<success_criteria>
- Full auth-protected portal funnel working end-to-end
- Calculator produces plausible results with USD defaults
- PDF download works in browser
- Attio events appear in dev console for portal_login, calculator_run, mockup_viewed
- Homepage teaser redirects through auth to pre-filled calculator
- Contact form shows calculator results summary
- No console errors in browser dev tools during normal usage flows
</success_criteria>

<output>
After completion, create `.planning/phases/07-add-value-calculators-to-the-portal/07-05-SUMMARY.md`
</output>
